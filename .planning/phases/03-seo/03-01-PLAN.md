---
phase: 03-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/site.ts
  - src/composables/useToolSeo.ts
  - src/App.vue
  - src/tools/json-formatter/json-formatter.vue
  - src/tools/timestamp-converter/timestamp-converter.vue
  - src/pages/home.vue
  - src/pages/not-found.vue
  - vite.config.ts
autonomous: true
requirements: [ARCH-03]
estimate:
  tokens: 70000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - 每个工具页预渲染 HTML head 含「工具名 - little-utils」title、非空 description、canonical、og:title/og:description/og:url（SC-1 本地实证，D-05/D-06/D-09）
    - 首页预渲染 title 保持「little-utils — 开发者 & 办公工具箱」且不被 titleTemplate 追加「- little-utils」双后缀（D-06）
    - dist/404.html 预渲染存在且含 robots noindex，暗色基线 class="dark" 不被破坏（D-08 + Phase 2 FOUC 基线）
    - canonical/og:url 全部 import SITE_URL 派生，源码零散落硬编码域名（D-12）
  artifacts:
    - src/config/site.ts — SITE_URL 唯一定义点（纯常量，Node 安全）
    - src/composables/useToolSeo.ts — 工具页标准 meta 集归拢层（D-05）
    - dist/json-formatter.html / dist/timestamp-converter.html / dist/404.html（含 meta 的构建产物，plan 02 断言对象）
  key_links:
    - src/router/routes.ts meta.tool → useToolSeo → @unhead/vue → 静态 HTML head（A4 实证链——链断则全阶段 meta 落空，Task 1 fail-first 实证）
    - src/config/site.ts SITE_URL → useToolSeo ogUrl/canonical（D-12 消费方 1/4，其余两方在 plan 02）
  prohibitions:
    - statement: 禁止把 SITE_URL 域名硬编码进任何组件或页面——canonical/og:url 必须 import src/config/site.ts（D-12）
      status: active
      verification: grep src/pages src/tools src/composables 无 edgeone.app 字面量；产物 canonical href 以 SITE_URL 值开头
    - statement: 禁止依赖 titleTemplate 生成 og:title——og 三件套必须在 useSeoMeta 显式赋值（RESEARCH Pitfall 5，titleTemplate 只影响 <title>）
      status: active
      verification: 产物 grep property="og:title" 计数 = 1（Task 1 verify 已内置）；plan 02 check-dist a) 持续守护
    - statement: 禁止改动 vite.config.ts onBeforePageRender 暗色注入逻辑（Phase 2 D-05「绝对零闪烁」基线）
      status: active
      verification: build 后 grep -c 'class="dark"' dist/index.html ≥ 1（Phase 2 proven command）
---

# Plan 01: meta 注入层 tracer——SITE_URL 单源 + useToolSeo 端到端落盘实证

<objective>
打通「注册表元数据 → useSeoMeta → unhead → 预渲染 HTML head」的完整垂直切片，并以产物级 grep 实证 RESEARCH A4 假设（useSeoMeta 在 vite-ssg 渲染进程落盘 meta）。交付 SITE_URL 单源常量、useToolSeo 归拢 composable、全部页面 meta 注入（双工具页 + 首页 + 404 noindex）与 404 预渲染。

Purpose: SC-1「curl 可见 title/description」的本地前置；本切片失败（A4 落盘失败）意味着架构需调整（/server 导出或 onPageRendered 兜底），必须在最便宜的时刻暴露。
Output: src/config/site.ts、src/composables/useToolSeo.ts、5 个页面组件 meta 注入、vite-ssg includedRoutes 追加 /404。
</objective>

## Artifacts this phase produces

- `src/config/site.ts` — 导出 `SITE_URL` 常量（D-11 占位值 `https://little-utils.edgeone.app`；plan 03 按实际分配域名修正）
- `src/composables/useToolSeo.ts` — 导出 `useToolSeo()`（消费 `route.meta.tool`，D-05 标准集）
- `vite.config.ts` §ssgOptions — `includedRoutes` 追加 `'/404'`（产物 `dist/404.html`）
- 组件层新增调用：`useToolSeo()`（双工具页）、`useHead`（App.vue titleTemplate、home.vue）、`useSeoMeta`（not-found.vue）
- 含 SEO meta 的预渲染产物：`dist/json-formatter.html`、`dist/timestamp-converter.html`、`dist/404.html`（ARCH-03 验收对象）

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo/03-CONTEXT.md
@.planning/phases/03-seo/03-RESEARCH.md
@.planning/phases/03-seo/03-PATTERNS.md
@src/router/routes.ts
@src/App.vue
@src/pages/home.vue
@src/pages/not-found.vue
@vite.config.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tracer——SITE_URL 单源 + useToolSeo 标准集在 json-formatter 页端到端落盘实证（A4 fail-first）</name>
  <files>src/config/site.ts, src/composables/useToolSeo.ts, src/App.vue, src/tools/json-formatter/json-formatter.vue</files>
  <read_first>
    .planning/phases/03-seo/03-RESEARCH.md §Pattern 1（useSeoMeta 骨架）+ §Pitfall 5（titleTemplate 不影响 ogTitle）+ §Assumptions Log A4
    .planning/phases/03-seo/03-PATTERNS.md §src/config/site.ts / §src/composables/useToolSeo.ts / §App.vue 段（analog 代码摘录）
    src/router/routes.ts（meta.tool SEO 消费点，:19）
    src/tools/json-formatter/index.ts（name='JSON 格式化'、path='/json-formatter'、description 原文——verify 断言的期望值来源）
    src/App.vue（script setup 修改落点）
    src/tools/json-formatter/json-formatter.vue（script setup :78 起的 import 纪律）
    src/composables/useThemeMode.ts:18-22（唯一定义点注释纪律 analog）
  </read_first>
  <action>
    1. 创建 src/config/site.ts（per D-11/D-12）：导出常量 SITE_URL，值 'https://little-utils.edgeone.app'（占位值）。文件头 JSDoc 注释标明：D-12 四消费方单源（canonical/og:url/sitemap 全部 URL/robots.txt Sitemap 行统一 import 此处，禁止散落硬编码）；占位值待首次部署后按 D-11 修正。文件必须 Node 安全——纯常量、零 import、零浏览器 API（构建脚本与组件层共用）。注释纪律照 useThemeMode.ts THEME_STORAGE_KEY 的唯一定义点先例。
    2. 创建 src/composables/useToolSeo.ts（研究 §Pattern 1 骨架，per D-05/D-06/D-09/D-12）：具名导出 useToolSeo()；内部 useRoute() 取 route.meta.tool；meta.tool 为 undefined 时直接 return（守卫，防非工具路由误用）。注入 useSeoMeta 五键：title 取 tool.name、description 取 tool.description（D-09 复用 ToolMeta.description 零改写）、ogTitle 显式赋 tool.name（titleTemplate 不影响 ogTitle，per RESEARCH Pitfall 5）、ogDescription 赋 tool.description、ogUrl 赋 `${SITE_URL}${tool.path}`（SITE_URL import 自 '@/config/site'）；再 useHead 挂 link rel=canonical href=`${SITE_URL}${tool.path}`。文件头注释交代决策号（D-05/D-09/D-12）与「为什么不用 useHead 逐项手写」（og 三件套显式集归拢、Phase 5 新工具零成本接入）。
    3. src/App.vue script setup 追加 useHead（import { useHead } from '@unhead/vue'）：titleTemplate 函数 (t) => (t ? `${t} - little-utils` : 'little-utils')（D-06 半角连字符；t 为空回落站点名）。onBeforePageRender 暗色注入与 layout computed 零改动。
    4. src/tools/json-formatter/json-formatter.vue script setup 首行 import 区追加 useToolSeo import 与单句调用 useToolSeo()（模板与既有渲染纪律零改动）。
    5. 本任务是 RESEARCH A4 假设的 fail-first 实证：先跑一次 verify 确认当前产物无工具页 title（红），实现后转绿。若 og/canonical 未落盘（A4 为假），按 RESEARCH 预案改用 @unhead/vue/server 导出或 onPageRendered 钩子兜底——禁止改用 onBeforePageRender 正则手插 meta 标签（与水合冲突，RESEARCH Don't Hand-Roll）。
  </action>
  <verify>
    <automated>corepack pnpm build-only && test "$(grep -c '<title>JSON 格式化 - little-utils</title>' dist/json-formatter.html)" = "1" && test "$(grep -c 'name="description"' dist/json-formatter.html)" -ge 1 && test "$(grep -c 'rel="canonical"' dist/json-formatter.html)" = "1" && test "$(grep -c 'property="og:title"' dist/json-formatter.html)" = "1" && test "$(grep -c 'property="og:url"' dist/json-formatter.html)" = "1" && corepack pnpm type-check</automated>
    <fails_when>任一 test 退出非零：dist/json-formatter.html 中 title 计数 0（A4 落盘失败，仍只有 index 模板兜底 title）、description/canonical/og:title/og:url 任一计数 0（Pitfall 5 静默缺失）、或 type-check 报错。</fails_when>
  </verify>
  <acceptance_criteria>
    - dist/json-formatter.html 的 head 含 title='JSON 格式化 - little-utils'、非空 meta description（值 === 注册表 description 原文）、link rel=canonical（href 以 SITE_URL 开头）、meta property og:title/og:url 各恰好 1 处
    - src/config/site.ts 存在且仅导出 SITE_URL 纯常量；src/composables/useToolSeo.ts 对 meta.tool undefined 有 return 守卫
    - corepack pnpm type-check 通过
  </acceptance_criteria>
  <done>tracer 单路径端到端绿：注册表元数据落进 dist/json-formatter.html 静态 head，A4 假设由红转绿实证，四文件已提交。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展——timestamp-converter 接入 + 首页 meta（D-06 全角 title 保持 + D-10 description 起草）</name>
  <files>src/tools/timestamp-converter/timestamp-converter.vue, src/pages/home.vue</files>
  <read_first>
    .planning/phases/03-seo/03-PATTERNS.md §home.vue 追加 useHead 段
    .planning/phases/03-seo/03-RESEARCH.md §Code Examples（首页 meta 骨架）+ D-06/D-10 决策
    src/tools/timestamp-converter/index.ts（name='时间戳转换'——verify 期望值来源）
    src/tools/timestamp-converter/timestamp-converter.vue（script setup :184 起）
    src/pages/home.vue（script setup :43-52 落点）
    index.html:29（静态 title 现值，保留作无 JS 兜底）
  </read_first>
  <action>
    1. timestamp-converter.vue script setup 首行 import 区追加 useToolSeo import 与单句 useToolSeo() 调用（与 Task 1 json-formatter 完全同型；per D-05）。
    2. home.vue script setup 追加 useHead（import { useHead } from '@unhead/vue'）：title 设 'little-utils — 开发者 & 办公工具箱'（D-06 保留全角破折号，与 index.html:29 同值）；titleTemplate 设 null 以 discharge App 级模板——防止渲染成「…工具箱 - little-utils」双后缀；meta 数组追加一项 name=description 的 meta 对象。注释标 D-06/D-10 决策号。
    3. D-10 description 文案（Claude 起草，验收时人工确认）：「面向开发者与办公场景的免费在线工具箱，JSON 格式化、时间戳转换等常用工具即开即用，全部在浏览器本地完成，数据不上传、无需登录。」——含开发者/办公双线关键词与「数据不出浏览器」价值点。
    4. 若 unhead 3.x 对 titleTemplate: null 的 discharge 语义不生效（verify 的双后缀负断言会暴露），改用等价 discharge 手段（如 home 级 titleTemplate 函数原样返回 t），禁止在 App.vue titleTemplate 函数里加首页特判（归拢层污染）。
  </action>
  <verify>
    <automated>corepack pnpm build-only && test "$(grep -c '<title>little-utils — 开发者 &' dist/index.html)" = "1" && test "$(grep -c '办公工具箱 - little-utils' dist/index.html)" = "0" && test "$(grep -c 'name="description"' dist/index.html)" -ge 1 && test "$(grep -c '<title>时间戳转换 - little-utils</title>' dist/timestamp-converter.html)" = "1" && corepack pnpm type-check</automated>
    <fails_when>dist/index.html 的 title 计数 0（首页 title 丢失）、'办公工具箱 - little-utils' 计数非 0（titleTemplate discharge 失败产生双后缀）、description 计数 0（D-10 未落盘）、timestamp-converter.html title 计数 0（useToolSeo 未接入）、或 type-check 报错。</fails_when>
  </verify>
  <acceptance_criteria>
    - dist/index.html 含且仅含一处全角 title（'办公工具箱 - little-utils' 半角双后缀计数 = 0）与一处非空 meta description
    - dist/timestamp-converter.html 含 title='时间戳转换 - little-utils'
    - home.vue 源码含 D-10 文案字面量与 D-06/D-10 注释决策号
  </acceptance_criteria>
  <done>两个工具页与首页的预渲染 HTML 均携带标准 meta 集；首页全角 title 与 D-10 description 就位。</done>
</task>

<task type="auto">
  <name>Task 3: 404 预渲染 + robots noindex（D-08）——includedRoutes 追加 /404</name>
  <files>src/pages/not-found.vue, vite.config.ts</files>
  <read_first>
    .planning/phases/03-seo/03-RESEARCH.md §Pattern 4（404 预渲染与 EdgeOne 404 服务）+ §Pitfall 4
    .planning/phases/03-seo/03-PATTERNS.md §vite.config.ts 段（includedRoutes 现状 :23-25）
    src/pages/not-found.vue（当前纯模板、无 script setup 块）
    vite.config.ts（onBeforePageRender 暗色注入——勿动区）
  </read_first>
  <action>
    1. src/pages/not-found.vue 新增 script setup 块（结构参照 home.vue:43 的 script 起始形态）：import { useSeoMeta } from '@unhead/vue' 并调用 useSeoMeta({ robots: 'noindex, nofollow', title: '页面不存在' })。per D-08——预渲染 HTML 仍生成但爬虫不收录；不设 canonical 与 description（RESEARCH Open Question 2 裁定：noindex 页 canonical 无意义）。注释标 D-08。
    2. vite.config.ts ssgOptions.includedRoutes 返回值改为 [...paths.filter((p) => !p.includes(':')), '/404']（vue-router 将 '/404' 匹配到 catch-all → NotFound 组件 → 产物平铺写为 dist/404.html；per RESEARCH Pitfall 4）。onBeforePageRender 暗色注入逻辑一个字符都不动（Phase 2 D-05 基线，must_haves.prohibitions 第 3 条）。
    3. 注意 check-chunks 对 404.html 的 slug 推断（slug='404'，其中无 CM）无影响、零改动（RESEARCH Pitfall 4 末段已裁定）。
  </action>
  <verify>
    <automated>corepack pnpm build-only && test -f dist/404.html && test "$(grep -c 'noindex' dist/404.html)" -ge 1 && test "$(grep -c 'class="dark"' dist/index.html)" -ge 1 && corepack pnpm type-check</automated>
    <fails_when>dist/404.html 不存在（includedRoutes 追加未生效，Pitfall 4）、noindex 计数 0（useSeoMeta 未落盘）、class="dark" 计数 0（暗色预渲染基线被破坏）、或 type-check 报错。</fails_when>
  </verify>
  <acceptance_criteria>
    - dist/404.html 存在且 head 含 noindex, nofollow 的 meta robots 与「页面不存在」title
    - vite.config.ts includedRoutes 含 '/404' 追加项；onBeforePageRender 与 Phase 2 版本逐字一致
    - Phase 2 proven 命令 grep -c 'class="dark"' dist/index.html ≥ 1 保持
  </acceptance_criteria>
  <done>dist/404.html 预渲染存在且 noindex；EdgeOne 侧以 404 状态码服务该文件的机制由平台约定提供（部署后 plan 03 curl 抽验）。</done>
</task>

</tasks>

<flagged_assumptions>
<!-- Spec-less probe fallback：ARCH-03 在 edge 探针中 unclassified/unresolved，以下为显式挂旗假设（非静默丢弃） -->
- **A4（ARCH-03）**：useSeoMeta 在 vite-ssg 渲染进程（@unhead/vue client 子路径 createHead）能落盘 meta 到静态 HTML——官方文档支持但该组合未端到端实证；Task 1 为 fail-first 实证任务，失败时按预案切换 /server 导出或 onPageRendered 钩子。
- **SC-1 口径**：本计划交付的是本地构建产物实证；「curl 公网 URL」验收在 plan 03（部署后）执行。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 注册表 → meta 注入 | ToolMeta.name/description（代码内数据，经 code review）内插进 HTML head；无用户运行时输入 |
| 构建脚本 → vite.config | SITE_URL/路径常量内插进产物 HTML 与 sitemap（plan 02）；本计划仅涉及 meta 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | useToolSeo meta 注入 / home·not-found useHead | medium | mitigate | meta 字段为 unhead 白名单键（title/description/og*/canonical/robots），值全部来自代码自有注册表或计划内字面量，无用户输入内插；unhead 负责属性转义（框架层）；plan 02 check-dist 值一致性断言兜底 |
| T-03-03 | Tampering | 第三方资源注入（script/link/img 外链） | high | mitigate | 本阶段新增代码零外链（系统字体栈 D-16、零新增 npm 依赖）；plan 02 check-dist c) 零外链断言进门禁链持续防线 |
| T-03-SC | Tampering | npm 包引入 | high | mitigate | 本计划零新增 npm 包；执行期如需引入任何包必须先过 blocking-human 供应链闸门（T-02-SC 纪律），Package Legitimacy Audit 表为空表 |
| T-03-05 | DoS/robustness | vite-ssg includedRoutes 对路由清单的解析 | low | accept | 路由清单由 assertRegistryInvariants 校验的注册表派生，'/404' 为字面量追加，解析失败会在构建期立即报错（fail-fast），无静默劣化路径 |
</threat_model>

<verification>
- corepack pnpm build-only && 对 dist/json-formatter.html / dist/timestamp-converter.html / dist/index.html / dist/404.html 的 meta 断言全部通过（各任务 verify 命令）
- corepack pnpm vitest run（存量 172 测试零回归）&& corepack pnpm type-check && corepack pnpm lint
- grep -c 'class="dark"' dist/index.html ≥ 1（Phase 2 基线未破坏）
</verification>

<success_criteria>
- 两个工具页预渲染 HTML 各含标准五键 meta 集 + canonical，title 格式「工具名 - little-utils」
- 首页全角 title 无双后缀 + D-10 description；dist/404.html 存在且 noindex
- SITE_URL 单源成立：src/pages、src/tools、src/composables 源码 grep 'edgeone.app' 仅命中 src/config/site.ts（若含注释提及则零字面量赋值散落）
- ARCH-03 的 meta 注入部分（组件层）完成；sitemap/robots 与断言门禁在 plan 02
</success_criteria>

<output>
Create `.planning/phases/03-seo/03-01-SUMMARY.md` when done
</output>
