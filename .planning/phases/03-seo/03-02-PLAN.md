---
phase: 03-seo
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/seo/index.ts
  - src/seo/index.test.ts
  - vite.config.ts
  - scripts/check-dist.mjs
  - package.json
autonomous: true
requirements: [ARCH-03, ARCH-05]
estimate:
  tokens: 75000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - dist/sitemap.xml 与 dist/robots.txt 由构建自动生成，URL 全部从 SITE_URL 单源派生，路由清单 jiti 实载注册表（SC-2 本地实证，D-03/D-12）
    - sitemap 路由集合与注册表双向一致（防漏收录 + 防多收录），/404 不在 sitemap 中（D-08 特例）
    - check:dist 四类断言（meta 非空 / sitemap↔注册表一致 / 零第三方外链 / 首包 gzip ≤ 200KB）任一失败 exit 1，并挂进聚合门禁链 check:all（SC-4，D-17/D-18）
    - Phase 5 新工具「加目录 + 注册表一行」即自动获得 meta 与 sitemap 收录——保障靠断言进门禁而非人工记忆（SC-5 前置，D-18）
  artifacts:
    - src/seo/index.ts — buildSitemapXml / buildRobotsTxt 纯函数（Node 安全，vitest 可测）
    - src/seo/index.test.ts — 生成器单测（TDD RED→GREEN）
    - scripts/check-dist.mjs — 四类产物断言脚本（violations 数组 + exit 1 模式）
    - package.json scripts — check:dist 与 check:all（EdgeOne 构建命令链填 check:all 单条）
    - vite.config.ts onFinished — sitemap/robots 构建期生成
  key_links:
    - src/config/site.ts SITE_URL → onFinished（sitemap/robots 全部 URL）+ check-dist（外链白名单前缀 + robots Sitemap 行断言）——D-12 消费方 2/3/4 落地
    - jiti 载注册表 → onFinished 路由清单 + check-dist 一致性断言（双源消除，check-chunks.mjs:30-37 先例）
    - check:dist 必须殿后于 build-only（onFinished 产物依赖）——RESEARCH Pitfall 7 线性编排
  prohibitions:
    - statement: 禁止把 robots.txt 放 public/ 静态文件或在任何脚本散落硬编码域名——四消费方必须 import SITE_URL（D-12）
      status: active
      verification: grep scripts/ public/ 无 edgeone.app 字面量（src/config/site.ts 唯一）；check-dist b) 断言 Sitemap 行 === SITE_URL 派生值
    - statement: 禁止在 sitemapOptions/onFinished/check-dist 手写工具路由清单——必须 jiti 实载注册表（双源消除先例）
      status: active
      verification: grep scripts/check-dist.mjs vite.config.ts 无 '/json-formatter' 或 '/timestamp-converter' 字面路由；check-dist b) 集合相等断言
    - statement: 禁止用全文 https?:// 正则做外链检测——只扫资源加载向量并显式排除 xmlns*/data:/#（RESEARCH Pitfall 3 实测误报源）
      status: active
      verification: corepack pnpm check:dist 在当前 dist 上输出 OK（零 w3.org 误报）
---

# Plan 02: 构建期生成 + 产物断言门禁——sitemap/robots 与 check-dist

<objective>
在 plan 01 的 meta 落盘切片之上构建构建期层：vite-ssg onFinished 从注册表 + SITE_URL 单源生成 sitemap.xml 与 robots.txt（D-03/D-12）；新增 scripts/check-dist.mjs 承担 meta 非空、sitemap↔注册表一致、零第三方外链、200KB gzip 预算四类断言（D-17/D-18），并以 check:all 线性聚合链作为 plan 03 EdgeOne 构建命令的唯一入口（D-15/Pitfall 7）。

Purpose: 把「每页 meta 齐全、收录路由正确、零外链、预算不漂移」从人工记忆变成机器门禁——Phase 5 七个新工具零成本自动覆盖的关键机制。
Output: src/seo/（纯函数 + 单测）、onFinished 接线、check-dist.mjs、package.json 门禁链。
</objective>

## Artifacts this phase produces

- `src/seo/index.ts` — 导出 `buildSitemapXml(paths: string[], siteUrl: string): string` 与 `buildRobotsTxt(siteUrl: string): string`
- `src/seo/index.test.ts` — 生成器单测（含 XML 实体转义用例）
- `vite.config.ts` §ssgOptions — `onFinished()`：jiti 载注册表 → 写 `dist/sitemap.xml`（/ + 工具路由，不含 /404）与 `dist/robots.txt`
- `scripts/check-dist.mjs` — 四类断言：a) 每页 meta 非空（工具页 title 含「 - little-utils」后缀、description === 注册表 ToolMeta.description、404 noindex 特例）b) sitemap↔注册表集合双向相等 + robots Sitemap 行 === SITE_URL 派生 c) 零第三方外链（资源加载向量扫描，排除 xmlns*）d) 首包 gzip ≤ 200*1024 字节
- `package.json` — `check:dist`、`check:all`（run-s test:unit type-check lint check:chunks build-only check:dist）

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo/03-CONTEXT.md
@.planning/phases/03-seo/03-RESEARCH.md
@.planning/phases/03-seo/03-PATTERNS.md
@.planning/phases/03-seo/03-01-SUMMARY.md
@scripts/check-chunks.mjs
@vite.config.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEO 纯函数生成器 buildSitemapXml / buildRobotsTxt（TDD）</name>
  <files>src/seo/index.ts, src/seo/index.test.ts</files>
  <read_first>
    .planning/phases/03-seo/03-RESEARCH.md §Pattern 2（生成器骨架 + sitemaps.org 协议要点：必选 urlset/url/loc、UTF-8、实体转义、可选字段忽略）
    .planning/phases/03-seo/03-PATTERNS.md §src/seo/index.ts / §src/seo/index.test.ts 段（analog：tool.ts 契约注释风格 + registry.test.ts 结构）
    src/tools/tool.ts（JSDoc 契约注释纪律 analog）
    src/tools/registry.test.ts（describe/it 中文「输入 → 期望」命名先例）
  </read_first>
  <behavior>
    - buildSitemapXml(['/', '/json-formatter'], 'https://x.test') 返回字符串含 xml 声明（version/encoding UTF-8）、urlset 且 xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"、两行 url/loc 分别为 https://x.test/ 与 https://x.test/json-formatter；不含 lastmod/changefreq/priority（Google 已声明忽略，最小集）
    - buildSitemapXml(['/a&b'], 'https://x.test') 的 loc 内 & 渲染为 &amp;（XML 实体转义）
    - buildRobotsTxt('https://x.test') 返回 'User-agent: *\nAllow: /\n\nSitemap: https://x.test/sitemap.xml\n'（D-03 全放开 + D-12 Sitemap 行派生）
    - 两函数为纯函数：不触碰 document/window/fetch（Node 安全，构建脚本可 import）
  </behavior>
  <action>
    1. RED：先写 src/seo/index.test.ts（analog registry.test.ts 结构，describe/it 中文命名），导入 ./index 的 buildSitemapXml/buildRobotsTxt——此文件创建时 src/seo/index.ts 尚不存在，vitest 运行必须先红（模块不存在或导出缺失）。
    2. GREEN：创建 src/seo/index.ts 实现两函数使其通过。风格照 tool.ts：文件头注释标明决策引用（D-03 允许全部抓取 / D-12 域名单源）与协议来源（sitemaps.org 最小集 urlset/url/loc + & → &amp; 实体转义）；具名导出、JSDoc 契约注释、零浏览器 API 零 import（或仅类型 import）。
    3. 提交节奏：RED 一笔（test(seo): add failing tests for sitemap/robots generators）、GREEN 一笔（feat(seo): implement sitemap/robots generators）。
    4. REFACTOR（仅在必要时）：清理重复拼接逻辑后重跑测试确认仍绿。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/seo/index.test.ts && corepack pnpm type-check</automated>
    <fails_when>vitest 汇总行 failed &gt; 0（实体转义或 robots 行格式不符）、或 vue-tsc 报错（导出签名与 behavior 不符）。</fails_when>
  </verify>
  <acceptance_criteria>
    - corepack pnpm vitest run src/seo/index.test.ts 全绿（≥4 个用例：基本形态/实体转义/robots 行/最小集负断言）
    - src/seo/index.ts 零浏览器 API、具名导出两函数
    - corepack pnpm type-check 通过
  </acceptance_criteria>
  <done>生成器纯函数 TDD 闭环（RED→GREEN 各一笔提交），单测覆盖协议最小集与实体转义。</done>
</task>

<task type="auto">
  <name>Task 2: vite.config.ts onFinished——sitemap.xml 与 robots.txt 构建期生成（D-03/D-12）</name>
  <files>vite.config.ts</files>
  <read_first>
    .planning/phases/03-seo/03-RESEARCH.md §Pattern 2（onFinished 官方示例模式）+ §Pitfall 4（/404 不进 sitemap）
    .planning/phases/03-seo/03-PATTERNS.md §vite.config.ts 段 + §Shared Patterns 双源消除段
    vite.config.ts（plan 01 后现状：includedRoutes 含 '/404'）
    src/tools/index.ts（tools[].path 派生源）
    scripts/check-chunks.mjs:30-37（jiti 载注册表先例）
  </read_first>
  <action>
    1. vite.config.ts imports 区追加：import { writeFile } from 'node:fs/promises'、import { createJiti } from 'jiti'、import { buildSitemapXml, buildRobotsTxt } from './src/seo'、import { SITE_URL } from './src/config/site'（vite config 由 esbuild 打包，可直接 import TS 纯函数模块；alias '@' 指向 ./src 亦可用，取其一保持一致）。
    2. ssgOptions 新增 async onFinished()：createJiti(import.meta.url) 后 jiti.import('./src/tools/index.ts') 取 tools[].path（双源消除——禁止手写任何工具路由字面量，per must_haves.prohibitions 第 2 条）；toolPaths 前置 '/' 组成 ['/', ...toolPaths]；writeFile('dist/sitemap.xml', buildSitemapXml(paths, SITE_URL), 'utf8') 与 writeFile('dist/robots.txt', buildRobotsTxt(SITE_URL), 'utf8')。sitemap 清单不含 '/404'（D-08 特例：noindex 页不收录）。
    3. 禁止把 robots.txt 放 public/（public/ 静态文件无法引用 SITE_URL，会硬编码域名违反 D-12）；不新增任何 npm 包（RESEARCH Alternatives：vite-ssg-sitemap 类插件违反 T-02-SC）。
    4. onBeforePageRender 与 includedRoutes 现状（plan 01 产物）零改动。
  </action>
  <verify>
    <automated>corepack pnpm build-only && test -f dist/sitemap.xml && test -f dist/robots.txt && test "$(grep -c '<loc>' dist/sitemap.xml)" = "3" && test "$(grep -c '404' dist/sitemap.xml)" = "0" && grep -q '^Sitemap: https://little-utils.edgeone.app/sitemap.xml$' dist/robots.txt && grep -q '^Allow: /$' dist/robots.txt</automated>
    <fails_when>dist/sitemap.xml 或 dist/robots.txt 不存在（onFinished 未执行）、&lt;loc&gt; 计数 ≠ 3（漏收录或错误多收录，期望 / + 两个工具路由）、sitemap 出现 404（D-08 特例被破坏）、robots Sitemap 行与 SITE_URL 派生值不符（D-12 单源被破坏）。</fails_when>
  </verify>
  <acceptance_criteria>
    - build-only 后 dist/sitemap.xml 恰含 3 个 loc：SITE_URL + '/'、SITE_URL + '/json-formatter'、SITE_URL + '/timestamp-converter'；无 404
    - dist/robots.txt 含 User-agent: * / Allow: / / Sitemap: ${SITE_URL}/sitemap.xml 三行语义
    - vite.config.ts 无任何工具路由字面量（jiti 实载注册表）
  </acceptance_criteria>
  <done>sitemap/robots 成为构建产物（非静态文件），域名与路由清单全部单源派生；SITE_URL 修正后重跑构建即全量更新（D-01 换域名路径就绪）。</done>
</task>

<task type="auto">
  <name>Task 3: scripts/check-dist.mjs 四类断言 + package.json check:dist / check:all 门禁链（D-17/D-18）</name>
  <files>scripts/check-dist.mjs, package.json</files>
  <read_first>
    .planning/phases/03-seo/03-RESEARCH.md §Pattern 3（断言清单与实现要点，研究实测校准）+ §Pitfall 3（xmlns 误报）/ §Pitfall 7（线性编排）
    .planning/phases/03-seo/03-PATTERNS.md §scripts/check-dist.mjs 段（check-chunks 四模式摘录：imports 路径解析 / jiti 载注册表 / reachableChunks BFS / violations+exit 1）
    scripts/check-chunks.mjs（exact analog——四模式照抄落点）
    package.json scripts 区（check:chunks 行 = 新行先例；run-s/run-p 由 npm-run-all2 提供）
  </read_first>
  <action>
    1. 创建 scripts/check-dist.mjs（analog check-chunks.mjs 的 imports/jiti/violations 模式）。入口防御：dist 不存在时 push violation 提示「请先运行构建」并 exit 1（fail-fast，照 check-chunks.mjs:113 先例）。
    2. 断言 a) 每页 meta 非空：遍历 dist/*.html——每个文件 &lt;title&gt; 非空；工具页（按注册表 slug 对应的平铺 html）title 必须以「 - little-utils」结尾且含注册表 tool.name；meta description content 非空且 === 注册表 ToolMeta.description 原文（D-09 单源断言，jiti 实载注册表比对）；dist/404.html 特例：含 noindex，且 404 路径不在 sitemap loc 集合。
    3. 断言 b) sitemap↔注册表一致：jiti 载注册表得 paths + '/'；正则解析 dist/sitemap.xml 的 &lt;loc&gt; 集合；双向断言集合相等（防漏收录 + 防多收录）；robots.txt 的 Sitemap 行 === `${SITE_URL}/sitemap.xml`（SITE_URL 经 jiti 从 src/config/site.ts 载入——禁止在本脚本硬编码域名，per prohibitions 第 1 条）。
    4. 断言 c) 零第三方外链：只提取资源加载向量——script src / link href / img src / iframe src / source src 与 srcset / video·audio src / use href 与 xlink:href / CSS 文件内 url()；提取值若为 http(s) 绝对 URL 必须以 SITE_URL 开头，相对路径与 data:/# 锚点放行；显式排除 xmlns 与 xmlns:xlink 属性值（RESEARCH Pitfall 3：实测每页含 http://www.w3.org/2000/svg，非网络请求）。禁止全文 https?:// 正则。
    5. 断言 d) gzip 预算：首包 = gzipSync(dist/index.html) + 入口页 HTML 直接引用资源的 chunk 可达闭包（BFS 逻辑照 check-chunks.mjs reachableChunks:96-111 复制，研究裁定不抽 scripts/lib——两脚本职责独立、失败信息不混流）；逐文件 node:zlib gzipSync 求和；总和 &gt; 200 * 1024 字节 → VIOLATION（D-17 硬卡）。研究实测当前基线约 60.6KB，余量 3.3 倍。
    6. 统一出口照 check-chunks.mjs:192-198：violations 数组逐条 console.error + FAIL 计数 + process.exit(1)；全过时输出 'OK: dist checks pass (meta/sitemap/ext-link/gzip)'。
    7. package.json scripts 新增两行："check:dist": "node scripts/check-dist.mjs"（紧邻 check:chunks）与 "check:all": "run-s test:unit type-check lint check:chunks build-only check:dist"（D-15 EdgeOne 构建命令填 check:all 单条；check:dist 必须殿后于 build-only——onFinished 的 sitemap/robots 依赖产物，RESEARCH Pitfall 7）。零新增依赖。
  </action>
  <verify>
    <automated>corepack pnpm build-only && corepack pnpm check:dist</automated>
    <fails_when>输出含 VIOLATION 行或 'FAIL:' 或退出码 1——典型信号：外链断言误报 w3.org（Pitfall 3 扫描器写错）、sitemap 集合不等（Task 2 产物或注册表漂移）、gzip 总和 &gt; 2097152 字节（预算超限）、404.html 缺 noindex。</fails_when>
  </verify>
  <acceptance_criteria>
    - corepack pnpm check:dist 在刚构建的 dist 上输出 OK 行且 exit 0
    - scripts/check-dist.mjs 含四类断言各自独立的 violation 消息（失败可定位到 a/b/c/d 类别）
    - package.json 存在 check:dist 与 check:all，check:all 中 check:dist 位于 build-only 之后
    - 零新增 npm 依赖（package.json dependencies/devDependencies 无变化）
  </acceptance_criteria>
  <done>四类产物断言成为可执行门禁：check:all 单链覆盖 test→type-check→lint→check:chunks→build-only→check:dist，plan 03 直接填入 EdgeOne 构建命令。</done>
</task>

</tasks>

<flagged_assumptions>
<!-- Spec-less probe fallback：ARCH-03/ARCH-05 在 edge 探针中均 unresolved，以下显式挂旗 -->
- **A1（ARCH-05）**：EdgeOne 默认域名后缀假设为 *.edgeone.app（官方文档只说「平台子域名」）——本计划所有断言用 src/config/site.ts 的 SITE_URL 派生值而非平台假设，域名修正为 plan 03 的显式任务。
- **SC-2 口径**：本计划交付 sitemap/robots 的本地产物与断言；「公网可达」curl 验收在 plan 03（部署后）执行。
- **RESEARCH 测试图偏差声明**：原「组件单测（useToolSeo/home meta）」落点调整为产物层断言（check-dist a) 值一致性 + plan 01 产物 grep）——mock @unhead/vue 的组件单测无增量防护价值，产物断言更接近 SC-1 真实故障模式（planner 落点划分 discretion）。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 注册表/常量 → XML 生成器 | tool.path 与 SITE_URL 内插进 sitemap XML——特殊字符是注入面 |
| dist 产物 → check-dist 解析 | 构建产物形态异常（缺失/畸形 HTML）是断言脚本的输入面 |
| EdgeOne 构建环境 → 部署产物 | 构建命令链是「带病产物不上线」的唯一闸门 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | buildSitemapXml / onFinished 的域名与路径内插 | medium | mitigate | XML 实体转义（& → &amp;，Task 1 单测锁定）；tool.path 形态由 assertRegistryInvariants 约束（/ 开头、无 //）；SITE_URL 为代码内常量非用户输入 |
| T-03-03 | Tampering/Elevation | 第三方资源投毒（外链进产物） | high | mitigate | check-dist c) 只认 SITE_URL 前缀的绝对资源 URL，零外链断言进 check:all 门禁链，EdgeOne 构建失败即不部署（D-15/D-18） |
| T-03-04 | Elevation | 构建命令链被绕过（手动上传产物/链不全） | medium | mitigate | plan 03 将 check:all 全链填入 EdgeOne 控制台为唯一部署路径；仓库不提交 dist（check-dist 只在构建后运行）；prohibitions 声明禁止绕过 |
| T-03-05 | DoS/robustness | check-dist 对 dist 的解析健壮性 | low | accept | dist 缺失 fail-fast exit 1；HTML 解析用正则提取（与 check-chunks 同级健壮性），畸形产物在构建期即暴露，静默漏报风险由四类断言交叉覆盖 |
| T-03-SC | Tampering | npm 包引入 | high | mitigate | 本计划零新增 npm 包（run-s/jiti/zlib 均既有）；执行期引入任何新包必须先过 blocking-human 供应链闸门 |
</threat_model>

<verification>
- corepack pnpm vitest run src/seo/index.test.ts 全绿；corepack pnpm vitest run（存量 172 测试零回归）
- corepack pnpm build-only → dist/sitemap.xml（3 loc，无 404）+ dist/robots.txt（Sitemap 行 = SITE_URL 派生）
- corepack pnpm check:dist 输出 OK；corepack pnpm check:all 全链绿（~120s，Nyquist 反馈延迟达标）
- grep -c 'class="dark"' dist/index.html ≥ 1（Phase 2 基线持续保持）
</verification>

<success_criteria>
- SC-2 本地实证：构建自动产出 sitemap.xml（/ + 全部工具路由）与 robots.txt（允许全部 + Sitemap 行），零硬编码域名
- SC-4 机器化：meta 非空 / sitemap 一致 / 零外链 / 200KB 预算四断言进门禁链，超限 exit 1
- check:all 就绪作为 plan 03 EdgeOne 构建命令（D-15）；Phase 5 新工具经「注册表一行」自动获得 meta 与 sitemap 断言覆盖
- ARCH-03 构建期部分与 ARCH-05 断言部分完成；部署与公网验收在 plan 03
</success_criteria>

<output>
Create `.planning/phases/03-seo/03-02-SUMMARY.md` when done
</output>
