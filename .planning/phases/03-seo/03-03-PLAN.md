---
phase: 03-seo
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/config/site.ts
autonomous: false
requirements: [ARCH-05]
user_setup:
  - service: GitHub
    why: D-13 新建公开仓（本地环境无 gh CLI，建仓为浏览器人工步骤）
    dashboard_config:
      - task: "新建公开仓库 little-utils（不要初始化 README/.gitignore，避免与本地历史冲突）"
        location: "github.com → New repository → Public"
  - service: EdgeOne Pages（EdgeOne Makers）
    why: D-14/D-15 Git 集成部署绑定与构建配置；默认域名分配是 SITE_URL 修正依据（D-11）
    dashboard_config:
      - task: "创建项目并授权绑定 GitHub 仓库；构建命令填 corepack pnpm check:all；输出目录 dist；Node 版本显式选 24.18.0（Pitfall 6：.nvmrc 不切换包管理器，本仓不放 .nvmrc）"
        location: "EdgeOne Pages 控制台 → 项目设置"
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - 推 main 触发 EdgeOne 自动构建部署，构建命令链（check:all 全门禁）任一失败不部署（SC-5，D-15）
    - curl 公网工具页返回含该工具 title 与 description 的真实 HTML（SC-1，D-20 全球可达口径）
    - sitemap.xml 与 robots.txt 公网可达且含全部工具路由（SC-2）
    - 线上 canonical/og:url/sitemap 域名 = 实际分配域名；D-04 搜索引擎提交排在其后（D-11 两段式闭环，Pitfall 2）
    - 大陆无代理实测 = 挂起项（D-20 拆分口径）：登记于 human-check，待自定义域名绑定后执行，不阻塞本阶段完成
  artifacts:
    - GitHub 公开仓（D-13）+ EdgeOne Pages 项目（控制台配置：Node 24.18.0 / check:all / dist）
    - 修正后的 src/config/site.ts（SITE_URL = 实际分配域名）
    - 部署验收记录（plan SUMMARY human-check 清单）
  key_links:
    - git push main → EdgeOne 构建环境（check:all 全链）→ dist 部署边缘——SC-5 自动交付链
    - EdgeOne 分配域名 → src/config/site.ts SITE_URL → 重跑构建 → 线上 canonical/og:url/sitemap/robots 四消费方同步更新（D-12 换源机制首次真实验证）
  prohibitions:
    - statement: 禁止在 SITE_URL 为占位值（*.edgeone.app 占位符）或未修正状态下向 Google Search Console / 百度站长平台提交 sitemap——收录占位域名后换真域名收录作废（RESEARCH Pitfall 2）
      status: active
      verification: D-04 human-check 步骤顺序位于 SITE_URL 修正重部署 + curl 确认之后（本计划 Task 3 排序硬约束）
    - statement: 禁止绕过 EdgeOne 构建命令链部署（控制台手动上传产物、或构建命令未跑 check:all 全链）——线上产物必须过门（D-15）
      status: active
      verification: EdgeOne 控制台构建命令 = corepack pnpm check:all；git ls-files dist 输出为空（仓库不存产物）
    - statement: 仓库根禁止提交 .nvmrc（Pitfall 6：.nvmrc 切 Node 不含包管理器，Node 版本由控制台显式选 24.18.0）
      status: active
      verification: test ! -f .nvmrc
---

# Plan 03: 部署管线与公网验收——GitHub/EdgeOne 上线 + SITE_URL 闭环（D-13/D-14/D-15/D-11/D-04/D-20）

<objective>
把 plan 02 就绪的 check:all 门禁链接上部署平台：GitHub 公开仓 + EdgeOne Pages Git 集成（推 main 自动构建部署，SC-5），SITE_URL 从占位值修正为实际分配域名并重部署验证四消费方单源换源机制（D-11/D-12），最后完成 SC-1/SC-2 的公网 curl 验收与 D-04 sitemap 提交。大陆无代理实测按 D-20 拆分口径登记为挂起项（不阻塞）。

Purpose: 本阶段终点「站点公网可用且可持续自动交付」；代码侧全部就绪后，剩余工作只有平台控制台配置与作者账号操作。
Output: 公网可达的站点（全球不含大陆可用区）、真实域名四消费方闭环、搜索引擎提交完成、大陆实测挂起项登记。
</objective>

## Artifacts this phase produces

- GitHub 公开仓 `little-utils`（本地 remote origin 首次配置，D-13）
- EdgeOne Pages 项目（控制台：仓库绑定、构建命令 `corepack pnpm check:all`、输出目录 dist、Node 24.18.0、默认域名，D-14/Pitfall 6）
- `src/config/site.ts` SITE_URL 修正为实际分配域名（D-11；如控制台在首次构建前已显示域名，则修正先于首次推送执行，消除 Pitfall 2 的占位值部署）
- 公网 curl 验收记录（SC-1/SC-2/SC-3a）与 D-04 sitemap 提交记录、D-20 大陆实测挂起登记

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo/03-CONTEXT.md
@.planning/phases/03-seo/03-RESEARCH.md
@.planning/phases/03-seo/03-02-SUMMARY.md
@src/config/site.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 人工步骤——GitHub 建仓 + EdgeOne Pages 控制台配置（D-13/D-14/D-15）</name>
  <files>（无代码文件；产出为平台配置与两项回填信息）</files>
  <read_first>
    .planning/phases/03-seo/03-RESEARCH.md §Pitfall 6（版本错配防线）+ §Runtime State Inventory（平台侧人工步骤清单）+ §Environment Availability（gh CLI 缺失 → 浏览器建仓）
    .planning/phases/03-seo/03-CONTEXT.md D-13/D-14/D-15 决策
    package.json scripts.check:all（EdgeOne 构建命令取值来源，plan 02 产物）
  </read_first>
  <what-built>部署平台的代码外人工配置（D-13/D-14/D-15）：GitHub 新建公开仓 little-utils（本地无 gh CLI，浏览器人工建仓）；EdgeOne Pages 控制台创建项目并绑定该仓库，构建命令填 corepack pnpm check:all（全门禁链：test → type-check → lint → check:chunks → build-only → check:dist，任一失败不部署）、输出目录 dist、Node 版本显式选 24.18.0（Pitfall 6 版本错配防线）。这些配置只存在于平台控制台，git 无任何记录，是 Task 2 推送部署的前置条件。</what-built>
  <how-to-verify>
    1. github.com → New repository → Public，名 little-utils，不勾选 README/.gitignore/license 初始化（本地仓已有提交历史，避免冲突）。
    2. EdgeOne Pages（EdgeOne Makers）控制台 → 创建项目 → 授权绑定上述仓库 → 构建配置逐项核对：构建命令 = corepack pnpm check:all、输出目录 = dist、Node.js 版本 = 24.18.0。
    3. 记录控制台显示的项目默认域名（形如 https://&lt;xxx&gt;.edgeone.app，实际后缀以控制台为准——A1 假设的证实动作）。
    4. 向执行者回填两项信息：A = GitHub 仓库 URL（https://github.com/&lt;owner&gt;/little-utils.git）；B = 实际分配的默认域名。
  </how-to-verify>
  <acceptance_criteria>
    - GitHub 公开仓存在且与本地 remote 可关联（Task 2 用回填 URL 配置 origin）
    - EdgeOne 控制台构建命令逐字为 corepack pnpm check:all（全门禁链，D-15）
    - 默认域名已回填（SITE_URL 修正依据，D-11）
  </acceptance_criteria>
  <resume-signal>输入 "done" + 仓库 URL 与默认域名两项回填值确认完成；或 "STOP" + 原因中止部署路径。</resume-signal>
  <done>建仓与控制台配置完成，执行者拿到仓库 URL 与实际分配域名。</done>
</task>

<task type="auto">
  <name>Task 2: SITE_URL 修正 + 首推 main + 公网 curl 验收（D-11/D-14 + SC-1/SC-2/SC-3a）</name>
  <files>src/config/site.ts</files>
  <read_first>
    .planning/phases/03-seo/03-CONTEXT.md D-11/D-12/D-20 决策 + D-01（默认域名先上线）
    .planning/phases/03-seo/03-RESEARCH.md §Pitfall 2（占位值固化顺序约束）+ §Pitfall 1（大陆不可直连——401 ≠ 部署失败）
    src/config/site.ts（plan 01 占位值现状）
  </read_first>
  <action>
    以下 &lt;REPO_URL&gt; 与 &lt;DEPLOY_DOMAIN&gt; 用 Task 1 回填值替换（本任务所有命令中的域名一律用实际回填值，禁止保留占位符执行）。

    1. SITE_URL 修正（D-11）：若 Task 1 回填域名 ≠ src/config/site.ts 现值，Edit SITE_URL 为 'https://&lt;DEPLOY_DOMAIN 去除协议后的 host&gt;' 形式的实际域名常量（保序幕 'https://'）。控制台在首次构建前已分配域名时先修正再推——消除 Pitfall 2 的占位值部署；若域名仅在首次部署后才可见，则按两段式：先推占位值部署 → 读实际域名 → 二次修正 SITE_URL → 再推重部署（D-04 提交始终排在终态域名确认之后）。
    2. 本地预检（推前确认产物以真实域名生成）：corepack pnpm build-only && corepack pnpm check:dist（若已修正 SITE_URL）。
    3. 首次配置 remote 并推送（D-13/D-14）：git remote add origin &lt;REPO_URL&gt;（已存在则 set-url）→ git push -u origin main。推送触发 EdgeOne 自动构建（SC-5）；在控制台等待构建与部署完成（check:all 全链约 1–2 分钟）。
    4. 公网 curl 验收（全球可达口径，D-20）——见 verify 三连：工具页 meta 可见（SC-1）、sitemap 含全部工具路由（SC-2）、robots Sitemap 行为真实域名。若 curl 得到 401/超时，先核对是否在大陆网络误测（RESEARCH Pitfall 1：默认域名大陆不可直连，3h 预览链接 401 属期望行为），再排查部署状态。
    5. 若二次修正了 SITE_URL：确认重部署后线上 sitemap/canonical 均为真实域名后才进入 Task 3。
  </action>
  <verify>
    <automated>curl -fsS https://&lt;DEPLOY_DOMAIN&gt;/json-formatter | grep -c '<title>JSON 格式化 - little-utils</title>' 与 curl -fsS https://&lt;DEPLOY_DOMAIN&gt;/sitemap.xml | grep -c '<loc>'（期望 3）与 curl -fsS https://&lt;DEPLOY_DOMAIN&gt;/robots.txt | grep -c '^Sitemap: https://&lt;DEPLOY_DOMAIN&gt;/sitemap.xml$'（期望 1）；执行者将 &lt;DEPLOY_DOMAIN&gt; 替换为实际域名后三条命令须全部通过</automated>
    <fails_when>任一 curl 退出非零（DNS/404/401——401 需先排除大陆网络误测）、工具页 title 计数 0（线上产物非 meta 版本或部署了旧构建）、&lt;loc&gt; 计数 ≠ 3（sitemap 与注册表漂移）、Sitemap 行仍为占位域名（SITE_URL 修正未重部署）。</fails_when>
  </verify>
  <acceptance_criteria>
    - src/config/site.ts 的 SITE_URL = 实际分配域名（非占位符）
    - git remote origin = Task 1 回填仓库 URL；main 已推送并触发 EdgeOne 部署成功
    - 三条公网 curl 断言（SC-1 工具页 title、SC-2 sitemap 3 loc、robots Sitemap 行真实域名）全部通过
    - test ! -f .nvmrc（prohibitions 第 3 条）
  </acceptance_criteria>
  <done>站点在 EdgeOne 默认域名上公网可达（全球不含大陆），SC-1/SC-2/SC-3a curl 验收绿，四消费方域名单源闭环。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 终局人工验收——sitemap 提交（D-04）+ 大陆实测挂起登记（D-19/D-20）+ SC-5 确认</name>
  <files>（无代码文件；产出为验收记录与挂起项登记）</files>
  <read_first>
    .planning/phases/03-seo/03-CONTEXT.md D-04/D-19/D-20 决策 + Deferred Ideas（自定义域名绑定）
    .planning/phases/03-seo/03-RESEARCH.md §Pitfall 2（提交顺序硬约束）
    .planning/STATE.md §Blockers（ICP 备案——D-02 已确认全球可用区路径，state 更新提示）
  </read_first>
  <what-built>Phase 3 终局人工验收（全部为作者账号/环境专属操作，D-04/D-19/D-20）：向 Google Search Console 与百度站长平台提交真实域名的 sitemap（SC-2 收录动作）；把「无代理大陆网络实测可打开」按 D-20 拆分口径登记为显式挂起项（执行条件 = 自定义域名绑定后，不阻塞本阶段完成）；确认 SC-5「推 main 即部署」已由 Task 2 实证。另提示 orchestrator 清账 STATE.md 的 ICP 备案 blocker（D-02 已确认）。</what-built>
  <how-to-verify>
    1. 先做提交前抽查（硬顺序约束，Pitfall 2）：curl -fsS https://&lt;实际域名&gt;/sitemap.xml | grep -c 'edgeone.app 占位域名'，计数必须 = 0——确认 SITE_URL 终态域名已上线后才可提交。
    2. Google Search Console → 提交 https://&lt;实际域名&gt;/sitemap.xml；百度站长平台 → 同 URL 提交（D-04）。
    3. 大陆实测挂起登记：在 plan SUMMARY 挂起清单成文——执行条件（自定义域名绑定后，Deferred Ideas）、执行方式（大陆网络无代理打开首页与任一工具页，D-19）、不阻塞声明（D-20）。
    4. SC-5 确认：核对 Task 2 的 push → EdgeOne 自动构建部署日志；可选二次抽验（空改 README 推送观察自动构建）。
    5. 提示 orchestrator：STATE.md Blockers「ICP 备案与否需作者显式确认」由 D-02 关闭，可清账。
  </how-to-verify>
  <acceptance_criteria>
    - D-04 完成且顺序合规（提交的 sitemap URL 为 SITE_URL 终态域名，占位域名计数 = 0）
    - D-20 挂起项已显式登记（非静默丢弃）：执行条件与执行方式成文
    - SC-5 有实证记录（push → 部署日志）
  </acceptance_criteria>
  <resume-signal>输入 "done" 确认三项验收完成（含挂起项登记位置）；或 "STOP" + 原因。</resume-signal>
  <done>Phase 3 五条成功标准按 D-20 拆分口径全部闭环（SC-3 拆分项挂起登记）；部署管线交付完成。</done>
</task>

</tasks>

<flagged_assumptions>
<!-- Spec-less probe fallback：ARCH-05 在 edge 探针中 unresolved，以下显式挂旗（非静默丢弃） -->
- **A1（ARCH-05）**：默认域名后缀 *.edgeone.app 为假设——实际值以 EdgeOne 控制台分配为准（Task 1 回填信息 B 即为证实动作），SITE_URL 单源使修正成本仅一处常量 + 重部署。
- **A5（ARCH-05）**：EdgeOne 免费层「全球可用区（不含大陆）」下自定义域名绑定免备案（官方文档支持）——影响的是 D-20 挂起项的执行条件成立与否，绑定动作本身在 Deferred Ideas（默认域名上线后按需进行）。
- **SC-3 拆分口径（D-20）**：本阶段验收 = 部署管线 + 全球域名 curl 可达；「大陆无代理实测」延后至自定义域名绑定后执行，为显式挂起的 human-check，不阻塞本阶段完成。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地 git → EdgeOne 构建环境 | push 触发平台构建；构建命令链是带病产物上线的唯一闸门 |
| EdgeOne 控制台配置 → 线上行为 | 构建命令/Node 版本/产物目录配置错误直接决定线上正确性（git 无记录，只能人工核对） |
| 线上 URL → 搜索引擎 | sitemap 提交把域名固化进索引——占位域名提交即污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Elevation | 构建命令链被绕过（手动上传产物 / 链不全） | high | mitigate | 控制台构建命令逐字 corepack pnpm check:all（Task 1 核对项）；仓库不提交 dist（git ls-files dist 为空）；check:dist 殿后确保上线产物 = 门禁产物 |
| T-03-06 | Tampering | 占位域名被提交进搜索引擎（Pitfall 2） | high | mitigate | Task 3 顺序硬约束：D-04 提交排在 SITE_URL 修正重部署 + curl 确认之后；prohibitions 第 1 条声明；提交前 grep 占位域名计数 = 0 |
| T-03-07 | Tampering | EdgeOne 构建环境版本错配（engines 不满足的 Node） | medium | mitigate | 控制台显式选 Node 24.18.0（Task 1）；仓库不放 .nvmrc（prohibitions 第 3 条）；pnpm 由 lock 文件自动识别（11.25.0 在官方支持 6–11 列表） |
| T-03-08 | Information Disclosure | 部署日志/控制台截图含敏感信息 | low | accept | 仓库为公开代码仓、无密钥体系（纯前端无服务端）；控制台访问凭据仅在作者账号侧，不进仓库 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | 全阶段零新增包（RESEARCH Package Legitimacy Audit 空表）；执行期引入任何包必须 blocking-human 闸门人工批准，不可自动放行 |
</threat_model>

<verification>
- git push origin main → EdgeOne 控制台部署成功日志（check:all 全链绿）
- 公网 curl 三连（SC-1 工具页 title / SC-2 sitemap 3 loc / robots Sitemap 行真实域名）全部通过
- git ls-files dist 输出为空；test ! -f .nvmrc
- Search Console / 百度站长 sitemap 提交记录（真实域名）；D-20 大陆实测挂起项登记成文
</verification>

<success_criteria>
- SC-5：推 main 自动构建部署，门禁链任一失败不部署（控制台配置核对 + push 实证）
- SC-1：公网 curl 工具页返回含工具 title 与 description 的真实 HTML
- SC-2：公网 sitemap.xml 与 robots.txt 可达且含全部工具路由
- SC-3（D-20 拆分）：全球域名 curl 可达 ✓；大陆无代理实测挂起至自定义域名绑定后（登记，不阻塞）
- SC-4 已由 plan 02 门禁链在 EdgeOne 构建环境内天然执行（线上产物永远过门）
- D-04 完成（真实域名）；STATE.md ICP 备案 blocker 可清账（D-02 已确认）
</success_criteria>

<output>
Create `.planning/phases/03-seo/03-03-SUMMARY.md` when done
</output>
