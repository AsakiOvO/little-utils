---
phase: 01-app-skeleton-tool-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vite.config.ts
  - vitest.config.ts
  - eslint.config.ts
  - index.html
  - src/main.ts
  - src/App.vue
  - src/styles/tokens.css
  - src/styles/base.css
  - src/tools/tool.ts
  - src/tools/index.ts
  - src/router/routes.ts
  - src/router/index.d.ts
  - src/tools/registry.test.ts
  - src/router/routes.test.ts
  - src/layouts/home.layout.vue
  - src/layouts/tool.layout.vue
  - src/pages/home.vue
  - src/pages/not-found.vue
  - src/components/ToolCard.vue
  - src/components/ToolCard.test.ts
autonomous: true
requirements: [ARCH-01, ARCH-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "corepack pnpm type-check 与 corepack pnpm build（vite-ssg 预渲染）全绿，dist/index.html 产出（ARCH-02 构建基线）"
    - "路由表由注册表纯派生：home + tools.map + catch-all 404 置尾，routes.test.ts 回归保护（ARCH-01）"
    - "注册表不变量（路径唯一、以 / 开头、分类存在、keywords 非空）有单元测试覆盖，含空注册表合法用例（ARCH-01）"
    - "App.vue 按 route.meta.layout 切换 home/tool 双布局，布局组件不感知具体工具（D-03 结构定型）"
    - "home 页消费 toolsByCategory() 渲染分类卡片网格，空注册表显示空态而非白屏"
    - "@theme oklch 霓虹三色令牌 + 暗色语义底就位，@custom-variant dark 已声明但未启用（D-04/D-05）"
  artifacts:
    - path: "package.json"
      provides: "packageManager pnpm@11.25.0 锁定 + create-vue 基线脚本"
    - path: "src/tools/tool.ts"
      provides: "ToolMeta/Tool/ToolCategoryId 类型 + defineTool()（icon: Component 即 D-06 契约字段）"
    - path: "src/tools/index.ts"
      provides: "toolCategories + tools（空注册表起步）+ toolsByCategory + assertRegistryInvariants（单一事实来源）"
    - path: "src/router/routes.ts"
      provides: "注册表派生路由：home + tools.map + 404 置尾"
    - path: "src/router/index.d.ts"
      provides: "RouteMeta 增强：layout: 'home'|'tool'、tool?: Tool"
    - path: "src/styles/tokens.css"
      provides: "@theme oklch 霓虹三色 + 暗色语义底 + glow 阴影令牌（D-04）"
    - path: "src/router/routes.test.ts"
      provides: "派生路由回归测试（404 置尾、meta.layout、空注册表）"
    - path: "src/tools/registry.test.ts"
      provides: "注册表不变量 + isNew 派生测试"
    - path: "src/layouts/home.layout.vue"
      provides: "home 布局骨架（顶栏 + slot）"
    - path: "src/layouts/tool.layout.vue"
      provides: "tool 布局骨架（克制易读 + 返回导航）"
    - path: "src/components/ToolCard.vue"
      provides: "工具卡片组件（消费 icon/name/description/isNew）"
  key_links:
    - from: "src/router/routes.ts"
      to: "src/tools/index.ts"
      via: "tools.map() 纯派生 + 模块加载即 assertRegistryInvariants()，禁止手写静态工具路由"
      pattern: "tools\\.map"
    - from: "src/App.vue"
      to: "src/layouts/"
      via: "route.meta.layout 计算属性切换 <component :is>"
      pattern: "route\\.meta\\.layout"
    - from: "src/pages/home.vue"
      to: "src/tools/index.ts"
      via: "toolsByCategory() 渲染分类卡片网格"
      pattern: "toolsByCategory"
  flagged_assumptions:
    - "ARCH-01/empty 探针未决（空/单元素/null 输入）→ 已登记假设：空注册表（tools=[]）为合法状态——派生路由仅含 home+404、toolsByCategory 返回空分类数组、assertRegistryInvariants 通过不抛错；registry.test.ts 空注册表用例显式覆盖（探针 6 行中第 1 行，无静默丢弃）"
    - "ARCH-01/encoding 探针未决（长度/相等性定义）→ 已登记假设：路径与关键词的相等性按 JS 字符串精确比较（UTF-16 code units，不做 Unicode 归一化/NFC）；中英文关键词的模糊匹配语义留给 Phase 4 fuse.js 定夺（探针 6 行中第 2 行，无静默丢弃）"
  prohibitions:
    - statement: "工具的全部计算与数据转换必须且只能在浏览器本地完成：不得发起任何网络请求，不得将用户输入、输出或使用行为上传/发送到任何服务器或第三方（含匿名统计与遥测脚本）"
      category: privacy
---

<objective>
行走骨架（Wave 1）：create-vue 脚手架 + Tailwind 4 设计令牌 + 空注册表驱动的派生路由 + 双 layout 框架壳。

**用户故事（MVP_MODE，源自 ROADMAP Phase 1 Goal）：** **作为** 开发者与办公用户，**我想** 打开网站就立即用首批工具完成真实操作（浏览器本地完成、数据不出浏览器），**以便** 秒级搞定高频小任务；**作为** 站点作者，**我要** "注册表 → 路由/导航/分包"派生机制与 XSS 消毒管线作为全站契约定型，**以便** 后续 12 个工具零改动接入。

本计划交付：可 `corepack pnpm dev` 运行、可 `corepack pnpm build`（vite-ssg）预渲染的站点骨架；`defineTool()` 契约与空注册表；home/tool 双布局与工具卡片组件（D-03 结构定型）；@theme oklch 设计令牌（D-04，@custom-variant dark 声明不启用——D-05）。

Purpose: 这是 Walking Skeleton 的承重结构——注册表契约（costly：D-03/D-06 均锁定于此）与派生路由一旦定型，Phase 2-8 全部工作都是在本骨架上的扩展。
Output: 22 个文件（见 frontmatter）；`pnpm build` 产出 `dist/index.html`；注册表/路由回归测试就位。
</objective>

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-skeleton-tool-registry/01-CONTEXT.md
@.planning/phases/01-app-skeleton-tool-registry/01-RESEARCH.md
@.planning/phases/01-app-skeleton-tool-registry/01-PATTERNS.md
@.planning/phases/01-app-skeleton-tool-registry/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 脚手架 + 构建基线 + 设计令牌 + 空注册表派生路由（端到端可运行）</name>
  <files>package.json, pnpm-lock.yaml, vite.config.ts, vitest.config.ts, index.html, src/main.ts, src/App.vue, src/styles/tokens.css, src/styles/base.css, src/tools/tool.ts, src/tools/index.ts, src/router/routes.ts, src/pages/home.vue, src/pages/not-found.vue</files>
  <precondition>corepack 可用：`corepack --version` 输出版本号（本机 pnpm 直装 alias 已损坏属预期，全部命令走 `corepack pnpm`）</precondition>
  <read_first>
    - 01-RESEARCH.md §Standard Stack（Installation 与 pnpm 注意）、§Code Examples（vite.config.ts / main.ts / tokens.css）、§Pattern 1（前两块）、§Pattern 2（第一块）、§Pitfall 3/6/7
    - 01-CONTEXT.md D-04 / D-05
    - 01-PATTERNS.md 关键纪律 1-5
  </read_first>
  <action>
    1. 运行 `corepack enable pnpm` 消除本机损坏的 alias；此后统一使用 `corepack pnpm`。
    2. 脚手架进临时目录：`corepack pnpm create vue@latest .scaffold-tmp --ts --router --vitest --eslint --prettier --bare`（flags 已由 01-RESEARCH 验证）。**禁止**对 `.` 直接脚手架——工作区根目录非空（.planning/.claude/.codebuddy 等），create-vue 对非空目录有覆盖交互提示，会卡死或误删规划文件。
    3. 将 .scaffold-tmp 内容合并进工作区根目录：保留既有 .planning/.claude/.codebuddy 等目录；.gitignore 若冲突则合并而非覆盖；随后删除 .scaffold-tmp。
    4. 编辑 package.json：name 改为 `little-utils`；新增 `"packageManager": "pnpm@11.25.0"`（Pitfall 6）。
    5. `corepack pnpm install` 生成 lockfile；若 pnpm 提示 ignored build scripts，用 `corepack pnpm approve-builds` 仅批准 vite/esbuild 官方构建链并在 SUMMARY 记录。
    6. 安装核心依赖：`corepack pnpm add vue-router@5 @unhead/vue @vueuse/core tailwindcss @tailwindcss/vite dayjs dompurify` 与 `corepack pnpm add -D vite-ssg`（dompurify 提前安装，避免 Wave 2 并行计划的 package.json 冲突；勿安装 lucide-vue-next——已弃用，其替代包安装由 Plan 01-02 的供应链闸门把关）。
    7. vite.config.ts 照 RESEARCH Code Examples：plugins 为 vue() 与 tailwindcss()；alias `@` 指向 ./src；ssgOptions.includedRoutes 过滤含 `:` 的路由（暂不预渲染 catch-all）。
    8. src/main.ts 照 RESEARCH Code Examples：`export const createApp = ViteSSG(App, { routes, base: import.meta.env.BASE_URL }, ({ app }) => { app.use(createHead()) })`；createHead 从 `@unhead/vue/client` 子路径导入（v2+ 子路径导出）。若 @unhead/vue v3 与 vite-ssg 接线报错，按 RESEARCH 假设 A3 退化为 router.afterEach 设置 document.title 并在 SUMMARY 记录（useHead 全量接入归 Phase 3）。
    9. src/styles/tokens.css 照 RESEARCH Code Examples tokens.css 全文：`@import "tailwindcss"`；`@custom-variant dark` 声明但 Phase 1 不启用任何 `.dark` 类切换（D-05，Phase 2 只补切换不重做令牌）；`@theme` 内定义 --color-neon-cyan/--color-neon-magenta/--color-neon-yellow（oklch 值照抄该节，数值为设计占位、Phase 2 按对比度校验）、暗色语义底（--color-bg/surface/surface-raised/text-primary/text-muted/border）、--font-mono、--shadow-glow-cyan/--shadow-glow-magenta。src/styles/base.css：reset 与 :root 暗色基线（body 底色/文字色消费上述语义变量）。组件禁止硬编码色值，只消费 @theme 语义类/变量（RESEARCH Anti-Patterns）。
    10. src/tools/tool.ts 照 RESEARCH §Pattern 1 第一块：ToolCategoryId（'developer' | 'office'）、ToolMeta（name/path/description/keywords/category/icon: Component/component: () => Promise<Component>/createdAt）、Tool extends ToolMeta + isNew、NEW_WINDOW_DAYS=30、defineTool() 派生 isNew。icon 类型必须是组件引用而非字符串（D-06 契约字段）。
    11. src/tools/index.ts 照 §Pattern 1 第二块但 tools 初始为空数组（本计划只立契约，真实工具由 Plan 01-02/01-04 注册）：toolCategories（developer=开发辅助 / office=办公效率）、`export const tools: Tool[] = []`、`export function assertRegistryInvariants(list: Tool[] = tools): void`（重复 path / 非 / 开头或含 // / 未知 category / 空 keywords 四类不变量，违者 throw Error）——参数化默认值保持 RESEARCH 签名兼容且可单测错误分支。
    12. src/router/routes.ts 照 §Pattern 2：home 静态导入（首页不懒加载，LCP 关键页）+ `...tools.map(...)`（component: tool.component 懒加载、meta: { layout: 'tool', tool }）+ `{ path: '/:pathMatch(.*)*', component: NotFound, meta: { layout: 'home' } }` 置尾；模块加载即调用 assertRegistryInvariants()。**路由只能从注册表派生**（PATTERNS 关键纪律 3）。
    13. src/pages/home.vue 与 src/pages/not-found.vue 先写最小真实内容（站名标题 + 空态文案/404 文案 + 返回首页链接），Task 3 完善网格；src/App.vue 暂以 `<router-view />` 直出（Task 3 换双布局切换）。
    14. 清理 create-vue 模板残留（Pitfall 7）：删除生成的 src/router/index.ts、src/views/（或 --bare 残留的示例视图）、src/components/ 示例组件、src/assets/ 模板样式；删除 src/router 目录内除 routes.ts 外的模板文件。
    15. vitest 配置：确认 test.environment 为 'happy-dom'（create-vue 模板默认 jsdom 时改之），文件内保留一行注释说明 DOMPurify 如遇 happy-dom 边缘行为可按文件回退 jsdom（RESEARCH 假设 A4）。
    16. 跑 `corepack pnpm lint` 与 `corepack pnpm type-check` 修复到全绿（本计划尚不追加任何 lint 白名单规则，唯一出口卡口由 Plan 01-03 交付）。
  </action>
  <verify>
    <automated>corepack pnpm type-check && corepack pnpm build && test -f dist/index.html</automated>
    <fails_when>任一命令非零退出；或构建输出含 "is not defined"（window/document/localStorage 模块顶层访问违纪，Pitfall 3）；或 dist/index.html 不存在</fails_when>
  </verify>
  <acceptance_criteria>
    - `corepack pnpm type-check` 退出码 0；`corepack pnpm build` 退出码 0 且 dist/index.html 存在（test -f 通过）
    - package.json 同时含 `"packageManager": "pnpm@11.25.0"` 与 name `little-utils`
    - `grep -q "happy-dom" vitest.config.ts` 退出码 0（test.environment 确认为 happy-dom；A4 的 jsdom 回退说明注释保留在同一文件内——对应 action step 15）
    - `grep -r "HelloWorld" src/` 无输出；src/router/index.ts 模板文件与 src/views/ 目录不存在
    - src/styles/tokens.css 同时含 `@theme`、`--color-neon-cyan`、`--color-bg`、`@custom-variant dark`；src/styles/base.css 存在
    - src/tools/tool.ts 含 `icon: Component`（D-06 契约）与 `export function defineTool`；src/tools/index.ts 含 `toolCategories`、`tools: Tool[] = []`、`assertRegistryInvariants`
    - src/router/routes.ts 同时含 `tools.map` 与 `assertRegistryInvariants`；catch-all 404 路由存在且位于数组最后
    - package.json 依赖含 vue-router、@unhead/vue、@vueuse/core、tailwindcss、@tailwindcss/vite、dayjs、dompurify 与 devDep vite-ssg
  </acceptance_criteria>
  <done>vite-ssg 预渲染构建成功且产物就位；空注册表站点（home + 404）在 dev 与 build 两种形态下均可运行；模板残留清零。</done>
</task>

<task type="auto">
  <name>Task 2: 注册表契约与派生路由的回归测试 + RouteMeta 类型增强</name>
  <files>src/tools/index.ts, src/router/index.d.ts, src/tools/registry.test.ts, src/router/routes.test.ts</files>
  <read_first>
    - Task 1 产出的 src/tools/tool.ts、src/tools/index.ts、src/router/routes.ts
    - 01-RESEARCH.md §Pattern 1（第二块）、§Pattern 2（第二块）、§Validation Architecture（Test Map 前两行）
  </read_first>
  <action>
    1. src/router/index.d.ts：`import 'vue-router'` 后 `declare module 'vue-router'` 合并 RouteMeta：`layout?: 'home' | 'tool'`、`tool?: import('../tools/tool').Tool`（TS 标准增强点，勿手写 patch 类型）。
    2. src/tools/index.ts 补充 `toolsByCategory()` 派生 helper（照 §Pattern 1：Object.entries(toolCategories) 映射出 {id, name, tools: 分类内工具}）——消费方只读派生 helper，不碰原始数组。
    3. src/tools/registry.test.ts 用例：defineTool 对 30 天内 createdAt 派生 isNew=true、对超过 30 天派生 false；assertRegistryInvariants 对空数组通过不抛错（ARCH-01/empty 已登记假设的显式覆盖）；对重复 path、非 / 开头 path、未知 category、空 keywords 分别抛错（用直接传参调用参数化签名构造非法清单）。
    4. src/router/routes.test.ts 用例：routes[0] 为 home（path '/'、静态组件、meta.layout='home'）；最后一条为 catch-all 404（path '/:pathMatch(.*)*'）且**必须置尾**；空注册表时中间无工具路由段（与空注册表假设一致）；工具路由 meta.layout='tool' 且 meta.tool 携带完整元数据（用 vi.mock 或注入清单验证派生逻辑——直接 import 真实 tools 数组断言结构即可，空数组时对 map 段的断言跳过但 404 置尾断言必须存在）。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/tools/registry.test.ts src/router/routes.test.ts</automated>
    <fails_when>vitest 非零退出，或汇总行出现 "0 passed"（任一不变量/派生断言失败）</fails_when>
  </verify>
  <acceptance_criteria>
    - src/router/index.d.ts 含 `declare module 'vue-router'` 与 `layout?: 'home' | 'tool'` 与 `tool?:`
    - src/tools/index.ts 含 `export const toolsByCategory`
    - registry.test.ts 至少覆盖：isNew 30 天窗口正反两用例、空数组通过不抛错、重复 path / 非 / 开头 path / 未知 category / 空 keywords 四类抛错用例
    - routes.test.ts 至少覆盖：routes[0] 为 home、最后一条为 catch-all、meta.layout 断言
    - `corepack pnpm vitest run src/tools/registry.test.ts src/router/routes.test.ts` 退出码 0 且通过数 > 0
  </acceptance_criteria>
  <done>注册表契约（含 isNew 派生与四类不变量）与派生路由（含 404 置尾）均有单元测试回归保护；RouteMeta 类型增强就位。</done>
</task>

<task type="auto">
  <name>Task 3: 双 layout 框架壳 + 工具卡片 + 首页分类网格（D-03 结构定型）</name>
  <files>src/layouts/home.layout.vue, src/layouts/tool.layout.vue, src/App.vue, src/components/ToolCard.vue, src/components/ToolCard.test.ts, src/pages/home.vue, src/pages/not-found.vue</files>
  <reversibility rating="costly">D-03：首页结构是 Phase 4（站点体验）与 Phase 6（视觉强化）的改造基线，返工意味着两个 phase 连锁修改——结构此阶段定型，后续只填充内容/样式。</reversibility>
  <read_first>
    - 01-RESEARCH.md Code Examples（App.vue 布局切换）、§Pitfall 4（双 layout 接缝漏液）
    - 01-CONTEXT.md D-03 / D-04
    - Task 1 产出的 src/styles/tokens.css（语义类清单）
  </read_first>
  <action>
    1. src/App.vue 照 RESEARCH Code Examples：useRoute + computed layout（route.meta.layout === 'tool' 时 ToolLayout 否则 HomeLayout），`<component :is="layout">` 包裹 `<router-view />`。布局组件不感知具体工具（D-03 接缝）。
    2. src/layouts/home.layout.vue：顶栏（站名 little-utils，霓虹青克制点缀——仅静态 --shadow-glow-cyan 阴影类，Phase 1 不引入任何动画；动画将来只碰 opacity/transform）+ 默认 slot + 极简 footer；结构即 D-03 定型基线。
    3. src/layouts/tool.layout.vue：顶栏（含返回首页 router-link）+ 主区 `<router-view />`；风格克制易读，禁止任何 glow/动画类侵入（Pitfall 4：tool 区不得被装饰污染）；页面主内容宽度约束（max-w 容器）保证工具区可读。
    4. src/components/ToolCard.vue：props `tool: Tool`；渲染 icon（`<component :is="tool.icon" />`）、tool.name、tool.description、isNew 徽标（直接消费注册表派生值，勿在组件重算日期窗口）；整卡为 router-link 指向 tool.path；样式全部消费 @theme 语义类（bg-surface/border/text-primary/text-muted 等），禁止硬编码色值。
    5. src/pages/home.vue：消费 toolsByCategory() 按分类渲染小节（分类名 + 工具卡片网格，grid 基线：sm 单列 lg 三列即可，响应式精细化归 Phase 2）；分类无工具时渲染空态文案（如"该分类工具即将上线"），空注册表整页显示空态而非白屏（ARCH-01/empty 假设的 UI 侧体现）。
    6. src/pages/not-found.vue：404 文案 + 返回首页链接（落在 home 布局，由路由 meta 保证）。
    7. src/components/ToolCard.test.ts（@vue/test-utils mount）：传入假 tool（icon 用函数式组件 `() => h('span')` 模拟 D-06 组件引用形态）断言：名称与描述文本渲染、卡片根节点为指向 tool.path 的链接、isNew=true 时徽标出现且 false 时不出现。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/components/ToolCard.test.ts && grep -q "route.meta.layout" src/App.vue && grep -q "toolsByCategory" src/pages/home.vue</automated>
    <fails_when>vitest 非零退出或 "0 passed"；或任一 grep 退出码 1（App.vue 未按 meta 切布局 / home 未消费注册表派生 helper——手写静态清单即违纪）</fails_when>
  </verify>
  <acceptance_criteria>
    - src/App.vue 含 `route.meta.layout` 与 `<component :is` 与 `HomeLayout`/`ToolLayout` 两个 import
    - src/layouts/home.layout.vue 与 src/layouts/tool.layout.vue 存在且各含一个 router-view 或 slot 出口
    - src/pages/home.vue 含 `toolsByCategory`；src/components/ToolCard.vue 含 `:is="tool.icon"` 形态的动态组件渲染与 `tool.path` 的 router-link
    - `grep -q "route.meta.layout" src/App.vue` 退出码 0；`grep -q "toolsByCategory" src/pages/home.vue` 退出码 0
    - `corepack pnpm vitest run src/components/ToolCard.test.ts` 退出码 0 且覆盖 isNew 徽标出现/不出现两种断言
  </acceptance_criteria>
  <done>双 layout 切换与工具卡片就位；home 空态可渲染；ToolCard 行为有组件测试保护；D-03 结构定型完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → 本机 node_modules | 供应链：全部依赖经此进入（本计划安装 vue-router/@unhead/vue/@vueuse/core/tailwindcss/dayjs/dompurify/vite-ssg） |
| 构建期（Node） → 产物 | vite-ssg 在 Node 预渲染：模块顶层浏览器 API 违纪在构建期爆炸或泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm 依赖安装（本计划 Task 1） | high | mitigate | Package Legitimacy Audit 已由 researcher 运行（01-RESEARCH 审计表，全部 Approved）；package.json 锁 `packageManager: pnpm@11.25.0`；lockfile 提交；postinstall 全空已验证；@lucide/vue 属 [SUS] 单独由 Plan 01-02 Task 1 的 blocking-human 闸门把关（gate="blocking-human" 永不自动批准） |
| T-01-02 | Information Disclosure | 站点运行时外链/遥测 | medium | mitigate | 脚手架即零第三方运行时外链基线：无 analytics/无 CDN 字体/无遥测脚本；Tailwind @tailwindcss/vite 本地编译；must_haves.prohibitions 首条立约 |
| T-01-03 | Denial of Service | 模块顶层浏览器 API（vite-ssg 构建期） | medium | mitigate | 浏览器 API 只出现在 onMounted/事件回调/守卫分支；`corepack pnpm build` 作为每计划 verify 的 Nyquist 信号，违纪构建即红 |
| T-01-04 | Tampering | 路由注入（恶意 path 元数据） | low | accept | 注册表是仓库内代码而非运行时输入；assertRegistryInvariants 在模块加载即校验 path 形态与唯一性，剩余风险为作者自伤型，接受 |

## Canon Breadcrumb
- 通用 XSS/innerHTML 直插、OWASP 类注入 → canon，由 /gsd:secure-phase 与 Plan 01-03 的消毒管线 + ESLint 卡口拥有，此处不另铸禁令。
- 零第三方外链与体积预算 → Phase 3 ARCH-05 拥有，此处只立"无遥测"基线。
</threat_model>

<verification>
整体计划验证（全部通过才算完成）：
- `corepack pnpm vitest run` 全绿（fails_when: 任一用例失败或 "0 passed"）
- `corepack pnpm type-check` 通过（fails_when: vue-tsc 报任何类型错误）
- `corepack pnpm lint` 通过（fails_when: ESLint 报任何 error）
- `corepack pnpm build` 产出 dist/index.html（fails_when: 非零退出或文件缺失）
- `grep -r "HelloWorld" src/` 无命中（fails_when: 任何输出行——模板残留未清）
</verification>

<success_criteria>
1. `corepack pnpm dev` 启动后首页 HTTP 200，暗色底呈现，空注册表空态可见
2. `corepack pnpm build`（vite-ssg）成功，dist/index.html 存在
3. 路由表仅由注册表派生且 404 置尾（测试保护）
4. 注册表契约与不变量测试就位（含空注册表用例）
5. 双 layout + ToolCard + 分类网格（toolsByCategory 派生消费）就位
6. @theme oklch 令牌就位，@custom-variant dark 声明未启用
</success_criteria>

<output>
完成后创建 `.planning/phases/01-app-skeleton-tool-registry/01-01-SUMMARY.md`
</output>

## Artifacts this phase produces

（本计划交付：脚手架与构建基线、注册表契约与空注册表、派生路由、双布局框架壳、ToolCard、设计令牌。以下为本阶段全部产出总清单，与 SKELETON.md 同步。）

**共享契约与基建**
- `src/tools/tool.ts` — 类型 `ToolMeta`、`Tool`、`ToolCategoryId`；函数 `defineTool()`
- `src/tools/index.ts` — 常量 `toolCategories`、`tools`；函数 `toolsByCategory()`、`assertRegistryInvariants()`
- `src/router/routes.ts` — 常量 `routes`（注册表派生：home + tools.map + 404 置尾）
- `src/router/index.d.ts` — `RouteMeta` 增强字段 `layout`、`tool`
- `src/utils/sanitize.ts` — 函数 `sanitizeHtml()`、`escapeHtml()`
- `src/ui/safe-html.vue` — 组件 `SafeHtml`（全站唯一 HTML 字符串出口）
- `src/composables/useCopy.ts` — 函数 `useCopy()`
- `src/components/ToolCard.vue` — 组件 `ToolCard`
- `src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue` — 双布局组件
- `src/pages/home.vue`、`src/pages/not-found.vue` — 页面组件
**工具（Phase 5/8 批量样板）**
- `src/tools/timestamp-converter/` — 函数 `detectUnit()`、`fromTimestamp()`、`toTimestamp()`；组件 `timestamp-converter.vue`
- `src/tools/json-formatter/` — 函数 `formatJson()`、`minifyJson()`、`validateJson()`、`buildTree()`；组件 `json-formatter.vue`、`CodeMirrorJson.vue`、`JsonTree.vue`
**配置与脚本**
- `package.json`（`packageManager: pnpm@11.25.0` + `check:chunks` 脚本）、`vite.config.ts`（vue + tailwindcss + ssgOptions）、`vitest.config.ts`（happy-dom）、`eslint.config.ts`（唯一出口白名单卡口）、`src/styles/tokens.css`（@theme oklch 令牌）、`src/styles/base.css`、`scripts/check-chunks.mjs`
**测试**
- `src/tools/registry.test.ts`、`src/router/routes.test.ts`、`src/components/ToolCard.test.ts`、`src/composables/useCopy.test.ts`、`src/utils/sanitize.test.ts`、`src/ui/safe-html.test.ts`、`src/tools/timestamp-converter/timestamp-converter.service.test.ts`、`src/tools/json-formatter/json-formatter.service.test.ts`
