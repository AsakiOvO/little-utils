---
phase: 01-app-skeleton-tool-registry
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/tools/index.ts
  - src/tools/timestamp-converter/index.ts
  - src/tools/timestamp-converter/timestamp-converter.vue
  - src/tools/timestamp-converter/timestamp-converter.service.ts
  - src/tools/timestamp-converter/timestamp-converter.service.test.ts
  - src/composables/useCopy.ts
  - src/composables/useCopy.test.ts
autonomous: false
requirements: [TOOL-02]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "用户在首页卡片网格看到『时间戳转换』卡片，点击或直达 /timestamp-converter 打开工具页，刷新不 404（注册表派生路由零改动接入首次被真实工具验证）"
    - "Unix 时间戳 → 日期时间：显示浏览器本地时区时间、UTC 时间、Z 偏移（如 +08:00）与 IANA 时区名（如 Asia/Shanghai）"
    - "日期时间 → 时间戳：可指定目标时区，输出毫秒与秒两种粒度"
    - "秒/毫秒识别结果与原始输入必须回显；非法输入显示结构化错误信息而非崩溃（TOOL-02 + P3 透明性）"
    - "转换结果可一键复制，有已复制反馈（useClipboard legacy 降级可用）"
    - "『新增工具 = 新增目录 + 注册表一行』模式成立：timestamp-converter/index.ts 自注册 + tools 数组汇入一行，路由零手写"
  artifacts:
    - path: "src/tools/timestamp-converter/timestamp-converter.service.ts"
      provides: "detectUnit / fromTimestamp / toTimestamp（dayjs utc+timezone 插件链，utc 先于 timezone）"
    - path: "src/tools/timestamp-converter/timestamp-converter.service.test.ts"
      provides: "秒/毫秒识别边界、双向转换、时区偏移格式、非法输入结构化错误"
    - path: "src/tools/timestamp-converter/timestamp-converter.vue"
      provides: "双向转换 UI + 识别回显 + 时区选择 + 一键复制"
    - path: "src/tools/timestamp-converter/index.ts"
      provides: "defineTool 自注册样板（icon 组件引用 D-06 + component 懒加载）"
    - path: "src/composables/useCopy.ts"
      provides: "useClipboard({ legacy: true }) 统一封装（全站复制基建）"
    - path: "src/composables/useCopy.test.ts"
      provides: "happy-dom 剪贴板 mock 下的复制状态测试"
  key_links:
    - from: "src/tools/timestamp-converter/timestamp-converter.service.ts"
      to: "dayjs 插件链"
      via: "dayjs.extend(utc) 必须先于 dayjs.extend(timezone)（官方依赖顺序）"
      pattern: "dayjs\\.extend\\(utc\\)"
    - from: "src/tools/timestamp-converter/index.ts"
      to: "src/tools/index.ts"
      via: "defineTool 自注册 + 汇入一行（不加任何手写路由）"
      pattern: "import\\('\\./timestamp-converter\\.vue'\\)"
    - from: "src/tools/timestamp-converter/timestamp-converter.vue"
      to: "src/composables/useCopy.ts"
      via: "转换结果一键复制"
      pattern: "useCopy"
  flagged_assumptions:
    - "TOOL-02 探针未决（unclassified，手动复核）→ 已登记假设：秒/毫秒按位数启发判定——≤11 位按秒、12-14 位按毫秒、>14 位或非数字判 invalid（12 位毫秒为 1970 年代日期，属已知边界）；判定结果必须回显防 1000 倍错判；Intl.supportedValuesOf('timeZone') 不可用时降级为常用时区列表 + 手填 IANA 名（RESEARCH 假设 A6，实现时一行特性检测）（探针 6 行中第 6 行，无静默丢弃）"
  prohibitions:
    - statement: "时间戳工具不得在用户无感知的情况下替用户做单位假设：秒/毫秒识别结论与原始输入必须随结果一同回显，非法输入必须给出结构化错误而非静默吞掉或臆测值"
      category: transparency
---

<objective>
供应链闸门（@lucide/vue，D-06/costly）+ 首个真实工具端到端：时间戳转换（TOOL-02）+ 全站复制基建（useCopy）。行走骨架在此闭合——空注册表被真实数据取代，"新增目录 + 注册表一行"接入模式首次被验证。

Purpose: 成功标准 #2（时间戳双向转换含时区显示）在此交付；TOOL-02 同时是 Phase 5/8 批量工具的 registration-module 样板（service 纯函数 + 测试 + 自注册 + 复制）。
Output: timestamp-converter 工具目录（4 文件）、useCopy（+测试）、注册表汇入；`/timestamp-converter` 可直达、可转换、可复制。
</objective>

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-skeleton-tool-registry/01-CONTEXT.md
@.planning/phases/01-app-skeleton-tool-registry/01-RESEARCH.md
@.planning/phases/01-app-skeleton-tool-registry/01-VALIDATION.md
@.planning/phases/01-app-skeleton-tool-registry/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 供应链闸门——@lucide/vue 包名与导入示例人工确认（永不自动批准）</name>
  <read_first>
    - 01-RESEARCH.md §Package Legitimacy Audit（@lucide/vue 行：SUS too-new / Approved——官方延续包）与 §Open Questions #1
    - 01-CONTEXT.md D-06
  </read_first>
  <what-built>准备安装 @lucide/vue（D-06 图标方案）。lucide-vue-next 已被 npm 弃用（弃用原文指向 @lucide/vue）；@lucide/vue 为同仓库（lucide-icons/lucide）官方延续包，2026-09-03 刚发布新版，legitimacy 信号为 [SUS] too-new，按 Package Legitimacy Gate 策略须人工确认后方可安装。此检查点在任何模式下（含 auto/yolo）都不会被自动批准。</what-built>
  <how-to-verify>
    1. 打开 https://www.npmjs.com/package/@lucide/vue 确认：发布者为 lucide-icons 组织、仓库链接指向 github.com/lucide-icons/lucide、README 有导入示例（形如从 @lucide/vue 按名导入图标组件，例如 House）、近期有发布记录。
    2. 确认与 D-06 一致：注册表 icon 字段将存该包导出的组件引用。
    3. 如发现仓库不符、疑似仿冒或导入形态与 D-06 冲突：输入 STOP 并说明，执行中止改道。
  </how-to-verify>
  <acceptance_criteria>确认结果（确认时间、README 导入示例原文、发布者/仓库 URL）记录进本计划 SUMMARY；后续安装命令只允许用已确认的包名。</acceptance_criteria>
  <resume-signal>输入 "approved" 确认安装；或 "STOP" + 原因。</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: 安装 @lucide/vue + 时间戳转换工具完整切片（service 先测后码 + 自注册 + 页面）</name>
  <files>package.json, pnpm-lock.yaml, src/composables/useCopy.ts, src/tools/timestamp-converter/index.ts, src/tools/timestamp-converter/timestamp-converter.service.test.ts, src/tools/timestamp-converter/timestamp-converter.service.ts, src/tools/timestamp-converter/timestamp-converter.vue, src/tools/index.ts</files>
  <reversibility rating="costly">D-06：icon 字段类型（组件引用）是 defineTool 契约的一部分，Phase 5 批量工具接入后修改需触及全部工具目录；本任务的注册表行模式即全站样板。</reversibility>
  <read_first>
    - 01-RESEARCH.md §Pattern 7（dayjs 插件链完整代码）、§Pattern 1（第三块自注册）、Code Examples（useCopy）、§Pitfall 3、§Security Domain V7、§Validation Architecture（TOOL-02 行）
    - 01-CONTEXT.md D-06、`<specifics>`（时区显示要求）
    - 01-01-SUMMARY.md（tool.ts 契约与 routes 派生的最终形态）
  </read_first>
  <behavior>
    - Test 1（单位识别）: detectUnit('1735689600') 返回 's'；detectUnit('1735689600000') 返回 'ms'；detectUnit('-86400') 返回 's'（负数合法）；detectUnit('abc') 返回 'invalid'；detectUnit('123456789012345') 返回 'invalid'（>14 位）；detectUnit('') 返回 'invalid'（空输入不崩溃）
    - Test 2（正向转换）: fromTimestamp('0') 返回对象含 detectedUnit 's'、utc 等于 '1970-01-01 00:00:00'、offset 非空、timeZone 等于 dayjs.tz.guess() 结果；local 字段与 dayjs(Number(0)) 本地格式一致（用 dayjs 自身计算对照，避免测试硬编码本机时区）
    - Test 3（反向转换）: toTimestamp('2025-01-01 08:00:00', 'Asia/Shanghai') 返回 { ok: true, ms: 1735689600000, sec: 1735689600 }；toTimestamp('not-a-date') 返回 ok:false（结构化错误，不抛异常）
    - Test 4（目标时区）: fromTimestamp('1735689600000', 'UTC') 的 target 字段含 'Z' 或 '+00:00' 偏移且日期部分为 2025-01-01（UTC 视角）
    - Test 5（毫秒往返）: toTimestamp(fromTimestamp('1735689600000').local.replaceAll('-', '/'), undefined).ms === 1735689600000（本地时区往返一致；如格式解析受限，允许测试改用显式 'YYYY-MM-DD HH:mm:ss' 中间格式）
  </behavior>
  <action>
    1. 执行 `corepack pnpm add @lucide/vue`（仅此包名——Task 1 人工确认后的官方延续包；lucide-vue-next 已弃用，禁止安装）。
    2. 先写 src/composables/useCopy.ts 照 RESEARCH Code Examples：`useClipboard` 来自 @vueuse/core，参数 `{ legacy: true, copiedDuring: 1500 }`，返回 `{ copy, copied, isSupported }`——legacy 提供安全上下文缺失时的降级路径（Don't Hand-Roll：禁止裸调 navigator.clipboard）。
    3. RED：先写 src/tools/timestamp-converter/timestamp-converter.service.test.ts（上方 behavior 五组用例），运行 `corepack pnpm vitest run src/tools/timestamp-converter/timestamp-converter.service.test.ts` 确认失败。
    4. GREEN：实现 src/tools/timestamp-converter/timestamp-converter.service.ts 照 RESEARCH §Pattern 7：`dayjs.extend(utc)` 必须写在 `dayjs.extend(timezone)` 之前（官方插件依赖顺序，写错时区换算全错）；导出 detectUnit（≤11 位秒、12-14 位毫秒、其余 invalid，正则限整数）、fromTimestamp（返回 detectedUnit/local/utc/offset/timeZone/target）、toTimestamp（返回 {ok, ms, sec}，非法输入 ok:false）。service 必须零 Vue/DOM 依赖、浏览器 API 不出现在模块顶层（Pitfall 3：dayjs.tz.guess() 只能在函数体内调用）；结构化错误 {ok,error}（V7），service 不裸抛。运行测试至全绿。
    5. src/tools/timestamp-converter/index.ts 照 §Pattern 1 第三块：`export default defineTool({ name: '时间戳转换', path: '/timestamp-converter', description: 'Unix 时间戳与日期时间双向转换，含时区显示，浏览器本地完成', keywords: ['时间戳', 'timestamp', 'unix', '日期', '时区', 'date'], category: 'developer', icon: Clock, component: () => import('./timestamp-converter.vue'), createdAt: '2026-09-04' })`——Clock 从 @lucide/vue 按名导入。
    6. src/tools/index.ts 汇入：import timestampConverter + tools 数组加入该元素（仅此一行改动，不触碰 routes.ts——路由由注册表自动派生，这正是 ARCH-01 契约的首次真实验证）。
    7. src/tools/timestamp-converter/timestamp-converter.vue：上部输入区（时间戳字符串输入框 + 转换按钮，或输入即算）；结果区回显"识别为秒/毫秒 + 原始输入 + 本地时间 + UTC 时间 + Z 偏移 + IANA 时区名"，并附目标时区下拉（Intl.supportedValuesOf('timeZone') 特性检测，失败降级为常用时区列表 + 手填输入框，A6）；下部反向区（日期时间输入 + 可选时区 + 毫秒/秒输出）；invalid 输入渲染结构化错误卡（不崩溃）；每段结果带复制按钮接 useCopy。全部输出经 Vue 文本插值渲染（vnode 路径，不构造任何 HTML 字符串——ARCH-04 渲染纪律）。样式消费 @theme 语义类，风格克制（tool.layout 内）。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/tools/timestamp-converter/timestamp-converter.service.test.ts && corepack pnpm type-check</automated>
    <fails_when>vitest 非零退出或汇总行 "0 passed"（任一 behavior 用例失败）；或 vue-tsc 报任何类型错误</fails_when>
  </verify>
  <done>服务五组用例全绿；/timestamp-converter 经注册表派生自动可直达（无手写路由）；首页网格出现该工具卡片；icon 组件引用契约（D-06）落地。</done>
</task>

<task type="auto">
  <name>Task 3: useCopy 组件级测试 + dev/build 端到端验收 + 视觉走查清单</name>
  <files>src/composables/useCopy.test.ts, src/tools/timestamp-converter/timestamp-converter.vue</files>
  <read_first>
    - Task 2 产出的 src/composables/useCopy.ts 与 timestamp-converter.vue
    - 01-RESEARCH.md §Validation Architecture（成功标准 #4 行）、01-VALIDATION.md Manual-Only Verifications
  </read_first>
  <action>
    1. src/composables/useCopy.test.ts：happy-dom 环境——用 vi.stubGlobal 或 Object.defineProperty 模拟 navigator.clipboard.writeText 为 vi.fn(resolve)，断言调用 copy 后 copied 状态翻转为 true 且 copiedDuring 窗口后复位（可用 vi.useFakeTimers）；再模拟 isSupported 为 false 的降级路径（legacy execCommand 分支），断言 copy 仍可调用且不抛异常。
    2. timestamp-converter.vue 补齐复制交互细节：已复制状态给按钮文案/样式反馈（copied 驱动），复制内容为格式化的结果文本行（本地时间、UTC、时间戳原文），确保键盘可达（button 元素原生语义即可）。
    3. 端到端验收（自动化部分）：后台启动 `corepack pnpm dev`，等待 ready 后用 node fetch 断言 http://localhost:5173/ 与 http://localhost:5173/timestamp-converter 均返回 200（dev SPA 路由回退），随后结束 dev 进程；再跑一次 `corepack pnpm build` 确认新工具路由进入 vite-ssg 预渲染且构建不因任何浏览器 API 违纪而失败。
    4. 视觉走查项（无法自动化的部分）放入本任务 human-check，由 phase 末 UAT 批量收口：暗色底与霓虹点缀观感、卡片网格布局、识别回显完整性、错误态呈现、复制反馈。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/composables/useCopy.test.ts && corepack pnpm build</automated>
    <fails_when>vitest 非零退出或 "0 passed"；或 vite-ssg 构建非零退出（含 "is not defined" 类违纪输出）</fails_when>
    <human-check>后台 corepack pnpm dev 运行中，走查：1) 首页『开发辅助』分类显示『时间戳转换』卡片；2) 点击进入工具页；3) 输入 1735689600000 → 回显识别为毫秒 + 本地时间（含 +08:00 类偏移与 Asia/Shanghai 类时区名）+ UTC 时间；4) 输入 abc → 显示错误不崩溃；5) 反向输入 2025-01-01 08:00:00 + Asia/Shanghai → 输出 1735689600000；6) 点击复制 → 按钮出现已复制反馈。</human-check>
  </verify>
  <done>useCopy 有回归测试；新工具通过 dev 与 build 双形态验收；视觉走查清单已列入 human-check 待 UAT 收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | @lucide/vue 安装（[SUS] too-new 信号）——人工闸门把关 |
| 用户输入（时间戳/日期文本）→ 渲染 | 工具页回显用户输入与转换结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | @lucide/vue 安装（Task 1→Task 2） | high | mitigate | Task 1 为 gate="blocking-human" 检查点：人工在 npmjs.com 核对发布者/仓库/README 导入示例后方可 `corepack pnpm add @lucide/vue`；lockfile 提交；该检查点在任何模式（含 auto）不被自动批准 |
| T-01-05 | Tampering | 用户输入回显（timestamp-converter.vue） | medium | mitigate | 全部输出走 Vue 文本插值（vnode 自动转义），不构造任何 HTML 字符串、不使用唯一出口组件——结构性不可能注入；01-03 的 ESLint 卡口提供全站兜底 |
| T-01-06 | Information Disclosure | 剪贴板写入（useCopy） | low | accept | 仅写入用户主动点击复制的内容；无网络传输；无持久化（Phase 1 零 localStorage） |
| T-01-07 | Denial of Service | dayjs 插件顺序错误导致时区全错 | medium | mitigate | utc 先于 timezone 的顺序写入 action 硬约束 + 服务测试用 UTC/固定时区断言兜底 |

## Canon Breadcrumb
- 用户输入 XSS（canon）→ 由 Plan 01-03 消毒管线 + /gsd:secure-phase 拥有；本计划以"零 HTML 字符串路径"结构性规避，不另铸禁令。
</threat_model>

<verification>
- `corepack pnpm vitest run` 全绿（fails_when: 任一用例失败）
- `corepack pnpm type-check` 通过（fails_when: 类型错误）
- `corepack pnpm lint` 通过（fails_when: 任何 error）
- `corepack pnpm build` 成功且 dist/timestamp-converter/index.html 存在（fails_when: 非零退出或文件缺失）
- 注册表新增路径唯一性不变量仍成立（registry.test.ts 通过）
</verification>

<success_criteria>
1. /timestamp-converter 可直达、刷新不 404，未手写任何路由（派生验证）
2. 时间戳 ↔ 日期时间双向转换可用，含本地时区、UTC、Z 偏移与 IANA 时区名显示
3. 识别结果与原始输入回显；非法输入结构化报错
4. 结果一键复制且有反馈
5. 『加目录 + 注册表一行』接入模式成立并记录进 SUMMARY（Phase 5 样板）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-app-skeleton-tool-registry/01-02-SUMMARY.md`（含 @lucide/vue 人工确认记录）
</output>

## Artifacts this phase produces

（本计划交付：@lucide/vue 供应链闸门、timestamp-converter 工具目录、useCopy、注册表首个真实条目。以下为本阶段全部产出总清单，与 SKELETON.md 同步。）

**共享契约与基建**
- `src/tools/tool.ts` — 类型 `ToolMeta`、`Tool`、`ToolCategoryId`；函数 `defineTool()`
- `src/tools/index.ts` — 常量 `toolCategories`、`tools`；函数 `toolsByCategory()`、`assertRegistryInvariants()`
- `src/router/routes.ts` — 常量 `routes`（注册表派生：home + tools.map + 404 置尾）
- `src/router/index.d.ts` — `RouteMeta` 增强字段 `layout`、`tool`
- `src/utils/sanitize.ts` — 函数 `sanitizeHtml()`、`escapeHtml()`
- `src/ui/safe-html.vue` — 组件 `SafeHtml`（全站唯一 HTML 字符串出口）
- `src/composables/useCopy.ts` — 函数 `useCopy()`
- `src/components/ToolCard.vue` — 组件 `ToolCard`
- `src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue` — 双布局组件
- `src/pages/home.vue`、`src/pages/not-found.vue` — 页面组件
**工具（Phase 5/8 批量样板）**
- `src/tools/timestamp-converter/` — 函数 `detectUnit()`、`fromTimestamp()`、`toTimestamp()`；组件 `timestamp-converter.vue`
- `src/tools/json-formatter/` — 函数 `formatJson()`、`minifyJson()`、`validateJson()`、`buildTree()`；组件 `json-formatter.vue`、`CodeMirrorJson.vue`、`JsonTree.vue`
**配置与脚本**
- `package.json`（`packageManager: pnpm@11.25.0` + `check:chunks` 脚本）、`vite.config.ts`（vue + tailwindcss + ssgOptions）、`vitest.config.ts`（happy-dom）、`eslint.config.ts`（唯一出口白名单卡口）、`src/styles/tokens.css`（@theme oklch 令牌）、`src/styles/base.css`、`scripts/check-chunks.mjs`
**测试**
- `src/tools/registry.test.ts`、`src/router/routes.test.ts`、`src/components/ToolCard.test.ts`、`src/composables/useCopy.test.ts`、`src/utils/sanitize.test.ts`、`src/ui/safe-html.test.ts`、`src/tools/timestamp-converter/timestamp-converter.service.test.ts`、`src/tools/json-formatter/json-formatter.service.test.ts`
