---
phase: 01-app-skeleton-tool-registry
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/utils/sanitize.ts
  - src/utils/sanitize.test.ts
  - src/ui/safe-html.vue
  - src/ui/safe-html.test.ts
  - eslint.config.ts
autonomous: true
requirements: [ARCH-04]
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "sanitizeHtml 对注入样本集（script 标签、img onerror 事件属性、javascript: 伪协议 href、内联事件属性）全部中和，无害文本与基本标记保留（CONTEXT <specifics> 验收标准的自动化形态）"
    - "SafeHtml 挂载含注入载荷的 html 后，组件 DOM 内不存在可执行节点（无 script 元素、无 on* 属性）"
    - "全站唯一出口收敛：ESLint 规则（vue/no-v-html + ignorePattern '^sanitized'）生效——任何其他文件使用该模板指令时 corepack pnpm lint 必报 error；豁免绑定名只能以 sanitized 开头，而该命名只能来自 sanitizeHtml() 返回值（lint + 类型双重收敛）"
    - "无 DOM 环境（vite-ssg 构建 / Node 测试）下 sanitizeHtml 退化为纯转义，构建期不失败（sanitize.test.ts 显式覆盖该分支）"
  artifacts:
    - path: "src/utils/sanitize.ts"
      provides: "sanitizeHtml(dirty): string（DOMPurify + 无 DOM 退化 escapeHtml）与 escapeHtml(s): string"
    - path: "src/utils/sanitize.test.ts"
      provides: "注入样本集回归（script/img onerror/javascript: URI/事件属性/正常文本保留/无 window 分支）"
    - path: "src/ui/safe-html.vue"
      provides: "SafeHtml 组件（全站唯一允许该模板指令的组件，绑定名 sanitizedHtml）"
    - path: "src/ui/safe-html.test.ts"
      provides: "挂载注入载荷后无可执行节点的组件级断言"
    - path: "eslint.config.ts"
      provides: "vue/no-v-html + ignorePattern '^sanitized' 静态卡口"
  key_links:
    - from: "src/ui/safe-html.vue"
      to: "src/utils/sanitize.ts"
      via: "computed sanitizedHtml = sanitizeHtml(props.html)——命名耦合即白名单机制"
      pattern: "sanitizeHtml"
    - from: "eslint.config.ts"
      to: "src/ui/safe-html.vue"
      via: "ignorePattern '^sanitized' 只放行 sanitized* 绑定名，出口被 lint 收敛到唯一组件"
      pattern: "ignorePattern"
  flagged_assumptions:
    - "ARCH-04 探针未决（unclassified，手动复核）→ 已登记假设：DOMPurify 3.4.14 在 happy-dom 下如出现边缘行为，按测试文件加 // @vitest-environment jsdom 回退（官方 jsdom 支持在案，RESEARCH 假设 A4）；该回退本身必须被测试运行验证而非注释声明（探针 6 行中第 4 行，无静默丢弃）"
  prohibitions:
    - statement: "禁止在 src/ui/safe-html.vue 之外出现任何 v-html/innerHTML/insertAdjacentHTML 直插用户输入（或任何未经 sanitizeHtml 的内容）的渲染路径：全站唯一 HTML 字符串出口是 SafeHtml 组件，ESLint vue/no-v-html（ignorePattern '^sanitized'）+ no-restricted-properties 卡口与注入样本测试集是本禁令的强制执行机制"
      category: security
---

<objective>
ARCH-04 全站契约：XSS 消毒渲染管线两级收敛——vnode 优先（本阶段两个工具实际零 HTML 字符串路径），HTML 字符串路径唯一出口（sanitizeHtml + SafeHtml），ESLint 静态卡口封死旁路。

Purpose: 成功标准 #4 的管线部分（"向工具粘贴含脚本标签/事件属性的内容仅渲染为纯文本，不执行任何脚本"）由此计划的结构性保障 + 注入样本回归集共同兑现；Phase 5/8 所有"需要渲染富文本"的工具只有这一条合法通道。
Output: sanitize.ts + SafeHtml 组件 + 注入样本测试集 + ESLint 白名单卡口（5 文件）。
</objective>

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-app-skeleton-tool-registry/01-CONTEXT.md
@.planning/phases/01-app-skeleton-tool-registry/01-RESEARCH.md
@.planning/phases/01-app-skeleton-tool-registry/01-VALIDATION.md
@.planning/phases/01-app-skeleton-tool-registry/01-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 消毒函数管线切片（DOMPurify + 无 DOM 退化）+ 注入样本测试集</name>
  <files>src/utils/sanitize.ts, src/utils/sanitize.test.ts</files>
  <read_first>
    - 01-RESEARCH.md §Pattern 5（第一块 sanitize.ts 完整代码与 CITED 依据）、§Validation Architecture（ARCH-04 两行 Test Map）、§Security Domain（V5）
    - 01-CONTEXT.md `<specifics>`（注入验收标准原文）
    - 01-01-SUMMARY.md（vitest happy-dom 环境与 A4 回退注释）
  </read_first>
  <behavior>
    - Test 1: sanitizeHtml('\<script>alert(1)\</script>hello') 输出不含任何 script 标签且保留文本 'hello'
    - Test 2: sanitizeHtml('\<img src=x onerror=alert(1)>') 输出不含 onerror 属性（img 标签本身可被剥离或留下无害残留，断言点是不可执行）
    - Test 3: sanitizeHtml('\<a href="javascript:alert(1)">x\</a>') 输出不含 javascript: 伪协议 href（ALLOWED_URI_REGEXP 仅放行 https?/mailto/tel）
    - Test 4: sanitizeHtml('\<div onclick="alert(1)">x\</div>') 输出不含 onclick 内联事件属性
    - Test 5: sanitizeHtml('\<p>正常 \<b>文本\</b>\</p>') 保留基本标记与文本（无害内容不被误杀）
    - Test 6: escapeHtml 对 & < > " ' 五类字符全部转义；sanitizeHtml 在 window 未定义环境下（vi.stubGlobal('window', undefined)）走纯转义分支且输出不含任何尖括号标签（构建期退化路径显式覆盖）
  </behavior>
  <action>
    1. RED：先写 src/utils/sanitize.test.ts（上方六组用例；Test 6 用 vi.stubGlobal 模拟无 window 后调用，注意 sanitize.ts 的分支判断是每次调用时的 typeof window 检查而非模块加载期），运行确认失败。
    2. GREEN：实现 src/utils/sanitize.ts 照 RESEARCH §Pattern 5 第一块逐行：`import DOMPurify from 'dompurify'`；sanitizeHtml 在 typeof window === 'undefined' 时返回 escapeHtml(dirty)（vite-ssg 预渲染/Node 测试安全），否则 `DOMPurify.sanitize(dirty, { ALLOWED_URI_REGEXP: /^(?:https?|mailto|tel):/i })`；escapeHtml 用五字符映射表实现。禁止手写正则过滤标签（Don't Hand-Roll：DOMPurify 是唯一合法消毒器）。
    3. 若 happy-dom 下个别用例出现 DOMPurify 边缘行为：该测试文件顶部加 `// @vitest-environment jsdom` 回退（官方 Node 20-26 jsdom 支持在案），并在 SUMMARY 记录回退原因（A4 假设的落地）。
    4. service 风格纪律：函数纯、无 Vue 依赖、错误不裸抛（输入非字符串时按空串处理返回 ''）。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/utils/sanitize.test.ts</automated>
    <fails_when>vitest 非零退出或汇总行 "0 passed"——任一注入载荷在输出中存活（script 标签/on* 属性/javascript: href）即失败</fails_when>
  </verify>
  <acceptance_criteria>
    - src/utils/sanitize.ts 同时含 `import DOMPurify from 'dompurify'`、`typeof window === 'undefined'` 分支、`ALLOWED_URI_REGEXP`、`export function escapeHtml`
    - sanitize.test.ts 覆盖 behavior 六组用例（script/img 事件属性/javascript: 协议/内联事件/无害文本保留/vi.stubGlobal 无 window 分支）
    - `corepack pnpm vitest run src/utils/sanitize.test.ts` 退出码 0 且通过数 > 0
    - 如启用了 jsdom 回退：测试文件首行存在 `// @vitest-environment jsdom` 且 SUMMARY 记录回退原因
  </acceptance_criteria>
  <done>注入样本集六组用例全绿；无 DOM 退化分支被显式测试覆盖；sanitizeHtml 成为全站唯一消毒入口。</done>
</task>

<task type="auto">
  <name>Task 2: SafeHtml 唯一出口组件 + ESLint 白名单卡口（静态封旁路）</name>
  <files>src/ui/safe-html.vue, src/ui/safe-html.test.ts, eslint.config.ts</files>
  <read_first>
    - 01-RESEARCH.md §Pattern 5（第二块 safe-html.vue 与 ESLint flat config 块、机制说明段——ignorePattern 是唯一豁免选项）
    - Task 1 产出的 src/utils/sanitize.ts
    - 01-01 产出的 eslint.config.ts（create-vue flat config 结构）
  </read_first>
  <action>
    1. src/ui/safe-html.vue 照 RESEARCH §Pattern 5 第二块：props `html: string`；`computed sanitizedHtml = sanitizeHtml(props.html)`；模板中唯一一次使用该模板指令渲染 sanitizedHtml，且其上方保留 eslint-disable-next-line 行注释（照抄该 Pattern 的注释形态）。绑定名必须以 sanitized 开头——这不是风格偏好，而是 lint 白名单的触发条件。
    2. eslint.config.ts 在 create-vue 生成的 flat config 之上追加一个规则块（照 §Pattern 5 第三块）：规则键为 'vue/no-v-html'，取值 `['error', { ignorePattern: '^sanitized' }]`。机制：该规则报告所有模板指令用法，唯一豁免是绑定变量名匹配 '^sanitized'；而 sanitized* 命名只能来自 sanitizeHtml() 返回值——出口被 lint + 命名双重收敛。同时追加 no-restricted-properties 覆盖 innerHTML 与 insertAdjacentHTML 的直接赋值/调用（code review 红线的静态化）。
    3. src/ui/safe-html.test.ts（@vue/test-utils mount）：传入含 script 标签与 img 事件属性载荷的 html prop，断言 wrapper.element 内 querySelector 无 script 元素、遍历属性无 on* 开头的事件属性；传入无害文本断言正常渲染。
    4. 验证卡口有牙：临时在任一 src 下的 .vue 文件中引入一次违规模板指令用法并运行 lint 确认报 error，随后撤销该临时改动（fail-first 人工证明，结果记录进 SUMMARY）。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/safe-html.test.ts && corepack pnpm lint</automated>
    <fails_when>vitest 非零退出（组件 DOM 内存在可执行节点）；或 lint 输出任何 error——包括其他文件违规使用该模板指令被 vue/no-v-html 命中</fails_when>
  </verify>
  <acceptance_criteria>
    - src/ui/safe-html.vue 存在且其模板中该指令用法仅出现一次，绑定变量名以 sanitized 开头，上方含 eslint-disable-next-line 行注释
    - `grep -q "ignorePattern" eslint.config.ts` 退出码 0 且同文件含 `'^sanitized'` 与 `'vue/no-v-html'` 规则键
    - safe-html.test.ts 断言 mount 注入载荷后 wrapper 内无 script 元素、无 on* 开头属性
    - `corepack pnpm vitest run src/ui/safe-html.test.ts && corepack pnpm lint` 退出码 0
    - fail-first 验证（临时违规文件触发 lint error 后撤销）的结果记录在 SUMMARY
  </acceptance_criteria>
  <done>SafeHtml 唯一出口就位且注入载荷挂载后无可执行节点；ESLint 卡口全局生效并经 fail-first 验证；全站渲染出口收敛完成（ARCH-04）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入/未来富文本 → DOM 渲染 | 本计划的核心信任边界：任何 HTML 字符串到达 DOM 前必须过 sanitizeHtml |
| 构建期（Node，无 DOM） → 预渲染产物 | sanitize 在无 window 环境的退化路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation of Privilege | v-html 旁路（绕过唯一出口直插用户输入） | critical | mitigate | 1) vnode 优先路线（两个 Phase 1 工具零 HTML 字符串路径）；2) HTML 字符串路径收敛到 SafeHtml 唯一组件；3) ESLint vue/no-v-html error 级 + ignorePattern 白名单全站生效；4) 注入样本集（script/onerror/javascript:/onclick）进 vitest 回归；5) Task 2 Step 4 fail-first 证明卡口有牙 |
| T-01-08 | Denial of Service | vite-ssg 构建期无 window 时 sanitize 崩溃 | low | mitigate | typeof window 守卫 + escapeHtml 退化分支 + 显式测试覆盖（Test 6）；构建以 `corepack pnpm build` 持续验证 |
| T-01-09 | Tampering | DOMPurify 供应链 | low | mitigate | cure53 官方、周下载 6430 万、legitimacy 审计 Approved（01-RESEARCH 审计表）；版本锁定于 lockfile |
| T-01-SC | Tampering | npm 依赖安装 | high | mitigate | 承接 Plan 01-01 的 T-01-SC 处置：审计表 + packageManager 锁定 + lockfile 提交；本计划零新增安装（dompurify 已随 01-01 安装） |

## Canon Breadcrumb
- OWASP XSS / 注入类 canon 项 → /gsd:secure-phase + 本计划的 ESLint/测试卡口拥有；唯一出口禁令已落 must_haves.prohibitions（checker W3 补录，与本计划卡口同源）。
</threat_model>

<verification>
- `corepack pnpm vitest run src/utils/sanitize.test.ts src/ui/safe-html.test.ts` 全绿（fails_when: 任一注入载荷存活或组件含可执行节点）
- `corepack pnpm lint` 通过（fails_when: 任何 error；唯一出口组件之外使用该模板指令必被 vue/no-v-html 命中）
- `corepack pnpm type-check` 通过（fails_when: 类型错误）
- `corepack pnpm build` 成功（fails_when: 无 window 守卫缺失导致预渲染崩溃）
</verification>

<success_criteria>
1. 注入样本集全绿：script 标签、img onerror、javascript: 伪协议、内联事件属性全部被中和，无害文本保留
2. SafeHtml 挂载注入载荷后无可执行节点
3. 全站唯一出口：lint 卡口 + '^sanitized' 白名单生效且经 fail-first 验证
4. 无 DOM 构建期退化为纯转义且被测试覆盖
</success_criteria>

<output>
完成后创建 `.planning/phases/01-app-skeleton-tool-registry/01-03-SUMMARY.md`（含 fail-first 卡口验证记录）
</output>

## Artifacts this phase produces

（本计划交付：sanitize 管线、SafeHtml 唯一出口、ESLint 卡口。以下为本阶段全部产出总清单，与 SKELETON.md 同步。）

**共享契约与基建**
- `src/tools/tool.ts` — 类型 `ToolMeta`、`Tool`、`ToolCategoryId`；函数 `defineTool()`
- `src/tools/index.ts` — 常量 `toolCategories`、`tools`；函数 `toolsByCategory()`、`assertRegistryInvariants()`
- `src/router/routes.ts` — 常量 `routes`（注册表派生：home + tools.map + 404 置尾）
- `src/router/index.d.ts` — `RouteMeta` 增强字段 `layout`、`tool`
- `src/utils/sanitize.ts` — 函数 `sanitizeHtml()`、`escapeHtml()`
- `src/ui/safe-html.vue` — 组件 `SafeHtml`（全站唯一 HTML 字符串出口）
- `src/composables/useCopy.ts` — 函数 `useCopy()`
- `src/components/ToolCard.vue` — 组件 `ToolCard`
- `src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue` — 双布局组件
- `src/pages/home.vue`、`src/pages/not-found.vue` — 页面组件
**工具（Phase 5/8 批量样板）**
- `src/tools/timestamp-converter/` — 函数 `detectUnit()`、`fromTimestamp()`、`toTimestamp()`；组件 `timestamp-converter.vue`
- `src/tools/json-formatter/` — 函数 `formatJson()`、`minifyJson()`、`validateJson()`、`buildTree()`；组件 `json-formatter.vue`、`CodeMirrorJson.vue`、`JsonTree.vue`
**配置与脚本**
- `package.json`（`packageManager: pnpm@11.25.0` + `check:chunks` 脚本）、`vite.config.ts`（vue + tailwindcss + ssgOptions）、`vitest.config.ts`（happy-dom）、`eslint.config.ts`（唯一出口白名单卡口）、`src/styles/tokens.css`（@theme oklch 令牌）、`src/styles/base.css`、`scripts/check-chunks.mjs`
**测试**
- `src/tools/registry.test.ts`、`src/router/routes.test.ts`、`src/components/ToolCard.test.ts`、`src/composables/useCopy.test.ts`、`src/utils/sanitize.test.ts`、`src/ui/safe-html.test.ts`、`src/tools/timestamp-converter/timestamp-converter.service.test.ts`、`src/tools/json-formatter/json-formatter.service.test.ts`
