---
phase: 01-app-skeleton-tool-registry
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/timestamp-converter/timestamp-converter.service.ts
  - src/tools/timestamp-converter/timestamp-converter.service.test.ts
  - src/tools/timestamp-converter/timestamp-converter.component.test.ts
  - tsconfig.vitest.json
  - eslint.config.ts
  - scripts/check-chunks.mjs
autonomous: true
gap_closure: true
requirements: [TOOL-02, ARCH-01, ARCH-02]
user_setup: []

estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "在 /timestamp-converter 正向与反向选择『手动输入 IANA 时区…』并输入非法值（UTC+8、Foo/Bar）→ 渲染 role=alert 结构化错误卡，页面不白屏、其余部分正常可用（Gap-1/CR-01 闭合，01-02 must_haves truth #4 恢复）"
    - "fromTimestamp/toTimestamp 对非法时区返回 ok:false + error（含『未知时区』与 IANA 名提示）、全部字段 null，绝不抛异常（V7『不裸抛』契约恢复）"
    - "detectUnit(' 1735689600 ') === 's'、detectUnit('\\t1735689600000\\n') === 'ms'——空白不再引发 1000 倍单位错判（Gap-2/WR-01 闭合，全站公共 API 防线恢复）"
    - "全部 *.test.ts 测试文件（含本计划新增组件回归测试）被 tsconfig.vitest.json 覆盖并纳入 type-check；vitest 可靠性 lint 规则中 oxlint 未覆盖项（如 no-identical-title）对测试文件生效（WR-02/WR-03 闭合）"
    - "scripts/check-chunks.mjs 的工具路由清单从 src/tools/index.ts 注册表模块派生（jiti 加载 tools[].path），脚本零硬编码路由——ARCH-01『路由只能从注册表派生』不变量在构建脚本侧恢复（WR-04 闭合）"
  artifacts:
    - path: src/tools/timestamp-converter/timestamp-converter.service.ts
      provides: isValidTimeZone Intl 校验 + fromTimestamp/toTimestamp 非法时区结构化错误分支 + detectUnit 单一 trim 变量
    - path: src/tools/timestamp-converter/timestamp-converter.service.test.ts
      provides: 新增 ≥5 条用例（非法时区正/反向 ×3 + 带空白识别 ×2），13→18 用例
    - path: src/tools/timestamp-converter/timestamp-converter.component.test.ts
      provides: 新增组件级渲染期回归测试 3 条（手填非法时区 → 错误卡而非崩溃）
    - path: tsconfig.vitest.json
      provides: include 覆盖 src/**/*.test.ts + 恢复继承的 DOM lib
    - path: eslint.config.ts
      provides: vitest 规则块 files 含 src/**/*.test.ts
    - path: scripts/check-chunks.mjs
      provides: TOOL_ROUTES 由注册表派生（jiti），硬编码清单删除
  key_links:
    - "timestamp-converter.vue computed（L244-246/L292-296）→ fromTimestamp/toTimestamp → isValidTimeZone 守卫 → ok:false → 错误卡 v-if（L50-56/L143-149）——渲染期 RangeError 路径彻底消除"
    - "scripts/check-chunks.mjs 路由清单 ← jiti.import('../src/tools/index.ts') → tools[].path——单一事实来源贯穿到构建脚本侧"
    - "tsconfig.vitest.json include → src/**/*.test.ts（执行时以 find src 实际数量为准）→ vue-tsc --build 全量覆盖"

---

<objective>
闭合 Phase 1 验证报告的全部 2 个结构化缺口（CR-01 阻断级白屏、WR-01 detectUnit 错判）与 3 个同域评审警告（WR-02/WR-03 测试基建路径错位、WR-04 分包卡口硬编码路由），恢复 TOOL-02 的 transparency 禁令、V7 服务契约与 ARCH-01/ARCH-02 的注册表单一事实来源不变量。

Purpose: CR-01 是阶段目标「用户可立即使用首批 2 个工具完成真实操作」的唯一未达项——UI 显式引导的手填时区输入 UTC+8 即整页白屏（node 实证三条路径必抛 RangeError）；WR-01/WR-04 是 Phase 5/8 批量工具复用前的公共 API 与构建卡口地雷。
Output: 6 个文件修改/新建（1 个 service + 2 个测试文件 + 2 个配置 + 1 个构建脚本），全部既有 78 用例回归通过，全部 9 个测试文件纳入 type-check，check-chunks 从注册表派生路由。
</objective>

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-app-skeleton-tool-registry/01-VERIFICATION.md
@.planning/phases/01-app-skeleton-tool-registry/01-REVIEW.md
@.planning/phases/01-app-skeleton-tool-registry/01-CONTEXT.md
@.planning/phases/01-app-skeleton-tool-registry/01-RESEARCH.md
@.planning/phases/01-app-skeleton-tool-registry/01-02-SUMMARY.md
@.planning/phases/01-app-skeleton-tool-registry/01-04-SUMMARY.md

@src/tools/timestamp-converter/timestamp-converter.service.ts
@src/tools/timestamp-converter/timestamp-converter.service.test.ts
@src/tools/timestamp-converter/timestamp-converter.vue
@src/components/ToolCard.test.ts
</context>

## 多源覆盖审计（Gap Closure 契约全覆盖，无静默丢弃）

| 来源 | 项目 | 去向 |
|------|------|------|
| VERIFICATION Gap-1（BLOCKER, TOOL-02） | service 层 tz 校验 + 2 条非法时区测试 + UI 渲染结构化错误而非白屏 | Task 1 |
| VERIFICATION Gap-2（PARTIAL, = 评审 WR-01, TOOL-02） | detectUnit trim 收窄 + 2 条带空白回归用例 | Task 1 |
| 评审 WR-02 | tsconfig.vitest.json include 与 `*.test.ts` 布局错位 | Task 2 |
| 评审 WR-03 | ESLint vitest 插件 files 与 `*.test.ts` 布局错位 | Task 2 |
| 评审 WR-04 | check-chunks.mjs 硬编码工具路由 | Task 3 |
| 评审 IN-01..IN-05 | info 级观察（注册表注释过时/createdAt 契约/README/vitest 导入扩展名/import 正则） | **明确排除**：info 级评审观察，非验证缺口，不在 gap_closure 契约范围；WR-04 与 IN-05 同文件不同缺陷（IN-05 是 `../` 引用正则放宽，属静默失效方向预警，保留现状不扩 scope） |

注：WR-01 同时出现在验证缺口（Gap-2）与评审警告清单中，按同一缺陷计一次，在 Task 1 闭合。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 时间戳 service 非法时区校验（CR-01）+ detectUnit trim（WR-01）+ 渲染期组件回归测试</name>
  <files>src/tools/timestamp-converter/timestamp-converter.service.ts, src/tools/timestamp-converter/timestamp-converter.service.test.ts, src/tools/timestamp-converter/timestamp-converter.component.test.ts</files>
  <read_first>
    - .planning/phases/01-app-skeleton-tool-registry/01-REVIEW.md — CR-01 Fix 代码（isValidTimeZone 实现与 fromTimestamp 错误分支形态）与 WR-01 Fix 代码（detectUnit 单变量收窄），按其逐字落实（用户认可的修复方案，勿另行发明）
    - src/tools/timestamp-converter/timestamp-converter.service.ts — 被改文件：FromTimestampResult/ToTimestampResult 宽接口形态（ok/error + 各字段 | null）、detectUnit L50-56、fromTimestamp L62-89、toTimestamp L92-103、既有错误文案风格（「未知时区」类全角标点 + 「」引用）
    - src/tools/timestamp-converter/timestamp-converter.service.test.ts — 既有 13 用例形态：dayjs 自对照、不硬编码本机时区、describe 分组
    - src/tools/timestamp-converter/timestamp-converter.vue — 零修改，仅确认消费面：错误卡 v-if 分支（L50-56 正向 / L143-149 反向，role="alert" 渲染 result.error）、四个 aria-label（「Unix 时间戳输入」「目标时区（换算视图，可选）」「自定义目标 IANA 时区名」「日期时间输入」「解析时区（默认浏览器本地时区）」「自定义解析 IANA 时区名」）、__custom__ 选项 value
    - src/components/ToolCard.test.ts — 组件测试既有模式：@vue/test-utils mount + happy-dom + 显式 vitest 导入
  </read_first>
  <behavior>
    service 测试新增（TDD RED 先行，全部放入既有 describe 结构或新 describe「非法时区结构化错误（CR-01）/带空白识别（WR-01）」）：
    - fromTimestamp('0', 'UTC+8') → ok === false；error 包含「未知时区」与「Asia/Shanghai」；detectedUnit/local/utc/offset/timeZone/target 全部 === null；不抛异常
    - fromTimestamp('1735689600000', 'Foo/Bar') → ok === false（毫秒输入 + 反例时区）
    - toTimestamp('2025-01-01 08:00:00', 'Foo/Bar') → ok === false；error 包含「未知时区」；ms/sec === null；不抛异常
    - detectUnit(' 1735689600 ') === 's'（10 位秒级不得因空白错判为 ms）
    - detectUnit('\t1735689600000\n') === 'ms'
    组件回归测试（新文件 timestamp-converter.component.test.ts，3 条）：
    - 手填非法时区正向：mount → find('select[aria-label="目标时区（换算视图，可选）"]').setValue('__custom__') → find('input[aria-label="自定义目标 IANA 时区名"]').setValue('UTC+8') → find('input[aria-label="Unix 时间戳输入"]').setValue('1735689600000') → await nextTick() → find('[role="alert"]') 存在且 text 含「未知时区」；wrapper.text() 不含「识别为」（结果卡不渲染）；组件未崩溃
    - 手填非法时区反向：find('select[aria-label="解析时区（默认浏览器本地时区）"]').setValue('__custom__') → find('input[aria-label="自定义解析 IANA 时区名"]').setValue('Foo/Bar') → find('input[aria-label="日期时间输入"]').setValue('2025-01-01 08:00:00') → await nextTick() → [role="alert"] 存在且含「未知时区」；text 不含「毫秒时间戳」（结果区不渲染）
    - 有效路径健全性：仅 setValue tsInput '1735689600000' → text 含「识别为毫秒」（确认修复未破坏正常回显）
  </behavior>
  <action>
    严格 TDD 三步（RED → GREEN → 组件回归），每步一个原子提交：

    1. RED：先在 timestamp-converter.service.test.ts 追加上述 5 条 service 用例（组件测试文件此时不建）。运行 `corepack pnpm vitest run src/tools/timestamp-converter`，确认失败形态正确：3 条非法时区用例以 RangeError（"Invalid time zone specified: …"）抛出失败，2 条空白用例以断言失败（当前返回 'ms'/'invalid'）。提交 test(01-05)。
    2. GREEN：按 01-REVIEW.md CR-01 Fix 代码逐字实现三处修改（per Gap-1 missing 项 1 与 Gap-2 missing 项 1）：
       - 新增模块级私有函数 isValidTimeZone(tz: string): boolean——try 内 `new Intl.DateTimeFormat('en-US', { timeZone: tz })` 返回 true，catch RangeError 返回 false。不导出；Intl 调用只在函数体内（Pitfall 3 纪律，vite-ssg 预渲染 Node 环境安全）。带 JSDoc 注明「非法 IANA 名抛 RangeError → 收敛为布尔」。
       - fromTimestamp：在 detectUnit invalid 分支之后、构造 dayjs 对象之前，插入 tz 守卫——`tz !== undefined && !isValidTimeZone(tz)` 时返回与现有形态一致的宽接口结构化错误：ok:false、error 为 `未知时区：「${tz}」，请输入 IANA 时区名（如 Asia/Shanghai）。`（沿用现有全角标点文案风格）、detectedUnit/local/utc/offset/timeZone/target 全部 null。校验顺序遵循 REVIEW Fix 代码：先单位后时区。
       - toTimestamp：在 dayjs.tz 调用之前插入同样守卫，返回 ok:false、同款 error、ms:null、sec:null。绝不让非法 tz 到达 dayjs.tz。
       - detectUnit（per WR-01 Fix 代码）：收窄为单一 trim 后变量——`const s = ts.trim()`；合法性正则与位数判定（`s.replace('-', '').length`）都基于 s。≤11 位 's'、≤14 位 'ms'、其余 'invalid' 逻辑不变。
       运行 service 测试全绿后提交 feat(01-05)。
    3. 组件回归测试：新建 timestamp-converter.component.test.ts（per Gap-1 missing 项 3 的自动化代理——UI 手填非法时区显示 role=alert 错误卡而非崩溃；浏览器端人工走查仍由 VERIFICATION.md human_verification 第 2 项在阶段末收口）。按 behavior 块三条用例实现，选择器一律用 aria-label 属性选择器（不依赖 DOM 顺序/索引，规避 noUncheckedIndexedAccess 断言）；不传 props、无需 router 上下文（该组件不使用 RouterLink）；useCopy legacy 语义下 setup 阶段无需剪贴板 mock（01-02 SUMMARY 已锁定 isSupported 恒 true）。运行通过后提交 test(01-05)。

    禁止事项：不得改 timestamp-converter.vue（错误卡 UI 已就绪，01-VERIFICATION.md 确认）；不得把 isValidTimeZone 的调用下沉到 UI 层（服务层校验是 V7 契约位置，UI 只消费 ok:false）；不得改动 detectUnit 的位数阈值；error 文案不得内插超长原始输入（V7 限流纪律，仅内插 tz 本身）。
  </action>
  <acceptance_criteria>
    - `corepack pnpm vitest run src/tools/timestamp-converter` 全绿（既有 13 用例 + 新增 8 条）
    - timestamp-converter.service.test.ts 存在含字面量 UTC+8 与 Foo/Bar 的用例且断言 ok 为 false
    - timestamp-converter.component.test.ts 存在且 3 条用例全绿，含 role="alert" 与「未知时区」断言
    - `grep -c "isValidTimeZone" src/tools/timestamp-converter/timestamp-converter.service.ts` ≥ 3（定义 + 两处调用）
    - service 源码中 Intl.DateTimeFormat 仅出现在 isValidTimeZone 函数体内（无模块顶层调用）
    - detectUnit 函数体内只出现一个 trim 调用且位数判定基于 trim 后变量（ts.replace 形态不复存在）
    - `git diff` 不含 timestamp-converter.vue
  </acceptance_criteria>
  <verify>
    <automated>CI=true corepack pnpm vitest run src/tools/timestamp-converter</automated>
    <fails_when>退出码非 0，或输出出现 FAIL/RangeError 字样（3 条非法时区用例若抛 RangeError 即实现未收敛；组件测试失败即错误卡未渲染或渲染期崩溃）</fails_when>
    <automated>CI=true corepack pnpm vitest run</automated>
    <fails_when>退出码非 0，或任何既有基线用例失败（detectUnit trim 不得破坏既有识别边界用例；全套 9 文件必须全绿）</fails_when>
  </verify>
  <done>
    手填 UTC+8 与 Foo/Bar 在正反向均为结构化错误卡路径（组件测试实证渲染期不抛异常）；detectUnit 带空白输入识别正确；18+3 用例全绿，78 基线零回归。
  </done>
</task>

<task type="auto">
  <name>Task 2: 测试基建路径错位修复（WR-02 tsconfig.vitest.json + WR-03 eslint vitest files）</name>
  <files>tsconfig.vitest.json, eslint.config.ts</files>
  <read_first>
    - .planning/phases/01-app-skeleton-tool-registry/01-REVIEW.md — WR-02 Fix（include 三模式）与 WR-03 Fix（files 两模式）
    - tsconfig.vitest.json — 被改文件：当前 include 仅 src/**/__tests__/*（目录约定），且 "lib": [] 覆盖行会清空继承的 DOM lib
    - tsconfig.app.json — 只读参照：include/exclude 形态；本任务不修改它（app project 已实际覆盖全部测试文件，已实证 tsc -p tsconfig.app.json --listFilesOnly 命中 8 个 .test.ts；动它徒增回归面）
    - eslint.config.ts — 被改文件：vitest 规则块（L42-45）files 仅 __tests__/*；注意其后置的 pluginOxlint.buildFromOxlintConfigFile 块会把 oxlint 已覆盖的 17 条 vitest 规则置 off（设计如此——那 17 条由 lint:oxlint 的 oxlint 原生强制，探针实证 vitest/valid-title、vitest/expect-expect 在 oxlint 下对测试文件真实报错），本任务激活的是 oxlint 未覆盖项（vitest/no-identical-title、no-commented-out-tests、no-import-node-test、no-interpolation-in-snapshots、no-mocks-import、no-unneeded-async-expect-function、prefer-called-exactly-once-with）
    - .oxlintrc.json — 只读参照：plugins 含 vitest（理解上述委托关系）
  </read_first>
  <action>
    1. WR-02（per 评审 Fix）：tsconfig.vitest.json 的 include 改为三个模式——src/**/*.test.ts（实际就近布局）+ src/**/__tests__/*（保留脚手架目录约定）+ env.d.ts；exclude 保持 []。同时删除 compilerOptions 中的 "lib": [] 覆盖行（关键：include 扩大后测试文件将落入该 project，lib:[] 无 DOM lib 会让使用 document/navigator 的测试产生虚假类型错误；删除后继承 @vue/tsconfig/tsconfig.dom.json 的 ESNext+DOM+DOM.Iterable——与测试当前在 app project 下通过的 lib 完全一致）。保留 "types": ["node"] 与 tsBuildInfoFile。同步更新文件内注释说明就近 *.test.ts 布局。
    2. WR-03（per 评审 Fix）：eslint.config.ts vitest 规则块的 files 改为 ['src/**/__tests__/*', 'src/**/*.test.ts']，并在块前补一条注释说明「后置 oxlint 兼容块将 oxlint 已覆盖的重叠规则委托给 lint:oxlint，此处激活 oxlint 未覆盖项」。
    3. 运行 `corepack pnpm type-check`：若测试文件暴露类型错误（评审预期点位：ToolCard.test.ts 的 component 断言处、registry.test.ts 的 as never 处——注意 lib 恢复继承后多数预测错误不会出现），修复测试代码本身的最小类型问题，禁止通过放宽/回退配置消错。
    4. 运行 `corepack pnpm lint`：若新激活规则对既有测试报违规，修测试代码（如重名 it 标题）；lint 双段（oxlint + eslint）都必须 0 错。
    5. 失败方向探针（证明修复真实生效而非配置摆设）：临时创建 src/__wr03_probe__.test.ts（内容两条同名 it('dup')），eslint 直跑该文件必须 exit 1 且输出含 no-identical-title，随后立即删除探针文件。
  </action>
  <acceptance_criteria>
    - `corepack pnpm exec tsc -p tsconfig.vitest.json --listFilesOnly 2>/dev/null | grep -c "\.test\.ts"` 的值等于 `find src -name "*.test.ts" | wc -l | tr -d " "`（执行时点应为 9，含 Task 1 新增组件测试；动态相等断言而非写死数字）
    - `corepack pnpm type-check` exit 0
    - `corepack pnpm lint` exit 0
    - tsconfig.vitest.json 中不再存在 `"lib": []` 行（`grep -c '"lib": \[\]' tsconfig.vitest.json` 为 0）
    - eslint.config.ts 的 vitest 块 files 含 src/**/*.test.ts 字面量
    - 探针验证后仓库内无 __wr03_probe__ 残留（find src -name "__wr03_probe__.test.ts" 为空）
    - `git diff --stat tsconfig.app.json` 为空
  </acceptance_criteria>
  <verify>
    <automated>test "$(corepack pnpm exec tsc -p tsconfig.vitest.json --listFilesOnly 2>/dev/null | grep -c "\.test\.ts")" -eq "$(find src -name "*.test.ts" | wc -l | tr -d " ")"</automated>
    <fails_when>两个计数不相等（vitest project 仍有测试文件游离于类型检查之外，退出码 1）</fails_when>
    <automated>corepack pnpm type-check && corepack pnpm lint</automated>
    <fails_when>任一段退出码非 0（类型错误或 lint 违规，输出含 error TS / error 字样）</fails_when>
    <automated>printf 'import { describe, it } from "vitest"\ndescribe("p", () => {\n  it("dup", () => {})\n  it("dup", () => {})\n})\n' > src/__wr03_probe__.test.ts; corepack pnpm exec eslint src/__wr03_probe__.test.ts > /tmp/wr03-probe.log 2>&1; code=$?; rm -f src/__wr03_probe__.test.ts; grep -q "no-identical-title" /tmp/wr03-probe.log && test "$code" -eq 1</automated>
    <fails_when>探针 eslint exit 0（规则未生效——WR-03 修复为无效摆设）或输出不含 no-identical-title；该探针命令本身在探针删除后以最终 test 的退出码收口</fails_when>
  </verify>
  <done>
    全部测试文件（含新增）被 vitest project 覆盖且 type-check 通过；vitest 可靠性规则对 *.test.ts 真实生效（探针实证 eslint 拦截 no-identical-title 违规，oxlint 委托关系不变）；lint 双段 0 错。
  </done>
</task>

<task type="auto">
  <name>Task 3: check-chunks.mjs 工具路由改由注册表派生（WR-04）</name>
  <files>scripts/check-chunks.mjs</files>
  <read_first>
    - .planning/phases/01-app-skeleton-tool-registry/01-REVIEW.md — WR-04 Issue 与 Fix（jiti 加载 src/tools/index.ts 属其认可的更严格形态；gap 契约指定「读取构建产物注册表输出或导入注册表模块」）
    - scripts/check-chunks.mjs — 被改文件：L27-28 硬编码路由清单；其余断言（BFS 可达闭包 a+b、懒加载 chunk 存在性 c、预渲染页存在性 d）全部保留不动——本任务只替换清单来源
    - src/tools/index.ts — 派生源：导出 tools 数组，元素含 path（'/json-formatter' 形态）；顶层仅 defineTool/图标引用（Pitfall 3 纪律保证 Node 加载安全）
    - .planning/phases/01-app-skeleton-tool-registry/01-04-SUMMARY.md — check-chunks 既有语义决策（首包 = 入口页可达闭包、CM 断言按页分组 slug 匹配），替换清单来源时不得改变这些语义
  </read_first>
  <action>
    1. 在 check-chunks.mjs 顶部新增 `import { createJiti } from 'jiti'`（jiti ^2.7.0 为既有 devDependency，本计划零新增 npm 安装——供应链闸门不触发），将硬编码的路由清单常量（L27-28 的数组字面量及其注释）替换为：
       - `const jiti = createJiti(import.meta.url)`（以脚本自身 URL 为解析基准）
       - `const registry = await jiti.import('../src/tools/index.ts')`（相对 scripts/ 指向注册表模块；jiti 负责 TS 转译与无扩展名相对导入解析——已在本仓库实证：加载后 tools[].path 输出 /timestamp-converter 与 /json-formatter）
       - `const TOOL_ROUTES = registry.tools.map((t) => t.path.replace(/^\//, ''))`（'/json-formatter' → 'json-formatter'，直接喂给断言 d 的平铺/目录双形态检查；顶层 await 在 ESM .mjs 中合法）
       变量名 TOOL_ROUTES 保留（下游断言 d 引用不动）。加注释注明：清单唯一来源 = 注册表模块；注册表模块顶层必须保持 Node 安全（Pitfall 3 纪律——与 vite-ssg build 同等信任面，违纪会在本脚本 fail-fast）。
    2. 更新头部注释块：原「零第三方依赖（仅 node: 内置）」声明修正为「构建期脚本，除 node: 内置外仅使用 devDependency jiti 加载注册表」；补一行说明路由清单改由注册表派生（per WR-04，消除『与 src/tools 注册表一致』的人肉同步）。
    3. 断言 a/b/c/d 的函数体、正则、violations 收集与退出码语义零改动（IN-05 的 `../` 相对引用正则属 info 级、明确排除在本计划外，勿顺手改）。
    4. 失败方向探针（证明派生真实驱动断言 d）：构建成功后临时将 dist/timestamp-converter.html 改名为 .bak 跑脚本——必须 exit 1 且 VIOLATION 行提及该路由（说明清单确来自注册表而非恒空/恒真）；恢复文件名后复跑必须 exit 0。
  </action>
  <acceptance_criteria>
    - `! grep -qE "TOOL_ROUTES = \[" scripts/check-chunks.mjs` 成立（硬编码数组字面量已消失；头部注释不含该形态）
    - scripts/check-chunks.mjs 含 createJiti 与 jiti.import('../src/tools/index.ts') 调用
    - `corepack pnpm build && corepack pnpm check:chunks` → 输出 "OK: chunk budget pass" 且 exit 0
    - 派生对齐：`node --input-type=module -e "import {createJiti} from 'jiti'; const m = await createJiti(import.meta.url).import('./src/tools/index.ts'); console.log(m.tools.map(t=>t.path).join(','))"` 输出含 /timestamp-converter 与 /json-formatter 两条
    - 失败方向探针：dist/timestamp-converter.html 被临时移走时脚本 exit 1（VIOLATION 提及 timestamp-converter），恢复后 exit 0；探针不留 dist 残留
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm build && corepack pnpm check:chunks</automated>
    <fails_when>build 或脚本任一退出码非 0，或未输出 "OK: chunk budget pass"（jiti 加载注册表失败 / 断言回归破坏）</fails_when>
    <automated>! grep -qE "TOOL_ROUTES = \[" scripts/check-chunks.mjs</automated>
    <fails_when>grep 命中硬编码数组字面量（退出码非 0 的反面——清单仍是手写双源）</fails_when>
    <automated>mv dist/timestamp-converter.html dist/timestamp-converter.html.bak; node scripts/check-chunks.mjs > /tmp/cc-probe.log 2>&1; code=$?; mv dist/timestamp-converter.html.bak dist/timestamp-converter.html; grep -q "timestamp-converter" /tmp/cc-probe.log && test "$code" -eq 1 && corepack pnpm check:chunks</automated>
    <fails_when>移走预渲染页后脚本仍 exit 0（派生清单未驱动断言 d，恒真摆设）或 VIOLATION 行未提及该路由；最终以恢复后复跑 check:chunks 的退出码收口</fails_when>
  </verify>
  <done>
    构建脚本侧恢复「路由只能从注册表派生」不变量：check-chunks 的工具路由清单唯一来源为 src/tools/index.ts（jiti 实载），硬编码清单消失；全部既有断言语义保留且经构建 + 失败方向探针双重实证。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → service | 手填 IANA 时区自由文本（UI 显式引导）跨入服务层 dayjs 调用——CR-01 缺陷边界，本计划以服务层校验收敛 |
| 构建脚本 → 源码模块 | check-chunks（Node）经 jiti 加载 src/tools/index.ts——本地可信代码，与 vite-ssg build 同等信任面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01G-01 | Tampering / Input Validation（V5/V7） | timestamp-converter.service.ts fromTimestamp/toTimestamp | critical | mitigate | Task 1：isValidTimeZone Intl 校验 + 结构化 ok:false 返回 + 5 条 service 测试 + 3 条组件渲染期回归测试（白屏路径消除即本计划核心） |
| T-01G-02 | Tampering（供应链） | 无新增 npm 安装（jiti 为既有 devDependency ^2.7.0，Phase 1 已入 lockfile） | low | accept | 零新增依赖面，Package Legitimacy 闸门不触发；无需 checkpoint |
| T-01G-03 | Denial of Service（构建期） | check-chunks.mjs jiti 加载注册表模块链 | low | accept | 仅加载本地可信 TS 模块（defineTool + 图标引用，顶层零浏览器 API——Pitfall 3 纪律既有）；违纪即 fail-fast，属期望行为 |
| T-01G-04 | Information Disclosure（错误文案） | timestamp service 错误消息 | low | mitigate | Task 1 文案仅内插 tz 参数本身（不做长度截断以保留可读性，tz 为短自由文本），复用 01-02 既有 V7 限流口径；不回显时间戳原文于时区错误分支 |
</threat_model>

<verification>
计划级全量门（Task 3 完成后复跑一次收口）：

1. `CI=true corepack pnpm vitest run` — 9 个测试文件全绿（基线 78 用例 + 新增 ≥8 条），0 unhandled error
2. `corepack pnpm type-check` — exit 0，且 vitest project 覆盖数 === src 实际测试文件数
3. `corepack pnpm lint` — oxlint 0 + eslint 0
4. `corepack pnpm build && corepack pnpm check:chunks` — exit 0，"OK: chunk budget pass"（注册表派生路由驱动断言 d）
5. WR-03 探针与 WR-04 失败方向探针均通过后无残留文件（src 无 __wr03_probe__，dist 文件名完整）

人工走查（不新增 checkpoint——human_verify_mode=end-of-phase，已登记于 01-VERIFICATION.md human_verification 第 2 项，阶段末 verify-work 收口）：/timestamp-converter 正反向手填 UTC+8 与拼错地名 → 浏览器显示 role=alert 错误卡、页面其余部分可用、绝不白屏。
</verification>

<success_criteria>
- [ ] CR-01 闭合：非法时区输入在 service 层被 Intl 校验收敛为 ok:false 结构化错误；渲染期 RangeError 路径消除（组件测试 + 人工走查双证）
- [ ] WR-01 闭合：detectUnit 空白输入识别正确（' 1735689600 '→'s'），1000 倍错判防线恢复
- [ ] WR-02 闭合：全部 *.test.ts 纳入 tsconfig.vitest.json 并通过 type-check（app project 配置零改动）
- [ ] WR-03 闭合：vitest 可靠性规则对测试文件生效（oxlint 未覆盖项由 eslint 激活，重叠项维持 oxlint 委托，双段 lint 0 错）
- [ ] WR-04 闭合：check-chunks 工具路由清单唯一来源为注册表模块，硬编码消失，既有断言语义零回归
- [ ] 全量门 1-5 全绿；6 个文件改动全部原子提交；未触碰 timestamp-converter.vue 与 tsconfig.app.json
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Change | Consumed by |
|----------|------|--------|-------------|
| src/tools/timestamp-converter/timestamp-converter.service.ts | modified | +isValidTimeZone 守卫、两函数错误分支、detectUnit 单变量 trim | timestamp-converter.vue（零改动消费）、Phase 5/8 复用的全站公共 API |
| src/tools/timestamp-converter/timestamp-converter.service.test.ts | modified | 13→18 用例（非法时区 ×3 + 带空白识别 ×2） | 回归网（测试盲区补齐） |
| src/tools/timestamp-converter/timestamp-converter.component.test.ts | new | 3 条渲染期回归（错误卡而非崩溃 + 有效路径健全性） | CR-01 自动化代理，防渲染期异常类回归 |
| tsconfig.vitest.json | modified | include 覆盖 src/**/*.test.ts + 删除 lib:[] 覆盖 | `corepack pnpm type-check`（vue-tsc --build） |
| eslint.config.ts | modified | vitest 块 files + src/**/*.test.ts | `corepack pnpm lint` |
| scripts/check-chunks.mjs | modified | TOOL_ROUTES ← jiti 注册表派生，硬编码删除 | `corepack pnpm check:chunks`（ARCH-02 卡口，Phase 3 ARCH-05 前置形态） |

<output>
Create `.planning/phases/01-app-skeleton-tool-registry/01-05-SUMMARY.md` when done
</output>
