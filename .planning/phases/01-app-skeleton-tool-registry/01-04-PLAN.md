---
phase: 01-app-skeleton-tool-registry
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/tools/index.ts
  - src/tools/json-formatter/index.ts
  - src/tools/json-formatter/json-formatter.vue
  - src/tools/json-formatter/json-formatter.service.ts
  - src/tools/json-formatter/json-formatter.service.test.ts
  - src/tools/json-formatter/components/CodeMirrorJson.vue
  - src/tools/json-formatter/components/JsonTree.vue
  - scripts/check-chunks.mjs
autonomous: true
requirements: [TOOL-01, ARCH-02]
estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "用户在 /json-formatter 完成 JSON 格式化/压缩/校验：≥2^53 长数字 ID（如 9052710354240385291，电商订单/雪花 ID 场景）在格式化输出与树视图中逐字保留原文文本，全程不经 JS number 中转（TOOL-01 核心正确性）"
    - "对象键序与重复键保持原文出现顺序（不重排、不合并）；非法 JSON 报错并给行/列定位（line/column）"
    - "输出视图为左右双栏：格式化文本 + 可折叠树形视图共享同一份解析结果；树默认展开深度 2、长数组显示计数徽标、≥2^53 数字节点显示精度提示（D-02）"
    - "输入区为 CodeMirror 6（行号、语法高亮、jsonParseLinter 错误定位），仅被本工具 chunk 静态引用，共享层零引用（D-01）"
    - "构建产物卡口：每个工具路由独立异步 chunk，首包（各预渲染页 html 引用的 script）不含 codemirror 字样，check-chunks 卡口通过（ARCH-02）"
    - "格式化输出可一键复制（复用 useCopy）；含脚本标签/事件属性的 JSON 字符串值经 vnode 插值渲染为纯文本"
  artifacts:
    - path: "src/tools/json-formatter/json-formatter.service.ts"
      provides: "formatJson / minifyJson / validateJson / buildTree（纯文本 tokenizer，数字逐字保留）"
    - path: "src/tools/json-formatter/json-formatter.service.test.ts"
      provides: "2^53 保真、往返属性、键序保持、错误行列定位、BOM/emoji/转义边界"
    - path: "src/tools/json-formatter/components/CodeMirrorJson.vue"
      provides: "CM6 编辑器封装（onMounted 创建 / onBeforeUnmount 销毁 / watch 单向同步）"
    - path: "src/tools/json-formatter/components/JsonTree.vue"
      provides: "自研递归可折叠树（raw 字符串数字节点、展开深度 2、计数徽标、精度提示）"
    - path: "src/tools/json-formatter/json-formatter.vue"
      provides: "双栏视图（编辑器 | 格式化文本+树），一份解析喂两个视图"
    - path: "scripts/check-chunks.mjs"
      provides: "构建产物分包卡口（首包无 CM + 工具独立 chunk + 预渲染页存在性）"
  key_links:
    - from: "src/tools/json-formatter/json-formatter.vue"
      to: "src/tools/json-formatter/json-formatter.service.ts"
      via: "computed 一次解析，格式化文本与 JsonTree 共享同一结果"
      pattern: "buildTree|formatJson"
    - from: "src/tools/json-formatter/components/CodeMirrorJson.vue"
      to: "codemirror 包"
      via: "仅工具组件静态 import（懒加载 chunk 边界），共享层禁止引用"
      pattern: "from 'codemirror'"
    - from: "scripts/check-chunks.mjs"
      to: "dist/"
      via: "读取预渲染 html 引用的首包 chunk，断言无 CM 泄漏且工具异步 chunk 存在"
      pattern: "check-chunks"
  flagged_assumptions:
    - "ARCH-02 探针未决（unclassified，已手动复核）→ 已登记假设：Phase 1 分包卡口仅断言『每工具独立 chunk + 首包不含 codemirror 字样 + 预渲染页存在』；正式 gzip ≤ 200KB 体积预算 CI 卡口归 Phase 3 ARCH-05 交付（RESEARCH Open Questions #3 的 Phase 1 轻量形态）（探针 6 行中第 3 行，无静默丢弃）"
    - "TOOL-01 探针未决（unclassified，已手动复核）→ 已登记假设：JSON 语义边界按以下裁定——重复键保持原文顺序不合并；数字保真仅保证文本层显示与往返（不提供 BigInt 运算）；输入首字符 U+FEFF BOM 被忽略、其余位置按 ECMA-404 非法字符报错；\\uXXXX 等八种转义按 ECMA-404 文法；错误行列号按 JS 字符串索引（UTF-16 code units）计算（探针 6 行中第 5 行，无静默丢弃）"
  prohibitions:
    - statement: "JSON 工具不得静默修正非法输入（不得自动补引号/去尾逗号等『容错修复』），不得重排对象键序，不得以任何形式改变数字字面量的文本表示（包括经 JS number 中转）——数据保真是本工具的存在理由，静默改写即产品失信"
      category: transparency
---

<objective>
TOOL-01 完整交付 + ARCH-02 闭环卡口：纯文本 tokenizer 驱动的 JSON 格式化工具（格式化/压缩/校验/树形视图，长数字 ID 零精度丢失），CodeMirror 6 输入区随工具 chunk 懒加载，scripts/check-chunks.mjs 把"重依赖不进首屏"变成可执行断言。

Purpose: 成功标准 #1（JSON 工具 + 长 ID 精度）与 #3 的分包部分在此交付；tokenizer 的"数字逐字保留"是 json-bigint/lossless-json 对比后定夺的唯一无损路径（RESEARCH §Pattern 6），也是后续工具 service 层的 analog。
Output: json-formatter 目录（5 文件）+ 注册表汇入 + check-chunks 卡口（10 文件）。
</objective>

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-app-skeleton-tool-registry/01-CONTEXT.md
@.planning/phases/01-app-skeleton-tool-registry/01-RESEARCH.md
@.planning/phases/01-app-skeleton-tool-registry/01-VALIDATION.md
@.planning/phases/01-app-skeleton-tool-registry/01-01-SUMMARY.md
@.planning/phases/01-app-skeleton-tool-registry/01-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: JSON tokenizer 服务切片（保真格式化/压缩/校验/树形 四合一）</name>
  <files>src/tools/json-formatter/json-formatter.service.ts, src/tools/json-formatter/json-formatter.service.test.ts</files>
  <read_first>
    - 01-RESEARCH.md §Pattern 6（tokenizer 必要性论证与四导出定义）、§Pitfall 5（Unicode/转义边界）、§Validation Architecture（TOOL-01 两行 Test Map）
    - 01-CONTEXT.md D-02（树形视图数据源约束）
    - 01-PATTERNS.md 关键纪律 4/5
  </read_first>
  <behavior>
    - Test 1（2^53 保真，TOOL-01 核心）: 输入 '{"orderId":9052710354240385291}' → formatJson 输出含原文子串 '9052710354240385291'；buildTree 根对象属性节点的数字节点满足 type 'number' 且 raw === '9052710354240385291'（字符串形态，非 number 类型）；同样断言负数 '-9007199254740993' 与指数 '1.5e300' 的原文保留
    - Test 2（往返属性）: 对合法 JSON 样本集（嵌套 ≥10 层对象、含 1000 元素长数组、重复键、emoji 键、"\u0041" 转义、空前对象/空数组）逐一断言：formatJson(text, 2) 的输出经"忽略空白后逐 token 序列相等"与原文规范化结果一致（即 tokenizer 层往返无损）
    - Test 3（键序与重复键）: 输入含重复键 '{"a":1,"a":2,"b":3}' → formatJson 与 buildTree 均按原文出现顺序保留两个 a（不合并、不排序）
    - Test 4（压缩）: minifyJson 移除全部结构空白但保留字符串值内部空白：'{"a": "x y", "b":[1, 2]}' 压缩后为 '{"a":"x y","b":[1,2]}'
    - Test 5（校验与定位）: validateJson('{"a":1,}') 返回 ok:false 且 error 含 line=1 与 column 指向尾逗号附近（列号 >0）；validateJson('"unterminated') 返回 ok:false 且 error.message 非空含行列；validateJson('  ') 对空/纯空白输入返回 ok:false 的结构化错误（空输入不崩溃——ARCH-01/empty 同源边界）；validateJson('\{"ok":true\}') 返回 ok:true
    - Test 6（BOM）: 输入首字符 U+FEFF 被忽略，其余内容正常解析；字符串内部出现 U+FEFF 不影响
  </behavior>
  <action>
    1. RED：先写 json-formatter.service.test.ts（上方六组用例），运行确认失败。
    2. GREEN：实现 json-formatter.service.ts——手写 JSON tokenizer（RESEARCH §Pattern 6）：四类 token（string / number / punctuation / literal），字符串仅认 ECMA-404 八种转义（双引号、反斜杠、斜杠、b、f、n、r、t、uXXXX 四位十六进制）；**数字 token 原文逐字拷贝为 raw 字符串，永不经 Number()/parseFloat() 转换**（禁止 JSON.parse+stringify 主路径的三重理由：2^53 静默舍入、数字型键重排、库依赖——已由 research 定夺，勿翻案）。导出四个纯函数：formatJson(text, indent=2)（按 token 重建缩进文本，数字与键名逐字拷贝）、minifyJson(text)、validateJson(text) 返回 { ok, error?: { line, column, message } }（tokenize 失败即报错并计算行列；另用 try/catch 包 JSON.parse 做交叉语法校验兜底——解析结果仅用于校验布尔，绝不进入输出）、buildTree(text) 返回递归 TreeNode（对象/数组/字符串/数字{raw}/布尔/null 节点，数字节点 type 'number' + raw 字符串）。行列号按 JS 字符串索引（UTF-16 code units）累计（换行符 +1 行归零列）。首字符 U+FEFF 忽略。
    3. 纪律：service 零 Vue/DOM 依赖（可在 Node/SSG 环境运行）；结构化错误 {ok,error}（V7）不裸抛；PATTERNS 关键纪律 4 全程生效。
    4. 运行测试至全绿；如往返属性测试失败，优先修 tokenizer 而非放宽断言。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/tools/json-formatter/json-formatter.service.test.ts</automated>
    <fails_when>vitest 非零退出或汇总行 "0 passed"——任一保真/往返/定位用例失败即失败</fails_when>
  </verify>
  <acceptance_criteria>
    - json-formatter.service.ts 导出四个函数：`formatJson`、`minifyJson`、`validateJson`、`buildTree`；buildTree 数字节点 raw 字段类型为 string
    - service 文件不出现对数字 token 的 Number()/parseFloat() 转换调用；主路径不调用 JSON.parse（仅 validateJson 内 try/catch 交叉校验允许一处）
    - json-formatter.service.test.ts 覆盖 behavior 六组用例（含 '9052710354240385291' 原文断言、重复键保序、尾逗号行列断言、BOM 用例）
    - `corepack pnpm vitest run src/tools/json-formatter/json-formatter.service.test.ts` 退出码 0 且通过数 > 0
    - service 文件无 vue/DOM 相关 import
  </acceptance_criteria>
  <done>tokenizer 六组用例全绿：2^53 原文保留、往返无损、键序保持、压缩正确、错误行列定位、BOM 容错。</done>
</task>

<task type="auto">
  <name>Task 2: 双栏视图 + CodeMirror 输入区 + 递归树 + 注册表接入（D-01/D-02）</name>
  <files>package.json, pnpm-lock.yaml, src/tools/json-formatter/components/CodeMirrorJson.vue, src/tools/json-formatter/components/JsonTree.vue, src/tools/json-formatter/json-formatter.vue, src/tools/json-formatter/index.ts, src/tools/index.ts</files>
  <reversibility rating="costly">D-01：CodeMirror 6 将成为 Phase 5 正则测试器与后续编辑器类工具的共享依赖——封装形态（CodeMirrorJson.vue）一旦被批量复制，更换编辑器意味着多工具返工。</reversibility>
  <read_first>
    - 01-RESEARCH.md §Pattern 3（CodeMirrorJson.vue 完整代码：onMounted/watch/onBeforeUnmount 与懒加载机制段）、§Pattern 4（树形视图关键设计）、§Pitfall 2（CM 混入首包）
    - 01-CONTEXT.md D-01 / D-02、`<specifics>`（电商嵌套数据场景）
    - 01-02-SUMMARY.md（注册表接入一行模式与 useCopy 用法）
  </read_first>
  <action>
    1. 安装编辑器依赖：`corepack pnpm add codemirror @codemirror/lang-json @codemirror/lint @codemirror/theme-one-dark`。这四个包只允许被 src/tools/json-formatter/ 内的文件静态 import——共享层（src/ui、src/composables、src/utils、src/pages、src/layouts）出现任何引用即违反 ARCH-02（Pitfall 2）。
    2. src/tools/json-formatter/components/CodeMirrorJson.vue 照 RESEARCH §Pattern 3 逐项：props modelValue、emit update:modelValue；onMounted 创建 EditorView（extensions：basicSetup、json()、linter(jsonParseLinter())、oneDark、EditorView.updateListener 在 docChanged 时 emit）；watch props.modelValue 单向同步（值不同才 dispatch，避免回环）；onBeforeUnmount 销毁 view 置 null（vite-ssg 预渲染纪律：无 DOM 即不创建）。
    3. src/tools/json-formatter/components/JsonTree.vue 照 §Pattern 4：props 接收 buildTree 产出的 TreeNode；对象/数组节点可折叠（点击头部切换），默认展开深度 ≤2（更深默认收起）；数组头部渲染计数徽标（如 Array(50) 形式）；数字节点当 raw 绝对值 ≥ 2^53 时渲染"JS 精度不安全"小标记（提示该值在 JS 侧为文本保真显示）；递归渲染自身；全部节点经 Vue 文本插值/属性绑定渲染（vnode 路径，不构造 HTML 字符串）；样式消费 @theme 语义类。
    4. src/tools/json-formatter/json-formatter.vue：左右双栏布局（D-02）——左栏 CodeMirrorJson 输入；右栏同一份 computed 解析结果驱动的两个视图（格式化文本块 + JsonTree，上下并列或 Tab 切换均可，但数据源必须是同一次 service 调用）；解析失败时右栏渲染错误卡（line/column/message，来自 validateJson 结构化错误）；格式化/压缩/复制按钮组（复制接 useCopy，来自 01-02 的共享 composable）；输入含脚本标签或事件属性文本时，一切输出仅以纯文本形态出现（vnode 插值，无 HTML 字符串路径）。样式消费 @theme 语义类，克制易读。
    5. src/tools/json-formatter/index.ts 照 01-02 样板：defineTool({ name: 'JSON 格式化', path: '/json-formatter', description: 'JSON 校验、格式化与压缩，浏览器本地完成，长数字 ID 不丢精度', keywords: ['json', 'format', '格式化', '校验', '美化', '压缩'], category: 'developer', icon: Braces, component: () => import('./json-formatter.vue'), createdAt: '2026-09-04' })；Braces 从 @lucide/vue 导入。
    6. src/tools/index.ts 汇入一行（import jsonFormatter + tools 数组加入）——不触碰 routes.ts。
  </action>
  <verify>
    <automated>corepack pnpm vitest run && corepack pnpm type-check</automated>
    <fails_when>全套 vitest 任一用例失败（含 01-01/01-02 回归）；或 vue-tsc 报任何类型错误</fails_when>
  </verify>
  <acceptance_criteria>
    - package.json dependencies 含 codemirror、@codemirror/lang-json、@codemirror/lint、@codemirror/theme-one-dark
    - CodeMirrorJson.vue 同时含 `new EditorView`、`onMounted`、`onBeforeUnmount`、`updateListener`；CodeMirror 包的 import 仅出现在 src/tools/json-formatter/ 目录内（泄漏由 Task 3 脚本在产物层断言）
    - JsonTree.vue 含折叠切换、默认展开深度常量（值 2）、数组计数徽标渲染、2^53 精度提示分支
    - json-formatter.vue 中格式化文本与 JsonTree 的数据源为同一个 computed 解析结果；错误卡渲染 line/column/message；复制按钮接 useCopy
    - src/tools/json-formatter/index.ts 含 `path: '/json-formatter'`、`icon: Braces`、`component: () => import('./json-formatter.vue')`；src/tools/index.ts tools 数组含 jsonFormatter
    - `corepack pnpm vitest run && corepack pnpm type-check` 退出码 0
  </acceptance_criteria>
  <done>/json-formatter 可直达：输入长 ID JSON → 格式化文本与树视图均原文显示；错误 JSON 有行列定位；复制可用；注册表第二个工具零手写路由接入。</done>
</task>

<task type="auto">
  <name>Task 3: 分包预算卡口 scripts/check-chunks.mjs + build gate（ARCH-02 闭环）</name>
  <files>scripts/check-chunks.mjs, package.json</files>
  <read_first>
    - 01-RESEARCH.md §Validation Architecture（ARCH-02 build gate 行、Wave 0 Gaps 的 check-chunks 条目）、§Pitfall 2
    - 01-04 探针假设（Phase 1 轻量卡口形态：只查泄漏与分离，正式体积预算归 Phase 3）
  </read_first>
  <action>
    1. 新建 scripts/check-chunks.mjs（Node ESM 脚本，零第三方依赖）：a) 读取 dist/index.html 与所有 dist/**/index.html（vite-ssg 预渲染页），提取其中引用的本地 script src 集合 = 首包 chunk 集；b) 读取每个首包 chunk 的文件内容，断言不含大小写不敏感的字符串 'codemirror'；c) 断言 dist/assets 下存在至少一个非首包 chunk 内容含 'codemirror'（证明懒加载 chunk 真实存在而非被 tree-shake 掉）；d) 断言 dist/json-formatter/index.html 与 dist/timestamp-converter/index.html 存在（每工具路由被预渲染）。任一断言失败：打印以 VIOLATION: 开头的行并 process.exit(1)；全部通过：打印 'OK: chunk budget pass' 并 exit 0。
    2. package.json scripts 增加 "check:chunks": "node scripts/check-chunks.mjs"。
    3. 运行完整 gate：corepack pnpm build && node scripts/check-chunks.mjs；若 VIOLATION 出现 CM 泄漏，回查 Task 2 的 import 位置（唯一合法位置是 json-formatter 工具目录内），修复后复跑。
  </action>
  <verify>
    <automated>corepack pnpm build && node scripts/check-chunks.mjs</automated>
    <fails_when>构建非零退出；或脚本输出任何 VIOLATION: 行或退出码 1（首包泄漏 CM / 懒加载 chunk 缺失 / 预渲染页缺失）</fails_when>
  </verify>
  <acceptance_criteria>
    - scripts/check-chunks.mjs 存在且仅 import node: 内置模块（fs/path/url 等，零第三方依赖）
    - package.json scripts 含 `"check:chunks": "node scripts/check-chunks.mjs"`
    - 脚本断言四项：首包 chunk 无 codemirror 字样、存在含 codemirror 的非首包异步 chunk、dist/json-formatter/index.html 存在、dist/timestamp-converter/index.html 存在
    - `corepack pnpm build && node scripts/check-chunks.mjs` 退出码 0 且输出含 'OK: chunk budget pass'
    - 人为向某工具组件的共享层迁移一次 CM import 后复跑脚本必须输出 VIOLATION 且退出码 1（fail-first 验证，结果记录 SUMMARY 后还原）
  </acceptance_criteria>
  <done>分包卡口可重复执行且通过：首包无 CM、两工具各自独立 chunk、每工具预渲染页存在；ARCH-02 在 Phase 1 的验收形态闭环（正式体积预算归 Phase 3——已登记假设）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户粘贴内容（可能含脚本载荷的 JSON 字符串值）→ 输出渲染 | 本计划核心信任边界：任意用户文本进入格式化输出与树视图 |
| npm registry → node_modules | CodeMirror 四包安装 |
| 构建产物 → 浏览器 | chunk 边界决定重依赖是否进首包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation of Privilege | JSON 输出渲染（格式化文本 + 树视图） | critical | mitigate | 两条输出路径全部 vnode 插值（token→文本节点/组件 props），结构性零 HTML 字符串；不调用 SafeHtml 之外的任何出口；01-03 注入样本集 + ESLint 卡口全站兜底；P2 禁令（不得静默改写数据）与保真测试同源 |
| T-01-10 | Tampering | CodeMirror 四包供应链 | medium | mitigate | codemirror/@codemirror/* 官方仓库、周下载 700 万级、legitimacy 审计 OK（01-RESEARCH 审计表）；lockfile 锁定；无 postinstall（已验证） |
| T-01-11 | Information Disclosure | 首包体积/依赖泄漏（CM 进首包） | medium | mitigate | check-chunks.mjs 可执行断言（首包无 CM + 独立 chunk 存在）纳入每计划 verify；CM 仅被工具目录静态 import |
| T-01-SC | Tampering | npm 依赖安装 | high | mitigate | 承接 Plan 01-01 T-01-SC 处置（审计表 + packageManager 锁定 + lockfile）；本计划新增四包全部为审计表 Approved 项，无需人工闸门 |

## Canon Breadcrumb
- 用户输入 XSS（canon）→ Plan 01-03 消毒管线 + /gsd:secure-phase 拥有；本计划以 vnode-only 结构性规避，不另铸禁令。
- 零第三方外链与 gzip 预算 → Phase 3 ARCH-05 拥有（本计划 check-chunks 为其 Phase 1 前置形态）。
</threat_model>

<verification>
- `corepack pnpm vitest run` 全绿（fails_when: 任一用例失败——含 tokenizer 保真集与既有回归）
- `corepack pnpm type-check` 通过（fails_when: 类型错误）
- `corepack pnpm lint` 通过（fails_when: 任何 error，含渲染出口卡口）
- `corepack pnpm build && node scripts/check-chunks.mjs` 输出 OK: chunk budget pass（fails_when: 任何 VIOLATION: 行或非零退出）
- `corepack pnpm build` 产出 dist/json-formatter/index.html（fails_when: 文件缺失）
</verification>

<success_criteria>
1. 2^53 长数字 ID 在格式化输出与树视图中原文保留（电商场景验收）
2. 格式化/压缩/校验可用，错误含行列定位；键序与重复键保持原文
3. 双栏视图共享同一解析结果；树可折叠、默认展开深度 2、计数徽标、精度提示
4. CodeMirror 输入区（行号/高亮/linter 定位）仅在本工具 chunk
5. check-chunks 卡口通过：首包无 CM、工具独立 chunk、预渲染页存在
6. 输出一键复制
</success_criteria>

<output>
完成后创建 `.planning/phases/01-app-skeleton-tool-registry/01-04-SUMMARY.md`
</output>

## Artifacts this phase produces

（本计划交付：json-formatter 工具目录、check-chunks 卡口。以下为本阶段全部产出总清单，与 SKELETON.md 同步。）

**共享契约与基建**
- `src/tools/tool.ts` — 类型 `ToolMeta`、`Tool`、`ToolCategoryId`；函数 `defineTool()`
- `src/tools/index.ts` — 常量 `toolCategories`、`tools`；函数 `toolsByCategory()`、`assertRegistryInvariants()`
- `src/router/routes.ts` — 常量 `routes`（注册表派生：home + tools.map + 404 置尾）
- `src/router/index.d.ts` — `RouteMeta` 增强字段 `layout`、`tool`
- `src/utils/sanitize.ts` — 函数 `sanitizeHtml()`、`escapeHtml()`
- `src/ui/safe-html.vue` — 组件 `SafeHtml`（全站唯一 HTML 字符串出口）
- `src/composables/useCopy.ts` — 函数 `useCopy()`
- `src/components/ToolCard.vue` — 组件 `ToolCard`
- `src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue` — 双布局组件
- `src/pages/home.vue`、`src/pages/not-found.vue` — 页面组件
**工具（Phase 5/8 批量样板）**
- `src/tools/timestamp-converter/` — 函数 `detectUnit()`、`fromTimestamp()`、`toTimestamp()`；组件 `timestamp-converter.vue`
- `src/tools/json-formatter/` — 函数 `formatJson()`、`minifyJson()`、`validateJson()`、`buildTree()`；组件 `json-formatter.vue`、`CodeMirrorJson.vue`、`JsonTree.vue`
**配置与脚本**
- `package.json`（`packageManager: pnpm@11.25.0` + `check:chunks` 脚本）、`vite.config.ts`（vue + tailwindcss + ssgOptions）、`vitest.config.ts`（happy-dom）、`eslint.config.ts`（唯一出口白名单卡口）、`src/styles/tokens.css`（@theme oklch 令牌）、`src/styles/base.css`、`scripts/check-chunks.mjs`
**测试**
- `src/tools/registry.test.ts`、`src/router/routes.test.ts`、`src/components/ToolCard.test.ts`、`src/composables/useCopy.test.ts`、`src/utils/sanitize.test.ts`、`src/ui/safe-html.test.ts`、`src/tools/timestamp-converter/timestamp-converter.service.test.ts`、`src/tools/json-formatter/json-formatter.service.test.ts`
