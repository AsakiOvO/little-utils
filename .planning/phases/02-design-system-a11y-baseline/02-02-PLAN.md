---
phase: 02-design-system-a11y-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/useThemeMode.ts
  - src/composables/useThemeMode.test.ts
  - src/ui/ThemeToggle.vue
  - src/ui/ThemeToggle.test.ts
  - src/layouts/home.layout.vue
  - src/layouts/tool.layout.vue
  - index.html
  - vite.config.ts
autonomous: true
requirements: [SITE-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "用户可切换暗色/亮色模式:默认暗色、可跟随系统偏好、选择被持久记住(刷新后保持)(ROADMAP SC-1)"
    - "跟随系统态下系统偏好变化立即生效;手动选择后系统变化不影响,切回跟随态恢复联动(D-04)"
    - "主题切换为瞬切,无全局过渡动画(D-11);ThemeToggle 契约中无异步加载分支,点击反馈即终态(UI-SPEC loading 行)"
  artifacts:
    - path: src/composables/useThemeMode.ts
      provides: 三态主题状态机(偏好存储/循环切换/系统联动/SSG 守卫)
      exports: ["THEME_STORAGE_KEY", "useThemeMode", "ThemePreference", "ResolvedTheme"]
    - path: src/composables/useThemeMode.test.ts
      provides: 三态循环/持久化/回落暗/系统联动/脏值归一/DOM 副作用/index.html 镜像断言
    - path: src/ui/ThemeToggle.vue
      provides: 三态循环图标按钮(月亮/太阳/显示器,D-03)
    - path: src/ui/ThemeToggle.test.ts
      provides: 循环切换/aria-label 三态文案/持久化/44px 源码级断言
    - path: index.html
      provides: head 内联 FOUC 阻塞脚本(与 useThemeMode 判定镜像)
      contains: "little-utils:v1:theme"
    - path: vite.config.ts
      provides: ssgOptions.onBeforePageRender 预渲染默认暗色类
      contains: "onBeforePageRender"
  key_links:
    - from: index.html 内联脚本
      to: src/composables/useThemeMode.ts
      via: 判定逻辑与 storage key 镜像对齐(测试文本断言守护,Pitfall 1)
      pattern: "little-utils:v1:theme"
    - from: src/ui/ThemeToggle.vue
      to: src/composables/useThemeMode.ts
      via: 消费 useThemeMode() 的 preference/cycle
      pattern: "useThemeMode"
    - from: src/composables/useThemeMode.ts watch
      to: html.dark 与 .theme-switching
      via: DOM 副作用(classList.toggle + colorScheme + 瞬切类配对 base.css 规则)
      pattern: "classList"
  prohibitions:
    - "主题切换不得引入全局过渡动画,颜色瞬切(D-11;组件级 hover 过渡除外)"
  assumptions:
    - "未决假设(assumption): SITE-04 探针分类 unclassified — 按 ROADMAP 成功标准 #1(默认暗/跟随系统/持久化)与 D-01..D-05 执行;如与作者意图不符,请于 verify-work 提出"
---

## Phase Goal

**As a** 任意设备与亮暗主题下的站点用户, **I want to** 站点以统一霓虹设计令牌呈现、暗色/亮色可切换且被记住、移动端输入不缩放、动效可随系统降级, **so that** 我能在任何环境下舒适使用工具,工具操作区保持克制易读。

> 用户故事自 ROADMAP **Goal:** 行逐句映射(原文为结果式散文,非标准故事格式——映射未增删任何能力,已在规划返回中向作者披露)。

<objective>
交付 SITE-04 的完整垂直切片:useThemeMode 三态状态机(暗/亮/跟随系统,持久化 + 系统实时联动 + no-preference 回落暗)、ThemeToggle 三态循环图标按钮挂载双 layout 顶栏右上角、head 内联 FOUC 阻塞脚本 + 预渲染默认暗色类实现零闪烁。

Purpose: 主题切换是本阶段用户可直接感知的核心能力;状态单点(composable)与判定镜像(head 脚本)是 Phase 4 收藏等 localStorage 能力的键惯例首落地与 SSG 纪律样板。
Output: src/composables/useThemeMode.ts(+test)、src/ui/ThemeToggle.vue(+test)、双 layout 挂载、index.html 内联脚本、vite.config.ts 预渲染钩子。
</objective>

## Artifacts this phase produces

新建:`src/styles/tokens.ts`、`src/styles/tokens.test.ts`、`src/styles/base-rules.test.ts`、`src/composables/useThemeMode.ts`(+`.test.ts`)、`src/ui/Button.vue` / `Input.vue` / `Card.vue` / `Tabs.vue` / `CopyableText.vue` / `ThemeToggle.vue`(各配就近 `.test.ts`)、`src/tools/json-formatter/components/cm-theme.ts`。
修改:`src/styles/tokens.css`、`src/styles/base.css`、`index.html`、`vite.config.ts`、`package.json`/`pnpm-lock.yaml`(仅移除 oneDark)、`src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue`、`src/pages/home.vue`、`src/pages/not-found.vue`、`src/components/ToolCard.vue`、`src/tools/json-formatter/components/CodeMirrorJson.vue`、`src/tools/json-formatter/json-formatter.vue`。
新增符号:`tokens`/`parseOklch`、`THEME_STORAGE_KEY`/`useThemeMode`/`ThemePreference`/`ResolvedTheme`、`createCmTheme`、六件套组件名。

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-a11y-baseline/02-CONTEXT.md
@.planning/phases/02-design-system-a11y-baseline/02-RESEARCH.md
@.planning/phases/02-design-system-a11y-baseline/02-UI-SPEC.md
@src/composables/useCopy.ts
@src/composables/useCopy.test.ts
@src/layouts/home.layout.vue
@src/layouts/tool.layout.vue
@src/styles/base.css
@index.html
@vite.config.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: useThemeMode 三态状态机 — 偏好存储 + 系统联动 + 瞬切副作用</name>
  <files>src/composables/useThemeMode.ts, src/composables/useThemeMode.test.ts</files>
  <read_first>
    - src/composables/useCopy.ts(同目录唯一先例:VueUse 薄封装 + Don't Hand-Roll 头注释 + 单一导出)
    - src/composables/useCopy.test.ts(mock 注入模式:stubGlobal + beforeEach/afterEach 成对 + 环境缺口头注释)
    - 02-RESEARCH.md §Pattern 2(三态状态机完整骨架与为何弃用现成方案)、§Pitfall 3/5/6/7
    - 02-CONTEXT.md D-01/D-02/D-03/D-04/D-11
    - node_modules/.pnpm/@vueuse+core@14.4.0_*/node_modules/@vueuse/core/dist/index.d.ts(已核实:UseStorageOptions 含 initOnMounted 与 serializer(read/write 对象形态);usePreferredColorScheme 在导出表)
  </read_first>
  <action>
    新建 src/composables/useThemeMode.ts,按 RESEARCH §Pattern 2 骨架逐条落地:
    1. 头注释(引 D-01/D-02/D-04/D-11):禁止使用 useDark(布尔两态,无 auto 语义位)与 useColorMode(其 auto 分支经 usePreferredDark 解析,no-preference 系统回落亮色,违反 D-01 回落暗色);禁止裸调 matchMedia 监听(VueUse 管理监听/cleanup/SSR 守卫)。
    2. 导出 THEME_STORAGE_KEY = 'little-utils:v1:theme'——全站唯一 storage key 定义点(PITFALLS 键前缀惯例首落地);导出类型 ThemePreference = 'dark' | 'light' | 'auto' 与 ResolvedTheme = 'dark' | 'light'。
    3. normalize(raw: unknown): ThemePreference 白名单归一——非 'dark'/'light' 一律 'auto'(Pitfall 7 脏值防线)。
    4. createThemeMode():preference = useStorage(THEME_STORAGE_KEY, 'auto', undefined, { initOnMounted: true, serializer: { read: normalize, write: (v: ThemePreference) => v } })——initOnMounted 维持 vite-ssg mounted 纪律(预渲染期不读 storage,防水合不匹配,Pitfall 6);system = usePreferredColorScheme()(实时);resolved = computed:preference 非 auto 直接返回,auto 时 system==='light' ? 'light' : 'dark'——no-preference 显式回落 dark(D-01)。
    5. watch(resolved) DOM 副作用:首行 typeof document === 'undefined' 守卫(SSG Node 预渲染,Pitfall 6);依次:挂 .theme-switching 类(D-11/Pitfall 3 瞬切——配对规则在 base.css,plan 02-01 Task 2 已声明,两处注释互指)→ documentElement.classList.toggle('dark', mode==='dark') → documentElement.style.colorScheme = mode → 双 requestAnimationFrame 后移除 .theme-switching 类。
    6. cycle():preference 三态循环 dark→light→auto→dark(D-02/D-03)。
    7. 模块级单例:singleton ??= createThemeMode(),export function useThemeMode() 返回 { preference, resolved, cycle }(多消费点共享;ThemeToggle 与 CodeMirrorJson 均消费)。
    新建 src/composables/useThemeMode.test.ts:头注释写明 happy-dom 不计算媒体查询(Pitfall 5);beforeEach:vi.resetModules()(重置模块级单例,用例内动态 import)+ localStorage 清理 + vi.stubGlobal('matchMedia', 工厂 fake——返回 { matches 可控、addEventListener/removeEventListener 收集回调 });afterEach:vi.unstubAllGlobals()。用例:①cycle 三态循环且 localStorage 按序写入(D-02/D-03 持久化);②auto + 系统 no-preference → resolved dark(D-01);③auto + 系统 light → light;auto 态触发 matchMedia 回调变更偏好 → resolved 实时跟随(D-04);④手动 dark 后系统变化不影响,切回 auto 恢复联动(D-04);⑤storage 脏值 'blue' → 归一 auto(Pitfall 7);⑥resolved 变更驱动 documentElement class 与 style.colorScheme 翻转、瞬切窗口挂 .theme-switching、双 rAF 后移除(D-11)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/composables/useThemeMode.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - useThemeMode.ts 导出 THEME_STORAGE_KEY、useThemeMode(返回 preference/resolved/cycle)与两个类型;源码含 typeof document 守卫与 initOnMounted: true
    - 源码零 useDark/useColorMode 引用(理由经头注释声明)
    - 测试 ≥6 用例覆盖 ①-⑥ 且全绿
  </acceptance_criteria>
  <done>三态状态机全行为锁定:循环/持久化/no-preference 回落暗/系统联动/脏值归一/SSG 守卫/瞬切副作用,跑进现有 vitest 命令。</done>
</task>

<task type="auto">
  <name>Task 2: ThemeToggle 组件 + 双 layout 顶栏右上角挂载</name>
  <files>src/ui/ThemeToggle.vue, src/ui/ThemeToggle.test.ts, src/layouts/home.layout.vue, src/layouts/tool.layout.vue</files>
  <read_first>
    - src/ui/safe-html.vue(SFC 形态先例:template 前 script 后、defineProps 泛型、无 style 块、文件头决策注释)
    - 02-RESEARCH.md §Pattern 5(ThemeToggle 行:aria-label 文案、图标 aria-hidden、语义在 label)
    - 02-UI-SPEC.md §Copywriting Contract(aria-label 三态文案锁定)
    - 02-CONTEXT.md D-03/D-13/D-18/D-19/D-21
    - src/layouts/home.layout.vue / src/layouts/tool.layout.vue(现状全文,挂载点锚点)
    - 02-PATTERNS.md 六件套段(defineOptions + props 形态)
  </read_first>
  <action>
    新建 src/ui/ThemeToggle.vue:defineOptions({ name: 'ThemeToggle' });消费 const { preference, cycle } = useThemeMode()(显式 import '../composables/useThemeMode',D-18);图标映射 preference==='dark' → Moon、'light' → Sun、'auto' → Monitor(@lucide/vue 按需显式导入,三图标导出名已核实存在;图标元素 aria-hidden="true",语义全部在 aria-label);按钮 type="button" @click="cycle()",:aria-label 与 :title 同文案——切换主题(当前:暗色) / 切换主题(当前:亮色) / 切换主题(当前:跟随系统)(auto 文案为「跟随系统」,UI-SPEC Copywriting 锁定);根类:inline-flex items-center justify-center rounded-md border border-[var(--color-border)] text-[var(--color-text-muted)] transition-colors hover:bg-[var(--color-surface-raised)] hover:text-[var(--color-text-primary)] min-h-11 min-w-11(44px 触控目标,D-21;transition-colors 为 hover 用途——D-11 只禁全局主题过渡,组件级 hover 渐变允许,RESEARCH Pitfall 3);全 Tailwind utility 无 style 块;禁 glow 类(D-08 组件克制);头注释引 D-02/D-03/D-13/D-18/D-19/D-21。
    新建 src/ui/ThemeToggle.test.ts(matchMedia mock + resetModules + 动态 import,同 Task 1 测试纪律):mount 后断言①初始(preference='auto')aria-label 含「跟随系统」;②点击一次 → aria-label 含「暗色」;③连点三次循环回初始态;④localStorage 的 THEME_STORAGE_KEY 值随点击按 dark→light→auto 序列变化;⑤源码级断言根类含 min-h-11 与 min-w-11(Pitfall 5:不测 getBoundingClientRect);⑥图标元素 aria-hidden="true"。
    修改 src/layouts/home.layout.vue(D-03 顶栏右上角):header 右侧改为并列容器 <div class="flex items-center gap-4"> 包住现有 <nav>开发者 &amp; 办公工具箱</nav> 与 <ThemeToggle />(toggle 在 nav 之后);新增显式 import ThemeToggle from '../ui/ThemeToggle.vue'(D-18);其余结构一行不动(Phase 4/6 只填不改)。
    修改 src/layouts/tool.layout.vue(D-03):header 内容器(items-center gap-4)内在返回首页 RouterLink 与品牌文本之后新增 <div class="ml-auto flex items-center gap-3"> 包住现有品牌 span 与 <ThemeToggle />(ml-auto 推到顶栏右上角);新增显式 import;返回首页 RouterLink 一行不动(其归一在 plan 02-06 Task 3)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/ThemeToggle.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - 双 layout 顶栏右上角出现 ThemeToggle(home: nav 并列;tool: ml-auto 右推);两 layout 显式 import(D-18 无 barrel)
    - 点击循环 暗→亮→跟随系统,图标 Moon/Sun/Monitor 随态变化,aria-label/title 三态文案,选择写 localStorage
    - 按钮 min-h-11 min-w-11(44px);图标 aria-hidden;全 Tailwind 无 style 块
  </acceptance_criteria>
  <done>用户在任一 layout 可点击循环切换三态、选择被持久记住(D-02/D-03 UI 落地);新增工具页经 tool.layout 自动获得切换能力。</done>
</task>

<task type="auto">
  <name>Task 3: FOUC 内联阻塞脚本 + 预渲染默认暗色类 + 镜像断言</name>
  <files>index.html, vite.config.ts, src/composables/useThemeMode.test.ts</files>
  <read_first>
    - index.html(现状全文:head 仅 6 行,注入点 = meta charset 之后、其它标签之前)
    - vite.config.ts(现状全文:ssgOptions.includedRoutes 先例,本任务在其旁加钩子)
    - 02-RESEARCH.md §Pattern 3(FOUC 脚本完整骨架 + 预渲染默认类两案 + 「构建后目测暗色直开无白闪」验收口径)、§Pitfall 1(判定漂移)
    - 02-CONTEXT.md D-05(内联脚本实现与模板注入方式属 Claude's Discretion)
    - node_modules/vite-ssg/dist/ 类型声明(核对 onBeforePageRender 钩子真实参数签名后再接线)
  </read_first>
  <action>
    修改 index.html(D-05):在 <meta charset="UTF-8"> 之后、favicon/viewport/title 等其它标签之前,插入普通内联 IIFE script(逐字镜像 useThemeMode 的 resolved 判定,头尾注释与 src/composables/useThemeMode.ts 互指「镜像对齐,改此必改彼」):try 块内——localStorage.getItem('little-utils:v1:theme')(与 THEME_STORAGE_KEY 字面一致,注释指明定义点);mode = 白名单('dark'/'light')否则 (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark')(无记录/auto:no-preference 回落暗,D-01);document.documentElement.classList.toggle('dark', mode === 'dark')(白名单布尔挂类,无注入面——RESEARCH Security 表);document.documentElement.style.colorScheme = mode;catch 静默——保持预渲染默认暗色基线(隐私模式等 storage 不可用兜底)。
    修改 vite.config.ts:ssgOptions 内新增 onBeforePageRender 钩子——给每路由预渲染 HTML 的 html 标签追加 class="dark"(暗色为默认主题,亮色用户由内联脚本在首绘前移除——RESEARCH Pattern 3「绝对零闪烁」方案);实现前先读 node_modules/vite-ssg 类型声明核对钩子签名;接线用简单字符串替换且幂等(已含 class 时不重复);注释引 D-05 与 RESEARCH §Pattern 3。
    修改 src/composables/useThemeMode.test.ts(Task 1 文件追加 describe):readFileSync(new URL('../../index.html', import.meta.url), 'utf8') 后断言包含 THEME_STORAGE_KEY 字面值、包含 'dark' 与 'light' 白名单分支、包含 matchMedia 判定(防脚本与状态机判定漂移,Pitfall 1;失败消息提示「head 内联脚本与 useThemeMode 判定逻辑必须镜像对齐」)。
  </action>
  <verify>
    <automated>corepack pnpm build && corepack pnpm check:chunks && grep -c "little-utils:v1:theme" dist/index.html && grep -c "class=\"dark\"" dist/index.html && corepack pnpm vitest run src/composables/useThemeMode.test.ts</automated>
    <fails_when>任一命令非零退出码——如 grep 退出码 1(预渲染产物缺内联脚本或缺 dark 类)、check:chunks 输出 VIOLATION、vitest 0 passed 或 failed N</fails_when>
  </verify>
  <acceptance_criteria>
    - index.html head 内含镜像 IIFE 脚本,判定与 useThemeMode resolved 一致(测试守护)
    - vite.config.ts ssgOptions 含 onBeforePageRender 且幂等追加 class="dark"
    - build 后 dist/index.html 同时命中内联脚本与 dark 类两个 grep;镜像断言测试绿
    - 全量 vitest 无回归(执行者自跑 corepack pnpm vitest run 确认)
  </acceptance_criteria>
  <done>直开/刷新零闪烁(D-05);预渲染静态 HTML 默认暗色;判定逻辑与状态机镜像绑定有自动化防线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → 主题状态机/head 脚本 | 存储值可被任意站点脚本或手滑污染(untrusted),流入 classList 与 colorScheme |
| 构建期模板 → 预渲染产物 | 内联脚本为静态产物,无运行时外部输入参与 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | localStorage 脏值流入 DOM 属性 | medium | mitigate | normalize 白名单双实现(composable + head 脚本,非 dark/light 一律 auto,Pitfall 7);classList.toggle(布尔) 与 CSSOM 属性赋值本身非注入面(RESEARCH Security 表) |
| T-02-04 | Tampering | head 内联脚本被篡改 | low | accept | 脚本为构建期静态产物、无外部输入参与;Phase 3 若上 CSP 需为该脚本配 hash/nonce(记入 Phase 3 交接) |
| T-02-SC | Tampering | npm/pnpm 包安装 | high | mitigate | 本计划零新装;禁止 pnpm update 保持 lockfile 现状(RESEARCH Package Legitimacy Audit 纪律) |
</threat_model>

<verification>
- corepack pnpm vitest run src/composables/useThemeMode.test.ts src/ui/ThemeToggle.test.ts 全绿(状态机 + 组件 + 镜像断言)
- corepack pnpm type-check 退出码 0
- corepack pnpm build && corepack pnpm check:chunks 退出码 0(预渲染含脚本与默认暗色类,chunk 预算不变)
- corepack pnpm vitest run 全量无回归(86+ 既有测试)
</verification>

<success_criteria>
- 用户在双 layout 顶栏右上角点击图标循环 暗→亮→跟随系统,图标与 aria-label 同步变化,选择持久化(刷新后保持)
- 跟随系统态下 OS 深浅色切换实时生效;手动选择后系统变化不干扰;no-preference 回落暗(D-01)
- 预渲染产物 head 含镜像脚本 + html 默认 dark 类——直开无白闪(D-05)
- 主题切换瞬切无全局过渡(D-11,经 .theme-switching 配对)
</success_criteria>

<output>
Create `.planning/phases/02-design-system-a11y-baseline/02-02-SUMMARY.md` when done
</output>

