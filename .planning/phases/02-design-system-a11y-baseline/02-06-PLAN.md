---
phase: 02-design-system-a11y-baseline
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/components/ToolCard.vue
  - src/components/ToolCard.test.ts
  - src/pages/home.vue
  - src/pages/not-found.vue
  - src/layouts/home.layout.vue
  - src/layouts/tool.layout.vue
autonomous: true
requirements: [STYL-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "输入框/卡片/按钮/Tabs/可复制文本域等基础组件就位,工具操作区无装饰干扰、克制易读(ROADMAP SC-5;经 D-15 存量迁移真实验证六件套 API 完备性)"
    - "全站字体仅 400/600 两档、尺寸归一 16/14/20/28 四档(UI-SPEC Typography 迁移归一;间距 12px 存量归一到 8/16 核心档)"
    - "首页与 404 页消费六件套渲染,新旧两套组件不并存(D-15)"
  artifacts:
    - path: src/components/ToolCard.vue
      provides: 消费 Card(as=RouterLink)+ hover accent 保留
      contains: "Card"
    - path: src/pages/home.vue
      provides: 空态卡消费 Card + Typography 归一(28px/20px、600)
    - path: src/pages/not-found.vue
      provides: 返回首页 44px 触控 + Typography 归一
    - path: src/layouts/home.layout.vue
      provides: 品牌 logo Display 归一(28px/600)+ footer 14px
    - path: src/layouts/tool.layout.vue
      provides: header 16px 间距归一 + 返回键 44px
  key_links:
    - from: src/components/ToolCard.vue
      to: src/ui/Card.vue
      via: 显式 import 消费(D-18 无 barrel)
      pattern: "ui/Card"
    - from: src/layouts/home.layout.vue 与 tool.layout.vue
      to: plan 02-02 ThemeToggle 挂载
      via: 归一不移动切换入口位置(D-03)
      pattern: "ThemeToggle"
  prohibitions:
    - "装饰效果不得侵入工具操作区(Phase 6 边界;本阶段工具区保持中性克制——glow 仅保留 home.layout 品牌字既有用法)"
  assumptions:
    - "未决假设(assumption): STYL-03 探针分类 unclassified — 工具操作区克制易读由六件套中性消费与迁移实现(STYL-03 后半句)"
---

## Phase Goal

**As a** 任意设备与亮暗主题下的站点用户, **I want to** 站点以统一霓虹设计令牌呈现、暗色/亮色可切换且被记住、移动端输入不缩放、动效可随系统降级, **so that** 我能在任何环境下舒适使用工具,工具操作区保持克制易读。

> 用户故事自 ROADMAP **Goal:** 行逐句映射(原文为结果式散文,非标准故事格式——映射未增删任何能力,已在规划返回中向作者披露)。

<objective>
D-15 存量全量迁移(第一批):ToolCard/home/not-found/双 layout 改用六件套与归一排版——以真实消费验证 Card/Button API 完备性,UI-SPEC Typography(400/600 两档、16/14/20/28 四档)与 Spacing(12px 存量归一)全站落地;空态文案逐字保留。

Purpose: 避免 Phase 4/6 改版时新旧两套组件并存(D-15);排版归一是 UI-SPEC 契约的硬规则(迁移即断言)。
Output: ToolCard.vue(+test)、home.vue、not-found.vue、home.layout.vue、tool.layout.vue。
</objective>

## Artifacts this phase produces

新建:`src/styles/tokens.ts`、`src/styles/tokens.test.ts`、`src/styles/base-rules.test.ts`、`src/composables/useThemeMode.ts`(+`.test.ts`)、`src/ui/Button.vue` / `Input.vue` / `Card.vue` / `Tabs.vue` / `CopyableText.vue` / `ThemeToggle.vue`(各配就近 `.test.ts`)、`src/tools/json-formatter/components/cm-theme.ts`。
修改:`src/styles/tokens.css`、`src/styles/base.css`、`index.html`、`vite.config.ts`、`package.json`/`pnpm-lock.yaml`(仅移除 oneDark)、`src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue`、`src/pages/home.vue`、`src/pages/not-found.vue`、`src/components/ToolCard.vue`、`src/tools/json-formatter/components/CodeMirrorJson.vue`、`src/tools/json-formatter/json-formatter.vue`。
新增符号:`tokens`/`parseOklch`、`THEME_STORAGE_KEY`/`useThemeMode`/`ThemePreference`/`ResolvedTheme`、`createCmTheme`、六件套组件名。

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-a11y-baseline/02-CONTEXT.md
@.planning/phases/02-design-system-a11y-baseline/02-UI-SPEC.md
@.planning/phases/02-design-system-a11y-baseline/02-PATTERNS.md
@src/components/ToolCard.vue
@src/components/ToolCard.test.ts
@src/pages/home.vue
@src/pages/not-found.vue
@src/layouts/home.layout.vue
@src/layouts/tool.layout.vue
@src/ui/Card.vue
@src/ui/Button.vue
</context>

<tasks>

<task type="tracer">
  <name>Task 1: ToolCard 迁移 — Card(as=RouterLink) 消费 + hover accent 保留</name>
  <files>src/components/ToolCard.vue, src/components/ToolCard.test.ts</files>
  <read_first>
    - src/components/ToolCard.vue(现状全文——根 RouterLink 卡片样式 :3-6 是迁移点)
    - src/components/ToolCard.test.ts(现状全文——既有断言语义必须保留)
    - src/ui/Card.vue(plan 02-03 产物——as/padding 契约)
    - 02-PATTERNS.md ToolCard 段(迁移方向:换 Card 容器 + hover accent 保留 hover:border-[var(--color-neon-cyan)])
    - 02-CONTEXT.md D-15/D-18
  </read_first>
  <action>
    修改 src/components/ToolCard.vue:根 <RouterLink :to="tool.path" class="group flex flex-col gap-2 rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)] p-5 ..."> 替换为 <Card as="RouterLink" :to="tool.path" class="group flex flex-col gap-2 transition-colors hover:border-[var(--color-neon-cyan)] hover:bg-[var(--color-surface-raised)]">(rounded-lg/border/bg-surface/p-5 由 Card 默认提供;hover accent 保留——UI-SPEC Accent reserved「工具卡 hover 边框」);内部结构一行不动(icon size-6 aria-hidden、NEW 徽标 magenta border/文字、h3/p);新增显式 import Card from '../ui/Card.vue'(D-18);文件头注释补引 D-15。RouterLink import 若不再使用则一并移除(以 Card as 渲染)。
    修改 src/components/ToolCard.test.ts:适配 Card 结构——既有断言语义全部保留(名称/描述/图标/NEW 徽标渲染、链接 href);把对根 RouterLink 的断言改为对 Card as=RouterLink 渲染的 a 元素断言(或直接对 wrapper.find('a'));不新增行为断言。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/components/ToolCard.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - ToolCard 根由 Card 渲染(源码 import 可见),hover accent 两类保留;NEW 徽标与 icon 结构不变
    - ToolCard.test.ts 既有断言语义保留且全绿
  </acceptance_criteria>
  <done>首页工具卡片经六件套渲染(D-15 第一站);Card 的 as=RouterLink 用法经真实消费验证。</done>
</task>

<task type="auto">
  <name>Task 2: home/not-found 迁移 + Typography 归一 + 文案锁定</name>
  <files>src/pages/home.vue, src/pages/not-found.vue</files>
  <read_first>
    - src/pages/home.vue(现状全文:整页空态卡 :12-20、分类空态虚线卡 :28-33、h1/h2/空态标题)
    - src/pages/not-found.vue(现状全文:404 文字、h1、返回首页 RouterLink)
    - 02-UI-SPEC.md §Typography(归一四项:font-bold→600、font-medium→400 或 600、text-3xl→28px、text-lg→20px)与 §Copywriting Contract(Empty state 三行文案锁定不改写)
    - src/ui/Card.vue(plan 02-03 产物)
    - 02-CONTEXT.md D-15;02-PATTERNS.md home/not-found 段
  </read_first>
  <action>
    修改 src/pages/home.vue:
    - 整页空态卡(v-if="!hasAnyTool" 的 rounded-lg border bg-surface p-10 text-center 容器)替换为 <Card :padding="false" class="mt-12 p-10 text-center">(padding 关闭 + 显式 p-10,Card 默认 p-5 不符);内部两行文案逐字保留:「工具正在准备上线」「工具注册表尚未收录任何工具,敬请期待。」(UI-SPEC Empty state 锁定,不改写);空态标题类 text-lg font-medium → text-[20px] font-semibold(Heading 20px/600 归一)。
    - 分类空态虚线卡(border-dashed)保留原样式与文案「该分类工具即将上线」逐字不动(PATTERNS 迁移方向允许保留虚线样式——虚线不属于 Card 实线语言;注释说明保留原因)。
    - h1 类 font-mono text-3xl font-bold tracking-tight → font-mono text-[28px] font-semibold tracking-tight(Display 28px/600/1.2);h2 分类标题 text-lg font-semibold → text-[20px] font-semibold(Heading 20px);新增显式 import Card from '../ui/Card.vue'(D-18)。
    修改 src/pages/not-found.vue:
    - 「404」类 text-6xl font-bold → text-6xl font-semibold(weight 归一 600;尺寸 UI-SPEC 未点名,保留 text-6xl)。
    - h1「页面不存在」text-xl font-semibold → text-[20px] font-semibold(Heading 20px)。
    - 返回首页 RouterLink 替换为 Button 消费或 Button 样式语言 + min-h-11(PATTERNS 允许后者):改为 <RouterLink to="/" class="mt-8 inline-flex items-center justify-center min-h-11 rounded-md border border-[var(--color-border)] bg-[var(--color-surface)] px-4 text-sm text-[var(--color-text-primary)] transition-colors hover:bg-[var(--color-surface-raised)] hover:border-[var(--color-neon-cyan)] hover:text-[var(--color-neon-cyan)]">返回首页</RouterLink>(44px 触控,D-21;去 font-medium——weights 仅 400/600;hover accent 对齐 Button outline 语言)。若改用 <Button> 组件消费亦可(Button 无链接形态,选 RouterLink+样式方案以保语义导航)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;全量 vitest 出现 failed(含 ToolCard/registry/routes 既有用例)</fails_when>
  </verify>
  <acceptance_criteria>
    - home.vue 整页空态经 Card 渲染、分类空态虚线样式与文案保留、两处 Typography 归一到位
    - not-found.vue 返回首页 44px 触控、weight 归一 600、h1 20px
    - 空态三行文案逐字保留(人工对照 UI-SPEC Empty state 行)
  </acceptance_criteria>
  <done>首页与 404 页完成六件套消费与排版归一(D-15);文案零改写。</done>
</task>

<task type="auto">
  <name>Task 3: 双 layout 归一 — 品牌 Display/footer 14px/header 16px/返回键 44px</name>
  <files>src/layouts/home.layout.vue, src/layouts/tool.layout.vue</files>
  <read_first>
    - src/layouts/home.layout.vue(plan 02-02 已挂 ThemeToggle——归一不得移动其位置)
    - src/layouts/tool.layout.vue(同上;返回首页 RouterLink py-1.5 是间距归一点)
    - 02-UI-SPEC.md §Typography(Display 28px/600 品牌logo;Label/Meta 14px 含 footer)与 §Spacing Exceptions(存量 12px py-3/py-1.5 归一到 8/16 核心档,不新增 12px 用法)
    - 02-CONTEXT.md D-03(结构定型,Phase 4/6 只填不改)/D-15/D-21
  </read_first>
  <action>
    修改 src/layouts/home.layout.vue:
    - 品牌 logo span 类 font-mono text-lg font-bold tracking-tight → font-mono text-[28px] font-semibold tracking-tight(Display 28px/600;text-cyan 与 glow-cyan 保留——UI-SPEC Accent reserved「品牌标识 cyan + glow-cyan」);D-08:此 glow 为装饰/品牌层既有用法,保留。
    - footer 文案类 text-xs → text-sm(Label/Meta 14px——UI-Specified footer 属 Label/Meta 档);footer 其余不动。
    - header py-4(16px)与 nav text-sm 不动;ThemeToggle 挂载结构与位置不动(D-03)。
    修改 src/layouts/tool.layout.vue:
    - header 内容器 py-3 → py-4(12px 归一 16px 核心档)。
    - 返回首页 RouterLink 类 px-2.5 py-1.5 → 保留 inline-flex items-center gap-1 rounded-md border border-[var(--color-border)] px-3 text-sm text-[var(--color-text-muted)] transition-colors hover:bg-[var(--color-surface-raised)] hover:text-[var(--color-text-primary)] 并追加 min-h-11(44px 触控,D-21;py-1.5 淘汰,不新增 12px 用法)。
    - 品牌文本 text-sm 不动;ThemeToggle 挂载结构与位置不动(D-03);主区 max-w-4xl px-4 py-8 不动。
  </action>
  <verify>
    <automated>corepack pnpm vitest run && corepack pnpm type-check && corepack pnpm lint</automated>
    <fails_when>任一命令非零退出码;全量 vitest 出现 failed;lint 报错(含 oxlint)</fails_when>
  </verify>
  <acceptance_criteria>
    - home.layout 品牌 28px/600、footer 14px;tool.layout header 16px 间距、返回键 min-h-11
    - 双 layout 中 ThemeToggle 位置与结构未被本次归一移动(D-03 保持)
    - 全量测试/type-check/lint 绿
  </acceptance_criteria>
  <done>双 layout 完成排版与间距归一(D-15);工具区顶栏保持克制(零新增装饰)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 注册表 Tool 元数据 → ToolCard 渲染 | 工具 name/description 为注册表静态数据(非用户输入) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | 迁移中绕过消毒渲染管线 | medium | mitigate | 本计划全部迁移面均为静态元数据与文案插值;SafeHtml 唯一出口与 ESLint vue/no-v-html 卡口不动;json-formatter 渲染纪律头注释保留(plan 02-07) |
| T-02-SC | Tampering | npm/pnpm 包安装 | high | mitigate | 零新装、零 package.json 变更;禁止 pnpm update |
</threat_model>

<verification>
- corepack pnpm vitest run && corepack pnpm type-check && corepack pnpm lint 退出码 0
- ToolCard/home/not-found 源码 import 自 src/ui/(六件套),无双套并存
- 全站 grep 不得再出现 font-bold(700)与 font-medium(500)新用法;py-3/py-1.5 存量已归一
- 空态三行文案与 UI-SPEC Empty state 行逐字一致
</verification>

<success_criteria>
- 六件套经真实消费验证 API 完备性(ToolCard=Card as、not-found=Button 语言、双 layout=ThemeToggle+归一)
- 排版体系归一:weights 400/600、尺寸 16/14/20/28、间距核心档
- 工具操作区保持中性克制(STYL-03 后半句 + Phase 6 边界就位)
</success_criteria>

<output>
Create `.planning/phases/02-design-system-a11y-baseline/02-06-SUMMARY.md` when done
</output>

