---
phase: 02-design-system-a11y-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/styles/tokens.ts
  - src/styles/tokens.test.ts
  - src/styles/base.css
  - src/styles/base-rules.test.ts
autonomous: true
requirements: [STYL-01, STYL-03, SITE-05]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "霓虹青/品红/电光黄以 oklch 设计令牌贯穿全站,主题文字对比度 ≥ 4.5:1(可测量验证)(ROADMAP SC-4)"
    - "系统开启「减少动态效果」后,站点动画/过渡自动降级为静态或极简过渡(ROADMAP SC-3)"
    - "原生 input/select/textarea 一体适用 16px 兜底,输入不触发 iOS 强制放大(D-21)"
    - "亮暗双套语义令牌就位,html 挂载 .dark 类即整站零改动翻转(全部页面消费 var(--color-*))"
  artifacts:
    - path: src/styles/tokens.css
      provides: 三层令牌(@theme 原语+亮色语义 / .dark 暗色语义)+ D-10 功能色 + glow 亮暗两套 + color-scheme 双声明
      contains: ".dark"
    - path: src/styles/tokens.ts
      provides: D-20 色值单点常量模块 + parseOklch 纯函数
      exports: ["tokens", "parseOklch"]
    - path: src/styles/tokens.test.ts
      provides: WCAG 对比度算法 + 黑白锚点 21:1 + 20+ 色对双主题断言 + tokens.css↔tokens.ts 同步断言
    - path: src/styles/base.css
      provides: 16px 兜底 / reduced-motion 中和块 / 全局 focus-visible 焦点环 / .theme-switching 瞬切规则
    - path: src/styles/base-rules.test.ts
      provides: base.css 规则存在性内容断言(SITE-05/STYL-03 自动化部分)
  key_links:
    - from: src/styles/tokens.test.ts
      to: src/styles/tokens.css
      via: readFileSync 文本同步断言(改色即红双保险)
      pattern: "toContain"
    - from: src/styles/tokens.ts
      to: src/styles/tokens.css
      via: 字面 oklch 值逐字一致(单点双源)
      pattern: "oklch\\("
    - from: src/styles/base.css
      to: plan 02-02 useThemeMode
      via: .theme-switching 类配对(composable 挂载/双 rAF 移除,base.css 声明中和规则)
      pattern: "theme-switching"
  prohibitions:
    - "页面与组件不得直接引用原语层色值,必须经语义层 var(--color-*) 消费(D-09)"
  assumptions:
    - "未决假设(assumption): STYL-01 探针分类 unclassified — 本计划按 ROADMAP 成功标准 #4(oklch 令牌 + 4.5:1 可测量验证)与 D-07/D-20 执行;如与作者意图不符,请于 verify-work 提出"
    - "未决假设(assumption): STYL-03 探针分类 unclassified — 按 ROADMAP 成功标准 #3 与 D-22 全局中和块执行"
    - "未决假设(assumption): SITE-05 探针分类 unclassified — 按 ROADMAP 成功标准 #2 与 D-21 双层落点执行(本计划落地全局兜底层,组件默认层在 plan 02-03)"
---

## Phase Goal

**As a** 任意设备与亮暗主题下的站点用户, **I want to** 站点以统一霓虹设计令牌呈现、暗色/亮色可切换且被记住、移动端输入不缩放、动效可随系统降级, **so that** 我能在任何环境下舒适使用工具,工具操作区保持克制易读。

> 用户故事自 ROADMAP **Goal:** 行逐句映射(原文为结果式散文,非标准故事格式——映射未增删任何能力,已在规划返回中向作者披露)。

<objective>
把 tokens.css 重构为「@theme 原语+亮色语义 / .dark 暗色语义」三层令牌体系,建立 tokens.ts 色值单点与对比度/同步双保险单测,并在 base.css 落地 16px 兜底、reduced-motion 全局中和、焦点环兜底与主题瞬切四条全局规则。

Purpose: 本计划是 Phase 2 一切视觉与主题能力的第一块地基——六件套组件、存量迁移、CodeMirror 双主题全部消费这里的语义变量与 .theme-switching 配对规则;D-20「改色即红」保证 Phase 6 强风格化时对比度红线不被无声突破。
Output: src/styles/tokens.css(重构)、src/styles/tokens.ts(新)、src/styles/tokens.test.ts(新)、src/styles/base.css(增改)、src/styles/base-rules.test.ts(新)。
</objective>

## Artifacts this phase produces

新建:`src/styles/tokens.ts`、`src/styles/tokens.test.ts`、`src/styles/base-rules.test.ts`、`src/composables/useThemeMode.ts`(+`.test.ts`)、`src/ui/Button.vue` / `Input.vue` / `Card.vue` / `Tabs.vue` / `CopyableText.vue` / `ThemeToggle.vue`(各配就近 `.test.ts`)、`src/tools/json-formatter/components/cm-theme.ts`。
修改:`src/styles/tokens.css`、`src/styles/base.css`、`index.html`、`vite.config.ts`、`package.json`/`pnpm-lock.yaml`(仅移除 oneDark)、`src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue`、`src/pages/home.vue`、`src/pages/not-found.vue`、`src/components/ToolCard.vue`、`src/tools/json-formatter/components/CodeMirrorJson.vue`、`src/tools/json-formatter/json-formatter.vue`。
新增符号:`tokens`/`parseOklch`(tokens.ts)、`THEME_STORAGE_KEY`/`useThemeMode`/`ThemePreference`/`ResolvedTheme`(useThemeMode.ts)、`createCmTheme`(cm-theme.ts)、六件套组件名。

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-a11y-baseline/02-CONTEXT.md
@.planning/phases/02-design-system-a11y-baseline/02-RESEARCH.md
@.planning/phases/02-design-system-a11y-baseline/02-UI-SPEC.md
@src/styles/tokens.css
@src/styles/base.css
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 三层令牌重构 — tokens.css 亮暗双套语义 + tokens.ts 色值单点 + tokens.test.ts 对比度与同步断言</name>
  <files>src/styles/tokens.css, src/styles/tokens.ts, src/styles/tokens.test.ts</files>
  <read_first>
    - src/styles/tokens.css(现状 29 行全文——暗色语义值 :15-20 与 glow 暗值 :26-27 是 D-12 逐字搬迁的搬迁源)
    - src/styles/base.css(html 规则中硬编码的颜色方案声明位置,Task 2 要移除)
    - 02-RESEARCH.md §Pattern 1(tokens.css 重构骨架与色值)、§Pattern 6(对比度算法矩阵与测试锚点)、§Common Pitfalls Pitfall 2(.dark 源顺序)
    - 02-UI-SPEC.md §Color(60/30/10 色表:暗色锁定值 + 亮色 D-07 起点值 + 功能色)
    - 02-CONTEXT.md D-07/D-08/D-09/D-10/D-12/D-20
  </read_first>
  <reversibility rating="costly">语义层变量名是六件套组件与全部存量页面的消费契约(D-09);本任务一次定名,Phase 5 批量工具接入后改名需触及所有消费方。</reversibility>
  <action>
    重构 src/styles/tokens.css(D-09 三层 + D-12 机制性搬迁,暗色值一字不动):
    1. 保留 @import "tailwindcss" 与 @custom-variant dark (&:where(.dark, .dark *))(现 :4-6,原样不动)。
    2. @theme 内重组并注释分层:①原语层——--color-neon-cyan: oklch(0.85 0.16 195)、--color-neon-magenta: oklch(0.72 0.24 330)、--color-neon-yellow: oklch(0.92 0.19 105),注释标注「原语层,不直接用于页面(D-09)」,值一字不动(Phase 1 D-04/D-12);②语义层·亮色(:root 默认,D-07 浅冷灰底起点值)——--color-bg: oklch(0.97 0.008 260)、--color-surface: oklch(0.99 0.004 260)、--color-surface-raised: oklch(0.94 0.01 260)、--color-text-primary: oklch(0.24 0.02 260)、--color-text-muted: oklch(0.46 0.02 260)、--color-border: oklch(0.88 0.01 260)(border 亮色值为 executor 起点,可在 D-07 策略内微调,RESEARCH Open Question 4);③D-10 功能色亮色——--color-danger: oklch(0.50 0.19 25)、--color-success: oklch(0.45 0.12 150)(RESEARCH 实跑 6.45:1)、--color-warning: oklch(0.42 0.11 85)(实跑 7.79:1),并补三组背景/边框变体 --color-danger-bg/--color-danger-border(--color-success-bg/-border、--color-warning-bg/-border 同型;变体值 executor 定:背景变体用同色相极浅低饱和、边框变体用中强度,均不作文字用途不受 4.5:1 约束);④--font-mono 保留现值逐字不动;⑤glow 亮色降强度(D-08)——--shadow-glow-cyan: 0 0 12px oklch(0.42 0.10 230 / 0.18)、--shadow-glow-magenta: 0 0 12px oklch(0.40 0.16 330 / 0.16)(深霓虹血统+透明度约减半)。
    3. @theme 之后新增普通 .dark 块(必须置于 @theme 之后——与 :root 特异性同为 (0,1,0) 靠源顺序覆盖,RESEARCH Pitfall 2):暗色语义六值逐字搬迁自现 :15-20(bg oklch(0.16 0.02 260) / surface oklch(0.2 0.025 260) / surface-raised oklch(0.24 0.03 260) / text-primary oklch(0.93 0.01 260) / text-muted oklch(0.65 0.02 260) / border oklch(0.3 0.03 260),D-12 一字不动);暗色功能色 --color-danger: oklch(0.65 0.19 25)、--color-success: oklch(0.75 0.15 150)、--color-warning: oklch(0.80 0.15 85)(UI-SPEC 起点)+ 三组背景/边框变体;暗色 glow 原值逐字搬迁自现 :26-27(--shadow-glow-cyan: 0 0 12px oklch(0.85 0.16 195 / 0.35)、--shadow-glow-magenta: 0 0 12px oklch(0.72 0.24 330 / 0.3));块内加 .dark { color-scheme: dark; }。
    4. 文件末尾(所有块之后)加 :root { color-scheme: light; }——亮色兜底声明,配合 plan 02-02 的 JS 双写与预渲染 .dark 默认类。
    5. 组件层(D-09「仅组件需要时建」):声明 --color-focus: var(--color-neon-cyan)(注释:焦点环语义角色,D-19/D-09,亮暗同值;base.css 与六件套焦点环一律消费 var(--color-focus),页面/组件不直连原语)。
    新建 src/styles/tokens.ts(D-20 色值单点):头注释引 D-20/D-09 与数据来源(RESEARCH §Pattern 1/§Pattern 6),声明本模块是 tokens.test.ts 与 cm-theme(plan 02-05)的共同字面值来源、禁止引第三方色彩库(Don't Hand-Roll);导出嵌套常量 tokens:{ neon: { cyan, magenta, yellow } }(暗色霓虹原语值)、{ dark: { bg, surface, surface-raised, 'text-primary', 'text-muted', border, danger, success, warning } }、{ light: { 同上八键 + 'neon-cyan-deep', 'neon-magenta-deep', 'neon-yellow-deep' } }——字符串值与 tokens.css 对应声明逐字一致(含 oklch(...) 全串);light 三色深霓虹取 D-07 起点值:cyan-deep oklch(0.42 0.10 230)、magenta-deep oklch(0.40 0.16 330)、yellow-deep oklch(0.40 0.10 70)(UI-SPEC Color 表);导出 parseOklch(input: string): { l: number; c: number; h: number } 纯函数(正则解析,供测试用);导出相关 TS 类型。
    新建 src/styles/tokens.test.ts(D-20 改色即红双保险):①实现 WCAG 对比度算法——oklch→OKLab(a=c·cos h, b=c·sin h)→LMS′ 矩阵→立方→LMS→线性 sRGB 矩阵(bottosson 公有领域系数,RESEARCH §Pattern 6 全矩阵)→gamma(c≤0.0031308 ? 12.92c : 1.055c^(1/2.4)−0.055)→相对亮度(c≤0.03928 ? c/12.92 : ((c+0.055)/1.055)^2.4,L=0.2126R+0.7152G+0.0722B)→(L1+0.05)/(L2+0.05);②锚点断言:黑(#000)/白(#fff)对比度 = 21.00:1 精确匹配(RESEARCH 实跑验证的算法正确性哨兵,用 toBeCloseTo 21, 2);③色对断言(≥4.5:1,describe 按主题分组):暗色 (text-primary,bg)、(text-muted,bg)、(text-primary,surface)、(text-muted,surface)、(neon.cyan,bg)、(neon.magenta,bg)、(neon.yellow,bg)、(danger,surface)、(success,surface)、(warning,surface);亮色前四对镜像 + ('neon-cyan-deep',bg)、('neon-magenta-deep',bg)、('neon-yellow-deep',bg)、(danger,surface)、(success,surface)、(warning,surface);④tokens.css↔tokens.ts 同步断言:readFileSync(new URL('./tokens.css', import.meta.url), 'utf8') 后 toContain(`--color-bg: ${tokens.dark.bg};`) 等全部语义变量(亮值与暗值都断),并断言 tokens.css 中 .dark 块位置在 @theme 之后(indexOf 顺序检查,防 Pitfall 2 复发)、含 color-scheme 双声明;失败消息自带修复指引(中文提示「改 CSS 未改 TS 或反之——色值单点在 tokens.ts,同步后重跑」)。
    若某起点色对实测不达标:仅允许在 D-07 策略内(加深降饱和)微调亮色值;暗色值不得动(D-12);微调后 tokens.css/tokens.ts/断言三处同步,重跑至绿。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/styles/tokens.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行出现 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - tokens.css:@theme 内含原语三色(值与现状逐字一致)+ 亮色语义六值 + D-10 功能色亮值与三组变体;其后存在 .dark 块含暗色语义六值(与现 :15-20 逐字一致)+ 暗色功能色与变体 + 暗色 glow 两变量(与现 :26-27 逐字一致);文件含 --color-focus: var(--color-neon-cyan) 组件层声明与 :root/.dark 的 color-scheme 两条声明
    - tokens.ts:导出 tokens(含 neon/dark/light 三组)与 parseOklch;light 组含三个 *-deep 键
    - tokens.test.ts:含黑/白 21:1 锚点用例、暗色与亮色两组色对用例(合计 ≥16 对)、tokens.css 同步断言用例、.dark 源顺序断言用例
    - corepack pnpm vitest run src/styles/tokens.test.ts 全绿;corepack pnpm type-check 退出码 0
  </acceptance_criteria>
  <done>三层令牌亮暗双套就位;暗色值零变化(D-12);对比度与同步双保险单测跑进现有 vitest 命令且全绿——改任意一侧色值即红。</done>
</task>

<task type="auto">
  <name>Task 2: base.css 全局基线 — 16px 兜底 + reduced-motion 中和 + 焦点环兜底 + 主题瞬切规则</name>
  <files>src/styles/base.css, src/styles/base-rules.test.ts</files>
  <read_first>
    - src/styles/base.css(现状全文——html 规则 :9-12 与 button,input,select,textarea 块 :54-60 是增改锚点)
    - src/styles/tokens.css(Task 1 产物——color-scheme 动态接管方与 --color-focus 消费源)
    - 02-RESEARCH.md §Common Pitfalls Pitfall 4(iOS 16px 三选择器一体)、§Pattern 1(color-scheme 由 CSS 兜底)、§Pitfall 3(theme-switching)、Code Examples(base.css 断言骨架)
    - 02-CONTEXT.md D-11/D-19/D-21/D-22
  </read_first>
  <action>
    增改 src/styles/base.css:
    1. 删除 html 规则(:9-12)中的硬编码 color-scheme 声明——主题颜色方案改由 tokens.css 的 :root/.dark 动态接管(Task 1 已声明,双源会让 :root 与 html 打架),html 规则保留 -webkit-text-size-adjust: 100%。
    2. 新增 D-21 兜底规则(放在既有 button, input, select, textarea { font: inherit; color: inherit; } 块附近,注释引 D-21/Pitfall 4):选择器组 input, select, textarea 一条规则声明 font-size: 16px——三选择器一体适用(iOS 按最终渲染 computed font-size 判定);不扩展 [contenteditable](RESEARCH Open Question 3:按 D-21 原文三选择器执行)。
    3. 新增 D-22 reduced-motion 全局中和块(注释引 D-22,注明 Phase 6 装饰动画天然被覆盖):@media (prefers-reduced-motion: reduce) 内 *,*::before,*::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important; }。
    4. 新增 D-19 全局焦点兜底::focus-visible { outline: 2px solid var(--color-focus); outline-offset: 2px; }(经组件层 --color-focus 消费,不直连原语——D-09;组件外的可交互元素也获得可见焦点)。
    5. 新增 D-11/Pitfall 3 主题瞬切 CSS 侧:.theme-switching, .theme-switching *, .theme-switching *::before, .theme-switching *::after { transition: none !important; }(注释写明:JS 侧在 src/composables/useThemeMode.ts 切换 watch 内挂类、双 rAF 后移除,两处注释互指;此规则只服务主题瞬切,组件级 hover 过渡照常工作)。
    新建 src/styles/base-rules.test.ts(内容级断言,先例 = scripts/check-chunks.mjs 的 readFileSync+正则形态;失败消息自带修复指引):readFileSync(new URL('./base.css', import.meta.url), 'utf8') 后断言:①/input\s*,\s*select\s*,\s*textarea\s*\{[^}]*font-size:\s*16px/(D-21);②/@media\s*\(prefers-reduced-motion:\s*reduce\)[\s\S]*animation-duration:\s*0\.01ms/ 且同一媒体块内含 transition-duration(D-22);③/:focus-visible[\s\S]{0,200}?outline[\s\S]{0,200}?var\(--color-focus\)(D-19 经组件层);④/\.theme-switching[^{]*\{[^}]*transition:\s*none\s*!important/(D-11 配对);⑤html 规则不再含硬编码颜色方案声明(expect(css).not.toMatch(/html\s*\{[^}]*color-scheme/))。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/styles/base-rules.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N(任一内容断言红即失败)</fails_when>
  </verify>
  <acceptance_criteria>
    - base.css:五处增改全部落地(删硬编码颜色方案 / 16px 三选择器兜底 / reduced-motion 块 / :focus-visible 焦点环 / .theme-switching 中和)
    - base-rules.test.ts:五条断言用例齐全且全绿
    - 既有 vitest 全量无回归(执行者自跑 corepack pnpm vitest run 确认 86+ 测试绿)
  </acceptance_criteria>
  <done>16px 兜底、reduced-motion 一刀切、全局焦点环、主题瞬切规则全部落地并由内容级测试守护;html 颜色方案单源化(动态接管)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 构建期令牌值 → CSS 产物 | 纯常量,无用户输入跨越本边界;风险仅值为内部漂移 |
| tokens.ts → cm-theme(plan 02-05 消费) | 字面值单点扩散,同步由 tokens.test.ts 守护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | tokens.css ↔ tokens.ts 值漂移 | low | mitigate | tokens.test.ts 同步断言(改色即红双保险,D-20) |
| T-02-02 | Tampering | 语义层被绕过直连原语 | low | mitigate | D-09 消费契约写入 tokens.css/base.css 注释 + code review;纯 CSS 常量无注入面 |
| T-02-SC | Tampering | npm/pnpm 包安装 | high | mitigate | 本计划零安装;全阶段仅移除 oneDark(plan 02-05);禁止 pnpm update 保持 lockfile 现状(RESEARCH Package Legitimacy Audit 纪律) |
</threat_model>

<verification>
- corepack pnpm vitest run src/styles/tokens.test.ts src/styles/base-rules.test.ts 全绿(对比度/同步/16px/reduced-motion/焦点环/瞬切)
- corepack pnpm type-check && corepack pnpm lint 退出码 0
- corepack pnpm build && corepack pnpm check:chunks 退出码 0(Tailwind 4 编译新令牌无错、chunk 预算不变)
- 暗色语义六值与 glow 暗值与重构前逐字一致(D-12,人工对照现 :15-20/:26-27)
</verification>

<success_criteria>
- 三层令牌体系(@theme 原语+亮色语义 / .dark 暗色语义 / 组件层 --color-focus)就位,D-10 功能色亮暗双套含背景/边框变体,D-08 glow 亮色降强度
- tokens.test.ts 黑白锚点 21:1 精确匹配、16+ 色对双主题 ≥4.5:1、CSS↔TS 同步断言生效(改色即红)
- base.css 四条全局规则(16px/reduced-motion/focus-visible/theme-switching)落地并有内容级测试
- 后续计划(02-02/03/04/05)可直接消费 var(--color-*)、var(--color-focus) 与 tokens.ts 字面值
</success_criteria>

<output>
Create `.planning/phases/02-design-system-a11y-baseline/02-01-SUMMARY.md` when done
</output>
