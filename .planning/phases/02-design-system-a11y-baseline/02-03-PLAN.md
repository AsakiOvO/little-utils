---
phase: 02-design-system-a11y-baseline
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ui/Button.vue
  - src/ui/Button.test.ts
  - src/ui/Input.vue
  - src/ui/Input.test.ts
  - src/ui/Card.vue
  - src/ui/Card.test.ts
autonomous: true
requirements: [SITE-05, STYL-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Input 错误态:error prop → danger 边框/文案 + aria-invalid + aria-describedby 关联(UI-SPEC error 行;错误文案由调用方传入,组件不内置文案)"
    - "六件套默认态即 UI-SPEC §Color/§Typography/§Spacing 定义的常态(populated 行;本计划覆盖 Button/Input/Card)"
    - "Input/textarea 变体最终渲染字号 16px、交互件 44px 触控目标(D-21 组件默认层)"
    - "Card/Input 为受控展示容器,partial 数据形态由调用方内容决定,组件层无 partial 分支(UI-SPEC partial 行)"
    - statement: "Button 长文案换行不截断(min-height 44px 保底)——组件测试断言"
      verification: backstop
    - statement: "Card 描述自然换行(无 nowrap/截断)——组件测试断言"
      verification: backstop
  artifacts:
    - path: src/ui/Button.vue
      provides: 交互按钮组件(outline/ghost 两态 + 44px + disabled 语义)
    - path: src/ui/Button.test.ts
      provides: variant/disabled/slot/长文案 backstop 断言
    - path: src/ui/Input.vue
      provides: 输入组件(textarea 变体 + label 关联 + v-model + 错误态 aria)
    - path: src/ui/Input.test.ts
      provides: label 关联/v-model/错误态/16px+44px 源码级断言
    - path: src/ui/Card.vue
      provides: 卡片容器(as 动态标签 + padding 可关 + 默认槽)
    - path: src/ui/Card.test.ts
      provides: as 透传/padding/长文案 backstop 断言
  key_links:
    - from: src/ui/Input.vue
      to: src/styles/tokens.css
      via: 语义令牌消费(D-09/D-10)
      pattern: "var"
    - from: base.css 与组件 focus 边框
      to: tokens.css 组件层
      via: var(--color-focus) 转发(不直连原语)
      pattern: "color-focus"
  prohibitions:
    - "基础组件不得携带 glow 装饰或强风格效果(工具操作区克制;D-08 glow 仅限品牌/装饰层)"
  assumptions:
    - "未决假设(assumption): SITE-05 探针分类 unclassified — 按 D-21 组件默认层(16px/44px)执行;全局兜底层在 plan 02-01"
    - "未决假设(assumption): STYL-03 探针分类 unclassified — 组件保持中性克制,transition 仅 hover 用途且受 reduced-motion 全局中和覆盖(plan 02-01 Task 2)"
---

## Phase Goal

**As a** 任意设备与亮暗主题下的站点用户, **I want to** 站点以统一霓虹设计令牌呈现、暗色/亮色可切换且被记住、移动端输入不缩放、动效可随系统降级, **so that** 我能在任何环境下舒适使用工具,工具操作区保持克制易读。

> 用户故事自 ROADMAP **Goal:** 行逐句映射(原文为结果式散文,非标准故事格式——映射未增删任何能力,已在规划返回中向作者披露)。

<objective>
交付六件套的第一组垂直切片:Button(outline/ghost)、Input(textarea 变体 + label/错误态 a11y + v-model)、Card(动态标签容器),全部 Tailwind-only SFC、语义令牌消费、内置 a11y 与 16px/44px 组件默认层,各自带就近组件测试。

Purpose: 这三个组件是 plan 02-06/02-07 存量迁移(ToolCard/home/not-found/json-formatter)的消费前提,也是 Phase 5 七个批量工具「拿来即用」契约(D-14 costly)的实体。
Output: src/ui/{Button,Input,Card}.vue + 三个就近 .test.ts。
</objective>

## Artifacts this phase produces

新建:`src/styles/tokens.ts`、`src/styles/tokens.test.ts`、`src/styles/base-rules.test.ts`、`src/composables/useThemeMode.ts`(+`.test.ts`)、`src/ui/Button.vue` / `Input.vue` / `Card.vue` / `Tabs.vue` / `CopyableText.vue` / `ThemeToggle.vue`(各配就近 `.test.ts`)、`src/tools/json-formatter/components/cm-theme.ts`。
修改:`src/styles/tokens.css`、`src/styles/base.css`、`index.html`、`vite.config.ts`、`package.json`/`pnpm-lock.yaml`(仅移除 oneDark)、`src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue`、`src/pages/home.vue`、`src/pages/not-found.vue`、`src/components/ToolCard.vue`、`src/tools/json-formatter/components/CodeMirrorJson.vue`、`src/tools/json-formatter/json-formatter.vue`。
新增符号:`tokens`/`parseOklch`、`THEME_STORAGE_KEY`/`useThemeMode`/`ThemePreference`/`ResolvedTheme`、`createCmTheme`、六件套组件名。

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-a11y-baseline/02-CONTEXT.md
@.planning/phases/02-design-system-a11y-baseline/02-RESEARCH.md
@.planning/phases/02-design-system-a11y-baseline/02-PATTERNS.md
@.planning/phases/02-design-system-a11y-baseline/02-UI-SPEC.md
@src/ui/safe-html.vue
@src/ui/safe-html.test.ts
@src/components/ToolCard.test.ts
@src/styles/tokens.css
@src/styles/base.css
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Button 组件 — variant 契约 + 44px + disabled 语义</name>
  <files>src/ui/Button.vue, src/ui/Button.test.ts</files>
  <read_first>
    - src/ui/safe-html.vue(形态先例:template 前 script 后、无 style 块、文件头决策注释)
    - 02-PATTERNS.md 六件套段(defineOptions/withDefaults 形态、tool.layout 交互件样式基线)
    - 02-UI-SPEC.md §Component Inventory(Button 行:44px/焦点环/语义令牌)、§Typography(weights 仅 400+600)
    - 02-CONTEXT.md D-13/D-14/D-16/D-18/D-19/D-21
    - src/styles/tokens.css 与 src/styles/base.css(plan 02-01 产物:语义令牌、:focus-visible 焦点环、reduced-motion 覆盖)
    - src/tools/json-formatter/json-formatter.vue :20-45(现有按钮——variant 设计的迁移对象,保留其 disabled/hover 语义)
  </read_first>
  <reversibility rating="costly">Button 的 props/variant 契约是 Phase 5 七个批量工具的向后兼容面(D-14);本任务一次定形。</reversibility>
  <action>
    新建 src/ui/Button.vue:defineOptions({ name: 'Button' });props 用 withDefaults(defineProps 泛型)——variant?: 'outline' | 'ghost' 默认 'outline',size?: 'md' 默认 'md'(D-16 API 留位,不实现多尺寸);默认槽渲染文案;根元素 button 且显式 type="button"(attrs 可覆盖为 submit);outline 类:inline-flex items-center justify-center gap-1.5 min-h-11 rounded-md border border-[var(--color-border)] px-4 text-sm text-[var(--color-text-primary)] transition-colors hover:border-[var(--color-neon-cyan)] hover:text-[var(--color-neon-cyan)] disabled:cursor-not-allowed disabled:opacity-40(对齐 json-formatter 现有按钮与 tool.layout 返回键的视觉语言);ghost 类:inline-flex items-center justify-center gap-1.5 min-h-11 rounded-md px-3 text-sm text-[var(--color-text-muted)] transition-colors hover:bg-[var(--color-surface-raised)] hover:text-[var(--color-text-primary)] disabled:cursor-not-allowed disabled:opacity-40;不加任何 weight 类(继承 400——UI-SPEC weights 仅 400/600);hover 过渡为组件级用途(D-11 不受限,且受 base.css reduced-motion 中和覆盖);焦点环依赖 base.css 全局 :focus-visible(消费 var(--color-focus)),组件不重复声明;全 Tailwind utility 无 style 块;禁 glow 类(D-08);头注释引 D-13/D-14/D-16/D-18/D-19/D-21。
    新建 src/ui/Button.test.ts(happy-dom,沿用 ToolCard.test.ts 的 mount helper/工厂纪律):①根为 button 且默认 type="button";②默认 variant 含 min-h-11 与 border-[var(--color-border)];③variant='ghost' 不含 border-[var(--color-border)] 且含 hover:bg-[var(--color-surface-raised)];④传 disabled 后按钮具备 disabled 属性且类含 disabled:opacity-40;⑤slot 文案完整渲染;⑥backstop——传入 80+ 字长文案,mount 后 wrapper.text() 完整包含该文案且根类不含 truncate 与 whitespace-nowrap(min-height 44px 保底换行不截断,verification: backstop)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/Button.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - Button.vue:variant/size 两 props(withDefaults 默认值)、min-h-11、disabled 样式类、无 style 块、无 glow 类
    - Button.test.ts ≥6 用例含 backstop 长文案用例,全绿
    - 源码级断言代替布局计算(Pitfall 5)
  </acceptance_criteria>
  <done>Button 契约(两 variant + 44px + disabled)落地并被组件测试锁定,可被 plan 02-06/02-07 迁移消费。</done>
</task>

<task type="auto">
  <name>Task 2: Input 组件 — textarea 变体 + label 关联 + v-model + 错误态 aria</name>
  <files>src/ui/Input.vue, src/ui/Input.test.ts</files>
  <read_first>
    - 02-PATTERNS.md 六件套段(CodeMirrorJson.vue 的 defineProps/defineEmits 签名 = v-model 契约形态;JsonTree withDefaults 形态)
    - 02-UI-SPEC.md §Component Inventory(Input 行)、§Copywriting Contract(Input 错误态行:文案由调用方传 error prop,推荐格式「{问题说明},请{修正动作}」)
    - 02-CONTEXT.md D-13/D-16/D-18/D-19/D-21 与 D-10(错误态消费 danger 语义色)
    - src/tools/json-formatter/json-formatter.vue :53-62(role="alert" 错误卡——错误态 a11y 先例)
    - src/styles/tokens.css(plan 02-01 产物:--color-danger 等)
  </read_first>
  <action>
    新建 src/ui/Input.vue:defineOptions({ name: 'Input' });props——modelValue: string、label?: string、error?: string、multiline?: boolean、size?: 'md'(D-16 留位);emits (e: 'update:modelValue', v: string): void(PATTERNS v-model 签名形态);id 用 Vue 3.5 useId()(SSG 安全,无第三方依赖);label prop 存在时渲染 <label :for="id">,类 text-sm text-[var(--color-text-muted)];控件:m multiline 为真渲染 textarea(:id rows="4")否则 input(:id,attrs 透传 type 默认 text);控件类:min-h-11 w-full rounded-md border bg-[var(--color-surface)] px-3 py-2.5 text-base text-[var(--color-text-primary)] transition-colors placeholder:text-[var(--color-text-muted)] focus:outline-none focus:border-[var(--color-focus)](text-base = 16px,D-21 组件默认;focus 边框经组件层 --color-focus,D-09 不直连原语);错误态:error 非空时边框换 border-[var(--color-danger)](D-10)、aria-invalid="true"、aria-describedby 指向 `${id}-error` 并渲染 <p :id="`${id}-error`" class="text-sm text-[var(--color-danger)]">{{ error }}</p>;无 error 时不渲染错误节点、无 aria-invalid/aria-describedby;v-model::value/:checked 不需要——input 用 :value="modelValue" + @input="emit('update:modelValue', target.value)",textarea 同型;头注释引 D-13/D-16/D-18/D-19/D-21/D-10。
    新建 src/ui/Input.test.ts:①label 关联——传 label 后 label 元素 for 等于控件 id;②v-model——setValue 触发后 emitted('update:modelValue') 携带新值;③multiline → 渲染 textarea 否则 input;④error → 控件 aria-invalid="true"、aria-describedby 指向的节点渲染 error 文案、控件类含 border-[var(--color-danger)];⑤无 error → 无 aria-invalid;⑥源码级:控件类含 text-base 与 min-h-11(16px/44px,Pitfall 5);⑦size prop 留位(withDefaults 默认 'md')。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/Input.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - Input.vue:label for/id 关联、v-model 契约、multiline 变体、错误态 aria-invalid+aria-describedby+danger 边框、16px/44px 默认
    - 错误文案零内置(仅渲染调用方 error prop——UI-SPEC 契约)
    - Input.test.ts ≥7 用例全绿
  </acceptance_criteria>
  <done>Input 契约落地:a11y 三件套(label/错误态/焦点)齐备,Phase 5 表单类工具可直接消费。</done>
</task>

<task type="auto">
  <name>Task 3: Card 组件 — 动态标签容器(as/padding)</name>
  <files>src/ui/Card.vue, src/ui/Card.test.ts</files>
  <read_first>
    - 02-PATTERNS.md 六件套段(ToolCard.vue:3-6 / home.vue:12-20 卡片样式现状 = Card 的视觉起点)
    - 02-UI-SPEC.md §Component Inventory(Card 行)
    - 02-CONTEXT.md D-13/D-15(D-15 迁移将把 ToolCard/home 空态/not-found/json-formatter 卡片换为此组件)
    - src/components/ToolCard.test.ts(createRouter + mount helper 先例——as="RouterLink" 测试需要 router 上下文)
  </read_first>
  <action>
    新建 src/ui/Card.vue:defineOptions({ name: 'Card', inheritAttrs: true });props 用 withDefaults——as?: string 默认 'div'、padding?: boolean 默认 true;根 <component :is="as"> 渲染(attrs 如 to/href/class 自然透传到动态标签——Vue 原生 class 合并);基础类:rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)](PATTERNS 卡片语言)+ padding 为真时追加 p-5;默认槽;禁 glow;头注释引 D-13/D-15 与 UI-SPEC Card 行,注明 as='RouterLink' 用法供 ToolCard 迁移(plan 02-06)。
    新建 src/ui/Card.test.ts(沿用 ToolCard.test.ts 的 createRouter/createMemoryHistory helper):①默认渲染 div 且类含 rounded-lg/border-[var(--color-border)]/bg-[var(--color-surface)]/p-5;②as="RouterLink" + :to 传参(router 插件下)渲染为 a 且 href 正确;③padding=false 时不渲染 p-5(供 p-10/p-3 等定制场景);④默认槽内容渲染;⑤backstop——长描述文本(100+ 字)mount 后 wrapper.text() 完整包含且根类无 whitespace-nowrap/truncate(自然换行,verification: backstop)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/Card.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - Card.vue:as/padding 两 props、默认卡片语言四件类、attrs 透传、无 style 块
    - Card.test.ts ≥5 用例含 as=RouterLink 与 backstop 长文案,全绿
  </acceptance_criteria>
  <done>Card 容器契约落地;plan 02-06 可用 as="RouterLink" 迁移 ToolCard、用 padding=false 迁移 p-10/p-3 卡片。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 props/插槽 → 组件渲染 | 组件内容来自调用方(工具页未来传用户数据),渲染路径决定 XSS 面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | 六件套引入 v-html 捷径 | medium | mitigate | 三组件全部文本插值与 :prop 绑定渲染(零 v-html);ESLint vue/no-v-html 白名单(^sanitized)不变;测试断言插值转义 |
| T-02-SC | Tampering | npm/pnpm 包安装 | high | mitigate | 本计划零新装;六件套全部自研(无第三方 registry/block,UI-SPEC Registry Safety 表);禁止 pnpm update |
</threat_model>

<verification>
- corepack pnpm vitest run src/ui/Button.test.ts src/ui/Input.test.ts src/ui/Card.test.ts 全绿
- corepack pnpm type-check && corepack pnpm lint 退出码 0
- corepack pnpm vitest run 全量无回归(86+ 既有测试)
- 三个组件源码零 style 块、零 glow 类、零 v-html(源码级人工核对)
</verification>

<success_criteria>
- Button/Input/Card 三组件就位且 API 契约被组件测试锁定(D-13 六件套的前三件)
- 16px(D-21 组件默认)/44px 触控/焦点环/错误态 aria 全部内置(D-19)
- 迁移面就绪:Card 支持动态标签与 padding 关闭,Input 支持 v-model 与错误态,Button 支持 disabled 与两 variant
</success_criteria>

<output>
Create `.planning/phases/02-design-system-a11y-baseline/02-03-SUMMARY.md` when done
</output>

