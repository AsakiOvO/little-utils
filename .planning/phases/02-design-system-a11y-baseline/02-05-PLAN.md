---
phase: 02-design-system-a11y-baseline
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/tools/json-formatter/components/cm-theme.ts
  - src/tools/json-formatter/components/CodeMirrorJson.vue
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [STYL-01, SITE-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CodeMirror 编辑器亮暗双主题随站点主题切换:底色/选区/行高亮对齐 surface 语义令牌,语法高亮用深霓虹三色+中性色(D-06)"
    - "主题切换时编辑器经 Compartment reconfigure 切换,不重建视图(输入与光标状态无损)"
  artifacts:
    - path: src/tools/json-formatter/components/cm-theme.ts
      provides: createCmTheme(dark) 亮/暗 EditorView.theme + HighlightStyle 工厂(仅本工具目录可 import)
      exports: ["createCmTheme"]
    - path: src/tools/json-formatter/components/CodeMirrorJson.vue
      provides: oneDark 替换为 Compartment 双主题 + watch(resolved) reconfigure
      contains: "Compartment"
    - path: package.json
      provides: 移除 @codemirror/theme-one-dark(本阶段唯一 package.json 变更)
  key_links:
    - from: src/tools/json-formatter/components/cm-theme.ts
      to: src/styles/tokens.ts
      via: 字面 oklch 值消费(与 D-20 单测同源,非 var()——RESEARCH A3 已绕开)
      pattern: "tokens"
    - from: CodeMirrorJson.vue
      to: src/composables/useThemeMode.ts
      via: 消费 resolved + watch → compartment.reconfigure
      pattern: "useThemeMode"
  prohibitions:
    - "不得引入任何第三方 UI 组件库或新运行时依赖;唯一 package.json 变更 = 移除 oneDark(REQUIREMENTS Out of Scope + RESEARCH 零新依赖结论)"
    - "cm-theme.ts 只准放 json-formatter/components/ 内——共享层出现 CM 引用即违反 ARCH-02(check-chunks 断言首包可达集零 CM)"
  assumptions:
    - "未决假设(assumption): STYL-01 探针分类 unclassified — 编辑器配色对齐语义令牌按 D-06 执行"
    - "未决假设(assumption): SITE-04 探针分类 unclassified — 编辑器随主题切换按 D-06 执行"
---

## Phase Goal

**As a** 任意设备与亮暗主题下的站点用户, **I want to** 站点以统一霓虹设计令牌呈现、暗色/亮色可切换且被记住、移动端输入不缩放、动效可随系统降级, **so that** 我能在任何环境下舒适使用工具,工具操作区保持克制易读。

> 用户故事自 ROADMAP **Goal:** 行逐句映射(原文为结果式散文,非标准故事格式——映射未增删任何能力,已在规划返回中向作者披露)。

<objective>
D-06 落地:CodeMirror 6 编辑器从硬编码 oneDark 换为站点语义令牌对齐的亮/暗双主题——cm-theme.ts(EditorView.theme + HighlightStyle 打包进 Compartment)+ CodeMirrorJson.vue 经 useThemeMode resolved 驱动 reconfigure(不重建视图),并移除 oneDark 死依赖。

Purpose: 编辑器是 JSON 工具(站内最重交互面)的主题一致性最后一块;主题模块留在工具目录内是 check-chunks 分包纪律(ARCH-02)的既定约束。
Output: src/tools/json-formatter/components/cm-theme.ts(新)、CodeMirrorJson.vue(改)、package.json/pnpm-lock.yaml(移除 oneDark)。
</objective>

## Artifacts this phase produces

新建:`src/styles/tokens.ts`、`src/styles/tokens.test.ts`、`src/styles/base-rules.test.ts`、`src/composables/useThemeMode.ts`(+`.test.ts`)、`src/ui/Button.vue` / `Input.vue` / `Card.vue` / `Tabs.vue` / `CopyableText.vue` / `ThemeToggle.vue`(各配就近 `.test.ts`)、`src/tools/json-formatter/components/cm-theme.ts`。
修改:`src/styles/tokens.css`、`src/styles/base.css`、`index.html`、`vite.config.ts`、`package.json`/`pnpm-lock.yaml`(仅移除 oneDark)、`src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue`、`src/pages/home.vue`、`src/pages/not-found.vue`、`src/components/ToolCard.vue`、`src/tools/json-formatter/components/CodeMirrorJson.vue`、`src/tools/json-formatter/json-formatter.vue`。
新增符号:`tokens`/`parseOklch`、`THEME_STORAGE_KEY`/`useThemeMode`/`ThemePreference`/`ResolvedTheme`、`createCmTheme`、六件套组件名。

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-a11y-baseline/02-CONTEXT.md
@.planning/phases/02-design-system-a11y-baseline/02-RESEARCH.md
@.planning/phases/02-design-system-a11y-baseline/02-PATTERNS.md
@src/tools/json-formatter/components/CodeMirrorJson.vue
@src/styles/tokens.ts
@scripts/check-chunks.mjs
@package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: cm-theme 双主题模块 + CodeMirrorJson Compartment 切换</name>
  <files>src/tools/json-formatter/components/cm-theme.ts, src/tools/json-formatter/components/CodeMirrorJson.vue</files>
  <read_first>
    - src/tools/json-formatter/components/CodeMirrorJson.vue(现状全文:oneDark import :17、extensions 数组 :36-45、onMounted/onBeforeUnmount 生命周期、watch 单向同步)
    - 02-RESEARCH.md §Pattern 4(Compartment 双主题完整骨架:chrome spec/highlight spec/色值来源)、§Pitfall 8(泄漏面)
    - 02-PATTERNS.md cm-theme 段(懒加载边界头注释、EditorView.theme 现有用法、生命周期纪律)
    - src/styles/tokens.ts(plan 02-01 产物——字面 oklch 值单点,含 light 的三个 *-deep 键)
    - scripts/check-chunks.mjs :128-159(首包可达集零 CM 断言——确认主题模块归属约束)
    - 02-CONTEXT.md D-06/D-18;02-UI-SPEC.md §Color(Accent reserved:语法高亮深霓虹映射)
  </read_first>
  <action>
    新建 src/tools/json-formatter/components/cm-theme.ts:头注释逐字复制 CodeMirrorJson.vue:2-3 的懒加载边界声明(「CM 包仅被本组件静态 import → 只落进 json-formatter 工具 chunk;共享层出现任何引用即违反 ARCH-02」)并引 D-06;imports——EditorView(@codemirror/view)、HighlightStyle 与 syntaxHighlighting(@codemirror/language)、tags as t(@lezer/highlight)、tokens(from '../../../styles/tokens'——字面 oklch 字符串,非 var(),RESEARCH A3 已绕开 CSS 变量不确定性)。
    导出 createCmTheme(dark: boolean): Extension → 返回数组 [EditorView.theme(chrome(dark), { dark }), syntaxHighlighting(HighlightStyle.define(highlight(dark)))]:
    - chrome spec(dark 为真取 tokens.dark.*,为假取 tokens.light.*):'&' 背景backgroundColor surface + 文字 color text-primary;'.cm-gutters' 背景 surface + border 'none' + color text-muted;'.cm-activeLine' 背景 surface-raised;'.cm-selectionBackground, &.cm-focused .cm-selectionBackground, ::selection' 背景 surface-raised;'&.cm-focused' outline 'none'(焦点环交宿主容器 focus-visible——D-19 统一)。
    - highlight spec:tag propertyName → color = dark ? tokens.neon.cyan : tokens.light['neon-cyan-deep'];tag string → neon.magenta / light['neon-magenta-deep'];tag [number, bool, null] → neon.yellow / light['neon-yellow-deep'];tag [keyword, punctuation, operator] → text-muted(暗/亮各自值)——深霓虹三色 + 中性色,D-06 语法高亮契约。
    修改 src/tools/json-formatter/components/CodeMirrorJson.vue:①删除 import { oneDark } from '@codemirror/theme-one-dark'(:17);②新增 import { Compartment } from '@codemirror/state' 与 import { createCmTheme } from './cm-theme';③script setup 内消费 useThemeMode()(显式 import '../../../composables/useThemeMode'——CodeMirrorJson.vue 位于 src/tools/json-formatter/components/,需上溯三级到 src/,与 cm-theme.ts 引 '../../../styles/tokens' 同层级)解构 resolved,新增 const themeComp = new Compartment();④extensions 数组中 oneDark 原位置替换为 themeComp.of(createCmTheme(resolved.value === 'dark'))(初始主题对齐当前 resolved);⑤新增 watch(resolved, (mode) => { view?.dispatch({ effects: themeComp.reconfigure(createCmTheme(mode === 'dark')) }) })——Compartment reconfigure 不重建视图,输入/光标状态无损(D-06;先例 = 本文件既有 props.modelValue watch 的单向同步形态);⑥onMounted/onBeforeUnmount 生命周期纪律一行不动;⑦模板与其余扩展不动;渲染纪律/懒加载边界头注释保留并补一句「主题模块在 ./cm-theme.ts,仅本目录可 import」。
    注:工具目录导入共享 useThemeMode 合法(方向 = 工具→共享;违规方向是共享层引 CM),不改变 chunk 归属——check-chunks 断言口径不变。
  </action>
  <verify>
    <automated>corepack pnpm type-check && corepack pnpm vitest run</automated>
    <fails_when>任一命令非零退出码;type-check 输出 error TS(如仍引用已待移除的包路径);既有 86+ 测试出现 failed</fails_when>
  </verify>
  <acceptance_criteria>
    - CodeMirrorJson.vue 源码零 oneDark 引用;含 Compartment 声明与 createCmTheme 初始注入及 watch reconfigure
    - cm-theme.ts 仅存在于 json-formatter/components/ 下,色值全部取自 tokens.ts 字面值
    - 既有全量测试绿(服务/路由/组件回归无破)
  </acceptance_criteria>
  <done>编辑器亮暗双主题随站点主题切换,底色/语法高亮对齐语义令牌(D-06);视图不重建。</done>
</task>

<task type="auto">
  <name>Task 2: oneDark 死依赖移除 + 构建卡口复跑</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json(dependencies 内 @codemirror/theme-one-dark 条目)
    - 02-RESEARCH.md §Standard Stack Supporting 表与 §Installation(唯一 package.json 变更 = pnpm remove;禁止 pnpm update 纪律)、§Runtime State Inventory(移除后刷新 lockfile)
    - scripts/check-chunks.mjs(断言 c 要求存在含 CM 的懒加载 chunk——与 oneDark 无关,移除后应保持绿)
  </read_first>
  <action>
    执行 corepack pnpm remove @codemirror/theme-one-dark(自动同步更新 pnpm-lock.yaml;不要运行 pnpm update 或 pnpm install 拉新版——RESEARCH Package Legitimacy Audit 纪律:保持 lockfile 现状,避免无意拉入 too-new 版本)。移除后复跑构建卡口确认分包纪律不破:check-chunks 的入口页可达集零 CM 断言与「工具页可达集的 CM chunk 文件名以工具 slug 开头」断言均应保持绿(主题模块本就随工具 chunk)。
  </action>
  <verify>
    <automated>corepack pnpm build && corepack pnpm check:chunks</automated>
    <fails_when>构建非零退出码;check:chunks 输出 VIOLATION(如「首包可达集泄漏 codemirror 字样」或 chunk budget fail)</fails_when>
  </verify>
  <acceptance_criteria>
    - package.json 与 pnpm-lock.yaml 不再含该依赖;除此之外 package.json 零其它变更
    - corepack pnpm build && corepack pnpm check:chunks 退出码 0
  </acceptance_criteria>
  <done>死依赖清除;分包纪律卡口复绿(本阶段零新装纪律闭环)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokens.ts 常量 → CM 主题 spec | 字面值单点扩散,无用户输入;漂移由同源约束守护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Information Disclosure | CM 主题模块 | low | accept | 纯配色映射,无用户数据处理、无网络行为;新增输入面为零 |
| T-02-09 | Tampering | 主题模块被上移至共享层 | medium | mitigate | check-chunks 首包可达集零 CM 断言 + cm-theme.ts 头注释懒加载边界声明;type-check 下放错目录即 import 失败 |
| T-02-SC | Tampering | npm/pnpm 包安装 | high | mitigate | 零新装——唯一变更 = pnpm remove oneDark;禁止 pnpm update 保持 lockfile 现状(RESEARCH 纪律) |
</threat_model>

<verification>
- corepack pnpm type-check 退出码 0(oneDark 引用清除由类型系统确认)
- corepack pnpm vitest run 全量绿(86+ 既有测试)
- corepack pnpm build && corepack pnpm check:chunks 退出码 0(CM 仅在 json-formatter chunk、首包零 CM、预算通过)
- package.json 仅少一行依赖,lockfile 同步更新
</verification>

<success_criteria>
- 编辑器双主题(D-06)随主题切换瞬切,亮色配色对齐 D-07 深霓虹体系
- Compartment reconfigure 保持视图状态(输入/光标/滚动)
- oneDark 死依赖移除,零新装纪律闭环
</success_criteria>

<output>
Create `.planning/phases/02-design-system-a11y-baseline/02-05-SUMMARY.md` when done
</output>

