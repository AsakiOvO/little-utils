---
phase: 02-design-system-a11y-baseline
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ui/Tabs.vue
  - src/ui/Tabs.test.ts
  - src/ui/CopyableText.vue
  - src/ui/CopyableText.test.ts
autonomous: true
requirements: [SITE-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CopyableText 空内容 → 复制按钮禁用 + aria-label「暂无内容可复制」(UI-SPEC empty 行)"
    - "CopyableText 复制为同步快捷操作无 loading 态,copied 反馈即终态(UI-SPEC loading 行)"
    - "CopyableText 双层降级(API→execCommand)仍失败 → 「复制失败,请手动复制」(UI-SPEC error 行)"
    - "Tabs 超宽容器 overflow-x-auto 水平滚动不换行不截断,键盘导航时活动 tab scrollIntoView 保持焦点可见(UI-SPEC overflow 行)"
    - "Tabs 单 tab 照常渲染,aria 语义单/多 tab 一致(UI-SPEC zero-one-many 行)"
    - "CopyableText 与 Tabs 默认态即 UI-SPEC §Color/§Typography/§Spacing 定义的常态(populated 行;本计划覆盖六件套之 CopyableText/Tabs)"
    - statement: "CopyableText 展示区限高内部滚动但复制全文(复制不截断于可见区)——组件测试断言"
      verification: backstop
  artifacts:
    - path: src/ui/CopyableText.vue
      provides: 复制展示组件(封装 useCopy + 复制按钮 + 成功/失败/空内容三态反馈)
    - path: src/ui/CopyableText.test.ts
      provides: 空内容禁用/成功反馈/失败反馈/复制全文 backstop 断言
    - path: src/ui/Tabs.vue
      provides: WAI-ARIA 标签页(roving tabindex + 方向键 + hidden 面板)
    - path: src/ui/Tabs.test.ts
      provides: roving/键盘导航/hidden/单 tab/overflow 断言
  key_links:
    - from: src/ui/CopyableText.vue
      to: src/composables/useCopy.ts
      via: 内部消费 useCopy()(D-17 能力层/展示层分离,禁重写剪贴板逻辑)
      pattern: "useCopy"
    - from: src/ui/Tabs.vue
      to: WAI-ARIA tabs 模式
      via: 单一 selectTab() 驱动 aria-selected/tabindex/hidden 三件套(RESEARCH Pattern 5)
      pattern: "selectTab"
  prohibitions:
    - "面板隐藏不得使用 opacity/移出屏幕(屏幕阅读器仍可达,违反 4.1.2/1.3.1)——必须 hidden 属性(RESEARCH Anti-Patterns)"
  assumptions:
    - "未决假设(assumption): SITE-05 探针分类 unclassified — 按 D-21(交互件 44px)与 D-19(Tabs 键盘可达)执行"
---

## Phase Goal

**As a** 任意设备与亮暗主题下的站点用户, **I want to** 站点以统一霓虹设计令牌呈现、暗色/亮色可切换且被记住、移动端输入不缩放、动效可随系统降级, **so that** 我能在任何环境下舒适使用工具,工具操作区保持克制易读。

> 用户故事自 ROADMAP **Goal:** 行逐句映射(原文为结果式散文,非标准故事格式——映射未增删任何能力,已在规划返回中向作者披露)。

<objective>
交付六件套的第二组垂直切片:CopyableText(封装 useCopy 的复制展示组件,带成功/失败/空内容三态反馈)与 Tabs(WAI-ARIA 完整键盘导航),各带就近组件测试。

Purpose: CopyableText 是「输出类工具一键复制」站点纪律(SITE-06 前置)的标准展示层,Tabs 服务 Phase 4/5 的分组 UI;两者把 useCopy 能力层与 a11y 键盘模式固化为可复用契约。
Output: src/ui/{CopyableText,Tabs}.vue + 两个就近 .test.ts。
</objective>

## Artifacts this phase produces

新建:`src/styles/tokens.ts`、`src/styles/tokens.test.ts`、`src/styles/base-rules.test.ts`、`src/composables/useThemeMode.ts`(+`.test.ts`)、`src/ui/Button.vue` / `Input.vue` / `Card.vue` / `Tabs.vue` / `CopyableText.vue` / `ThemeToggle.vue`(各配就近 `.test.ts`)、`src/tools/json-formatter/components/cm-theme.ts`。
修改:`src/styles/tokens.css`、`src/styles/base.css`、`index.html`、`vite.config.ts`、`package.json`/`pnpm-lock.yaml`(仅移除 oneDark)、`src/layouts/home.layout.vue`、`src/layouts/tool.layout.vue`、`src/pages/home.vue`、`src/pages/not-found.vue`、`src/components/ToolCard.vue`、`src/tools/json-formatter/components/CodeMirrorJson.vue`、`src/tools/json-formatter/json-formatter.vue`。
新增符号:`tokens`/`parseOklch`、`THEME_STORAGE_KEY`/`useThemeMode`/`ThemePreference`/`ResolvedTheme`、`createCmTheme`、六件套组件名。

<execution_context>
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/workflows/execute-plan.md
@/Users/estrella/Code/demo/little-utils/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-a11y-baseline/02-CONTEXT.md
@.planning/phases/02-design-system-a11y-baseline/02-RESEARCH.md
@.planning/phases/02-design-system-a11y-baseline/02-PATTERNS.md
@.planning/phases/02-design-system-a11y-baseline/02-UI-SPEC.md
@src/composables/useCopy.ts
@src/composables/useCopy.test.ts
@src/ui/safe-html.vue
@src/tools/json-formatter/json-formatter.vue
</context>

<tasks>

<task type="tracer">
  <name>Task 1: CopyableText — useCopy 展示层封装 + 三态反馈</name>
  <files>src/ui/CopyableText.vue, src/ui/CopyableText.test.ts</files>
  <read_first>
    - src/composables/useCopy.ts(D-17 必须封装复用的能力层——copy/copied/isSupported,copiedDuring 1500ms)
    - src/composables/useCopy.test.ts(剪贴板 mock 注入模式:patchNavigator + stubGlobal ClipboardItem + fake timers 成对)
    - 02-PATTERNS.md 六件套段(json-formatter.vue:77-89 复制按钮 + :171-182 lastCopiedKey 驱动 = CopyableText 的行为模板)
    - 02-UI-SPEC.md §Copywriting Contract(CopyableText 四行:按钮 aria-label 复制内容/成功 已复制/失败 复制失败,请手动复制/空内容 暂无内容可复制)
    - 02-CONTEXT.md D-13/D-17/D-18/D-19;02-RESEARCH.md §Security V5(展示区禁 v-html)与 §Pitfall 5
  </read_first>
  <action>
    新建 src/ui/CopyableText.vue:defineOptions({ name: 'CopyableText' });props 用 withDefaults——text: string(必传,复制 payload 与展示内容)、label?: string 默认 '复制内容'(按钮 aria-label 可定制);内部 const { copy, copied } = useCopy()(D-17:复制逻辑零重写);failed = ref(false)——onClick 内 try { await copy(props.text) } catch { failed.value = true }(useClipboard legacy:true 双层降级仍失败时进入失败态)。
    结构(根 flex flex-col gap-2,全 Tailwind 无 style 块,头注释引 D-13/D-17/D-18/D-19):①展示区 = 默认插槽,无插槽时 fallback <pre class="max-h-72 overflow-auto whitespace-pre font-mono text-sm break-words text-[var(--color-text-primary)]">{{ text }}</pre>(限高内部滚动——复制仍传 props.text 全文,backstop 语义;展示区一律 {{ }} 插值,禁 v-html,RESEARCH V5);②按钮行 <button type="button" class="self-start inline-flex items-center gap-1 min-h-11 rounded-md border px-3 text-sm transition-colors"(44px,D-21)+ 状态类:常态 border-[var(--color-border)] text-[var(--color-text-muted)] hover:border-[var(--color-neon-cyan)] hover:text-[var(--color-neon-cyan)],成功 copied 为真时 border-[var(--color-neon-cyan)] text-[var(--color-neon-cyan)](沿用 json-formatter 复制反馈语言)>;按钮内图标:@lucide/vue 按需导入 Copy 与 Check(导出名已核实),copied 为真渲染 Check 否则 Copy,图标 aria-hidden="true";空内容(props.text 为空串)→ 按钮 :disabled="!text" 且 aria-label 换「暂无内容可复制」(UI-SPEC 空内容态);常态 aria-label = label prop;③反馈 span:常驻 DOM(class 常态 sr-only、成功/失败时切换为可见 text-sm),aria-live="polite"——成功渲染「已复制」(copiedDuring 1500ms 后随 useCopy copied 自动复原),失败渲染「复制失败,请手动复制」(text-[var(--color-danger)])。
    新建 src/ui/CopyableText.test.ts(剪贴板 mock 沿用 useCopy.test.ts 注入模式 + vi.useFakeTimers/useRealTimers 成对):①空 text → 按钮 disabled 且 aria-label 为「暂无内容可复制」;②有 text 点击 → mock 剪贴板写入成功 → 按钮类切成功态、反馈 span 显示「已复制」;advanceTimers 1500ms 后复原为常态(与 useCopy copiedDuring 同步);③copy 拒绝(stub copy 抛错)→ 反馈 span 显示「复制失败,请手动复制」;④backstop——传 1000+ 字文本,展示区类含 max-h-72 与 overflow-auto(限高滚动),点击复制后 mock 收到的 payload 等于全文(复制不截断于可见区,verification: backstop);⑤反馈 span 常驻且 aria-live="polite";⑥展示区渲染的 HTML 不含未转义标记(传含 <script> 的文本,断言 wrapper.find('script').exists() 为否——插值转义,V5)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/CopyableText.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - CopyableText.vue:内部 useCopy() 消费(零剪贴板 API 直调)、四态(常态/成功/失败/空内容)、展示区限高滚动、全插值渲染
    - 文案逐字对齐 UI-SPEC Copywriting Contract 四行
    - CopyableText.test.ts ≥6 用例含 backstop 全文复制,全绿
  </acceptance_criteria>
  <done>复制展示标准件就位:plan 02-07 迁移 json-formatter 两处复制按钮时直接替换,Phase 5 输出类工具拿来即用。</done>
</task>

<task type="auto">
  <name>Task 2: Tabs — WAI-ARIA roving tabindex + 方向键导航</name>
  <files>src/ui/Tabs.vue, src/ui/Tabs.test.ts</files>
  <read_first>
    - 02-RESEARCH.md §Pattern 5(Tabs 完整 WAI-ARIA 规范:role 三件套/roving tabindex/方向键/自动激活/hidden 面板/scrollIntoView)
    - 02-PATTERNS.md No Analog Found(Tabs roving tabindex 无库内先例——按 Pattern 5 规范落地)与 JsonTree 交互件先例
    - 02-UI-SPEC.md §Component Inventory(Tabs 行:方向键/roving/aria-tablist/活动态消费 accent)、§UI Considerations overflow/zero-one-many 行
    - 02-CONTEXT.md D-13/D-18/D-19/D-21
  </read_first>
  <action>
    新建 src/ui/Tabs.vue:defineOptions({ name: 'Tabs' });props——tabs: Array<{ id: string; label: string }>(必传)、modelValue?: string(活动 tab id,v-model)、ariaLabel?: string 默认 '标签页'(tablist 必需命名);emits update:modelValue;activeId = computed(() => props.modelValue ?? props.tabs[0]?.id);单一 selectTab(id)(D-19:aria-selected/tabIndex/panel hidden 三件套由这一个函数驱动,防状态漂移)——emit('update:modelValue', id);键盘 onKeydown(event, index):ArrowLeft/ArrowRight 循环移动((index±1+tabs.length) % tabs.length,水平布局)并 selectTab + 聚焦新 tab(自动激活模式,面板廉价默认)、Home/End 跳两端;键盘导航后活动 tab 元素 scrollIntoView({ block: 'nearest', inline: 'nearest' })(UI-SPEC overflow 行)。
    结构:①tablist <div role="tablist" :aria-label="ariaLabel" class="flex gap-1 overflow-x-auto border-b border-[var(--color-border)]">(超宽容器水平滚动不换行);每个 tab 是真实 <button type="button" role="tab" :id="`tab-${tab.id}`" :aria-selected="activeId === tab.id" :aria-controls="`panel-${tab.id}`" :tabindex="activeId === tab.id ? 0 : -1" @click="selectTab(tab.id)" @keydown="..." class="inline-flex min-h-11 items-center whitespace-nowrap px-3 text-sm transition-colors"(44px,D-21;roving tabindex 强制——仅活动 tab 可 Tab 聚焦)+ 活动态类 text-[var(--color-neon-cyan)] border-b-2 border-[var(--color-neon-cyan)]、非活动 text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)](活动指示消费 accent,UI-SPEC);②面板 v-for tab:<div :id="`panel-${tab.id}`" role="tabpanel" :aria-labelledby="`tab-${tab.id}`" :hidden="activeId !== tab.id" tabindex="0" class="pt-3"><slot :name="tab.id" /></div>——非激活面板用 hidden 属性(禁 opacity/移出屏幕,RESEARCH Anti-Patterns;面板常驻 DOM,aria-controls 引用有效),纯静态面板加 tabindex="0"(Pattern 5);头注释引 D-13/D-19 与 RESEARCH Pattern 5;全 Tailwind 无 style 块;显式导入(D-18)。
    新建 src/ui/Tabs.test.ts:①roving——两个以上 tab 时仅活动 tab tabindex=0 其余 -1;②aria-selected 与 activeId 对应;③点击 tab → emitted update:modelValue 携带该 id + 对应面板 hidden 翻转;④keydown.right → 选中移到下一个(循环,末尾回首个);⑤keydown.home/end → 跳首/尾;⑥单 tab(tabs 长度 1)照常渲染且 aria 语义一致(zero-one-many 行);⑦容器类含 overflow-x-auto(源码级,overflow 行);⑧非激活面板存在 hidden 属性(find('#panel-x').attributes 含 hidden,而非节点不存在)。
  </action>
  <verify>
    <automated>corepack pnpm vitest run src/ui/Tabs.test.ts && corepack pnpm type-check</automated>
    <fails_when>任一命令非零退出码;vitest 汇总行 0 passed 或 failed N;type-check 输出 error TS</fails_when>
  </verify>
  <acceptance_criteria>
    - Tabs.vue:role=tablist/tab/tabpanel 三件套、roving tabindex、ArrowLeft/Right/Home/End、自动激活、hidden 属性、scrollIntoView
    - 活动态指示消费 var(--color-neon-cyan)(accent)
    - Tabs.test.ts ≥8 用例全绿
  </acceptance_criteria>
  <done>Tabs 契约落地:Phase 4/5 分组 UI 可直接消费;键盘用户与屏幕阅读器完整可达(D-19)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 工具输出文本 → CopyableText 展示区 | 工具输出可能含用户粘贴的脚本载荷(经 Phase 1 消毒管线后的字符串) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | CopyableText 展示区渲染用户内容 | medium | mitigate | 展示区与反馈全 {{ }} 插值(自动转义),零 v-html(测试断言含脚本标记的文本不产生 script 节点);ESLint vue/no-v-html 卡口不变 |
| T-02-07 | DoS | 超长文本渲染 | low | accept | 展示区 max-h-72 限高滚动,DOM 节点量与文本长度线性;无递归/无无限循环面 |
| T-02-SC | Tampering | npm/pnpm 包安装 | high | mitigate | 零新装(图标 Copy/Check 来自已过闸的 @lucide/vue);禁止 pnpm update |
</threat_model>

<verification>
- corepack pnpm vitest run src/ui/Tabs.test.ts src/ui/CopyableText.test.ts 全绿
- corepack pnpm type-check && corepack pnpm lint 退出码 0
- corepack pnpm vitest run 全量无回归
- CopyableText 文案四行与 UI-SPEC Copywriting Contract 逐字一致(人工对照)
</verification>

<success_criteria>
- CopyableText 三态反馈(空/成功/失败)+ 限高滚动全文复制落地,D-17 能力层复用零重写
- Tabs WAI-ARIA 完整键盘模式落地,活动态消费 accent,单/多 tab 一致
- 两组件 44px 触控目标与 focus-visible 焦点环内置(D-19/D-21)
</success_criteria>

<output>
Create `.planning/phases/02-design-system-a11y-baseline/02-04-SUMMARY.md` when done
</output>

